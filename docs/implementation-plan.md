# V2 剩余交付 Gate

状态：V2-only 工程能力和发布验收门禁均已闭合；当前代码版本仍归入
`CHANGELOG.md` 的 `Unreleased`，尚未通过 tag 或独立发布批次声明为正式 `0.2.0` release。
本文只记录尚未闭合的外部 Gate，不保存已完成章节、施工波次、会话分工或历史问题。

产品、架构、时间线、API、运维和验收以 `docs/software-business-design.md` 为唯一权威；候选、
过滤、评分、风险、DeepSeek、融合和排名以 `docs/recommendation-strategy.md` 为唯一权威；依赖、
构建和入口以 `pyproject.toml` 为唯一权威。历史交付证据位于 `CHANGELOG.md` 和 `docs/reports/`。

## 1. 正式 0.2.0 发布声明

状态：未发起。

Gate：用户显式发起独立发布批次；该批次重新执行发布门禁，把当前 `Unreleased` 内容归入带日期的
`0.2.0` 版本段，并在提交、推送和上游核对完成后才能创建同一提交的版本 tag。未满足这些条件前，
权威文档只能表述为“V2-only 工程与发布验收已完成”，不得表述为已经正式发布。

## 2. 评分研究外部证据

状态：`score_p0_v2` 历史采集已失败；等待后续评分规范冻结后预注册新身份。

- `score_p0_v2` 固定历史窗口为 2026-08-21 至 2026-10-23，固定前向窗口为 2026-10-26 至
  2026-11-20；2026-08-24、2026-08-25、2026-08-26 已缺失且不得回退、顺延或替换，最大可达 37/40，身份固定为
  `historical_collection_failed`。
- Score-R6/Score-R7 的生产晋级必须等待独立预注册前向身份、完整证据和人工确认；离线历史通过不
  产生生产权限，不得修改活动配置。
- 新身份不能复用 P0v2 日期、证据、成功状态或 schema；必须等因子、标签、模型、成本和有限挑战者
  规范全部冻结后，在任何新标签可见前另行预注册完整未来窗口。

## 3. 原生评分因子诊断层

状态：未开始，是下一项可执行评分工程章节。

Gate：以 tomorrow 的 14:50 至下一交易日收盘净超额为首要标签，在现有类型和点时证据边界内实现
每日 IC/Rank IC、五分组单调性、覆盖/缺失/衰减、换手、20/50/100bp 成本、严重亏损、MAE/ATR20、
板块/行业/市值/流动性分层和候选 oracle recall。该章节只建立研究诊断能力，不改活动评分、78/73
门槛、固定 68/32 融合、冻结、DeepSeek 请求或 Web；完成 Review、门禁、单提交和推送后停止。
