• 结论很明确：连续一个多月没有正式推荐，不能当成健康、正常的产品表现。单日 0 只合法，但长期为 0 必须区分是数据门禁、冻结错过，还是评分门槛过严。

  ### 这轮评分到底靠谱吗？

  分三层看：

  - 工程可靠性：比以前强。新的研究链已经具备点时数据、成本、风险、样本外检验和人工晋级门禁。
  - 收益可靠性：尚未证明。真实 40+20 日证据还没发生，没有 promotion_eligible。
  - 生产状态：新评分根本还没上线。当前生产仍使用原来的 78/73 门槛和 68/32 融合。

  新研究窗口要到 2027-06-14 才开始，2027-09-03 才结束；在此之前不能声称评分提高了收益。docs/fenshu.md:214 也明确记录了当前没有生产权限，docs/
  fenshu.md:221。

  所以，“新的评分系统导致今天没有推荐”这个判断不成立——因为它还没进入生产。

  ### 今天为什么仍然没有正式推荐？

  我在 2026-08-28 18:11 对真实服务进行了只读诊断：

   策略                             实际漏斗    当前直接原因
  ━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Today                     360 → 0 → 0 → 0    已过11:20冻结点，当前没有截止前正式结果，规则禁止盘后补算
  ──────────  ───────────────────────────────  ───────────────────────────────────────────────────────────
   Tomorrow    360 → 360 → 120 → 77 → 56 → 0    基础资料覆盖仅120/360，发布门禁失败
  ──────────  ───────────────────────────────  ───────────────────────────────────────────────────────────
   D25         360 → 360 → 120 → 79 → 57 → 0    基础资料覆盖仅120/360，发布门禁失败

  Tomorrow/D25 的 56、57 表示部分股票完成了评分，不表示360只全部评分后都没达到门槛。系统在评分发布前已经被 security_master_coverage_incomplete 挡住。

  Today 当前为 candidate_quotes_pending，但这是盘后状态。它只能解释“现在为什么不能补”，不能证明上午11:20前具体为什么没有形成结果；相关运行级日志证据不
  足，不能把原因猜成评分门槛。

  因此，这一轮只解决了漏斗被 UTC/上海时区错误卡成“采集中”的问题，没有解决长期没有正式推荐。当前最先需要解决的是基础资料覆盖长期停在120/360。只有数据门
  禁恢复后，才能进一步判断78分门槛是否确实过严。

### Web 解释修复结论（2026-08-30）

- 主结论已经按真实生命周期分流：首次评分显示“采集中”；候选行情、基础资料或历史覆盖不满足门禁时
  显示“暂不可发布”及当前值/要求值；评分已完成但正式池为空时显示最高最终分、距正式线差值、达到
  观察线/正式线数量；Today 错过 11:20 时明确说明当日不补算。
- 评分完成的空池从同策略类型化运行状态读取聚合原因，按数量稳定排序且最多显示三项；不再读取生产
  响应中不存在的临时字典字段。
- `/api/v2/status` 的 `supply_funnel` 新增达到观察线和正式线的精确计数，公开投影采用字段白名单；
  schema 升为 `v2_status_v4`，静态资源握手升为 `release-contract-2026-08-30-v5`，旧页面与新服务不会
  被误判为兼容。
- 普通页面继续隐藏上市日期和交易日龄缺失构成；这些事实仍参与内部门禁和脱敏诊断，不影响正确性。
- 成本后预期净超额、亏损概率、数据完整度和新评分版本仍以新评分取得生产晋级权限为前置条件；当前
  新评分尚未晋级，因此本批没有伪造或提前展示这些指标。
- 自动契约覆盖四种状态、覆盖值、最高分/门槛差、两档计数、前三原因及上市日期细节隐藏；本地真实
  浏览器夹具同时捕获采集中、120/360 基础资料阻断和评分完成空池，并在三档桌面分辨率检查布局。

### 基础资料覆盖修复结论（2026-08-30）

- “四五种来源”不等于上市日期有四五份独立供给。新浪、腾讯主要提供价格，120 积分 Tushare 不能调用
  需要 2000 积分的 `stock_basic`；生产中可免费提供全市场上市日期的活动路径实际只剩东方财富 `f26`。
- 东方财富富身份分页过去又与实时报价共用 20 秒截止。新浪先返回价格时，报价可以正常发布；东方财富
  分页一旦断连或超过截止，上市日期不会完成，系统只能继续使用已持久化的 120 条，因此形成
  “行情 360/360、基础资料 120/360”。
- 当前 release 已新增独立沪深交易所官方证券主数据通道，启动即刷新，不与报价 deadline 竞争；只接纳
  代码唯一、沪深两所齐全、三板不少于 4000 条且上市日期 100% 完整的原子快照，失败保留最近有效资料。
- 真实官方源实测为 5212/5212：上交所 2315、深交所 2897；正常重启后的状态同样为
  `total_rows=listing_date_rows=listing_age_rows=complete_rows=5212`，交易所来源一次成功且无持久化调度错误。
- 本批没有修改活动评分、78/73 门槛、68/32 融合、风险扣分或冻结。2026-08-30 为非交易日，不能用当日
  空漏斗评价 78 分是否过严；应在下一个真实交易窗口取得 360/360 门禁后的完整评分分布，再单独判断门槛。
