## 结论

  当前评分系统的工程约束、冻结机制和风险门禁比较扎实，但“评分本身”仍属于人工权重驱动的启发式模型，不是针对未来净超额收益校准出来的预测模型。

  提高收益的最佳路线不是继续微调 22.22%/27.78% 这些权重，而是建立新的影子评分体系：

  > 点时特征 → 残差化因子 → 个股成本 → 预期净超额 → 严重亏损概率 → 受约束选股 → 样本外晋级

  现有生产评分、68/32 融合和冻结契约暂时不动。新模型只有通过历史样本外和前向影子门禁，才能另立批次替换生产配置。收益改善只能定义为“成本后样本外净超额
  提高且尾部风险不恶化”，不能保证未来盈利。

  ## 重新确认的六个评分问题

   问题      1. 评分目标与收益目标没有直接对齐
   当前证据  当前是组件分位数加权为 0–100 分，78/73 也明确不是已证明的最优收益阈值。docs/recommendation-strategy.md:698
   可能影响  80 分和 75 分之间的差异没有明确的预期净超额含义
   优化方向  直接预测 14:50→下一交易日收盘的成本后净超额，再投影为展示分数
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   问题      2. 不同收益周期和方向被混合
   当前证据  Tomorrow 把尾盘、20–60 日趋势、突破、稳定性简单相加。src/trader/domain/recommendation/scoring.py:407
   可能影响  T+1 短期反转可能和中期动量互相抵消；尾盘同日延续不等于次日收益
   优化方向  拆成短期残差反转、中期残差动量、隔夜、日内和尾盘五类独立信号
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   问题      3. 候选层和评分层重复使用相同因子
   当前证据  Tomorrow 候选层先按流动性、趋势、稳定性筛选，正式评分再次使用趋势和稳定性。src/trader/domain/recommendation/scoring.py:345
   可能影响  因子被隐式重复加权，可能提前剪掉反转型或低趋势但高预期收益股票
   优化方向  候选层只负责可交易性、数据完整性和容量；Alpha 只在评分层计算
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   问题      4. 流动性、容量和 Alpha 混为一体
   当前证据  amount_percentile_20d 在候选、执行和 D25 量价组件中反复作为正向得分；量价确认只使用 5 日收益的正负号，忽略幅度。src/trader/domain/
             recommendation/scoring_calculations.py:211
   可能影响  高成交额股票可能被误认为更高收益；个股冲击成本没有进入预期收益
   优化方向  流动性拆成“交易容量”“价格冲击”“异常流量”三类，前两者进入成本或约束
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   问题      5. 风险和市场状态处理过于静态
   当前证据  市场状态固定映射为 60/50/40；稳定性使用总波动率和回撤的平均值。src/trader/domain/recommendation/scoring.py:419
   可能影响  无法表达“某因子只在特定状态有效”，也没有区分系统波动和个股残差风险
   优化方向  用市场状态作为交互变量；稳定性改为个股残差波动、下行波动和极端损失概率
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   问题      6. 当前证据不足以支持继续调权
   当前证据  score_p0_v2 计划 40 个历史交易日目前只记录 1 日，outcome 为 0；R6、R6D、R6S 均被门禁拒绝
   可能影响  继续搜索权重会放大数据窥探，无法获得生产晋级资格
   优化方向  先修复点时证据链；不可补写缺失日期，必要时以新研究身份重新预注册

  其中 R6 的结果最能说明问题：5 日净超额从基线约 0.367% 提高到 0.465%，但严重亏损率从约 4.53% 升至 8.17%，因此被拒绝；R6D 把代理基线的 -0.396% 提升至
  0.153%，但换手从 0.396 升至 0.473，同样未通过。.runtime/v2/score-r6/score_r6_historical_v1/historical-report.json .runtime/v2/score-r6-daily/
  score_r6_daily_trend_v1/historical-report.json

  ## 论文证据给出的方向

  - 短期反转与较长期动量可以同时存在，不能把所有过去收益统一作为正向趋势。Jegadeesh 1990
    (https://onlinelibrary.wiley.com/doi/abs/10.1111/j.1540-6261.1990.tb05110.x)

  - A 股研究显示，价值、风险和交易类异常相对稳定，而普通历史收益和质量因子的证据较弱；残差动量和残差反转更值得验证。A 股异常研究
    (https://pure.eur.nl/en/publications/anomalies-in-the-china-a-share-market/)

  - 中国市场确实存在日内时段之间的可预测性，但相关论文主要证明“前半小时/倒数第二个半小时预测最后半小时”，不能直接推导为次日收盘
    Alpha。中国日内动量研究 (https://ideas.repec.org/a/eee/ecmode/v76y2019icp319-329.html)

  - 动量风险会随市场状态变化，波动率管理可以显著减少动量崩溃。因此“稳定性”更适合作为条件暴露或风险门禁，而不是简单加分。Momentum Has Its Moments
    (https://ciencia.ucp.pt/en/publications/momentum-has-its-moments/)

  - 流动性既可能包含风险溢价，也代表交易冲击成本，不能简单把高成交额等同于高预期收益。Amihud 2002 (https://doi.org/10.1016/s1386-4181%2801%2900024-6)
  - 树模型的优势主要来自非线性交互；动量、流动性和波动率是重要信号，但这不代表深度模型一定适合当前样本规模。Gu、Kelly、Xiu
    (https://www.nber.org/papers/w25398)

  - 新建和维持仓位使用不同门槛，是降低换手成本的有效方法。交易成本研究 (https://doi.org/10.1093/rfs/hhv063)
  - 因子研究必须控制多重检验，普通的单次显著性不足以证明有效。Harvey、Liu、Zhu (https://www.nber.org/papers/w20592)

  ## 推荐的 Tomorrow 五个固定挑战者

  新研究只能预注册有限的五个本地挑战者，禁止无限增加因子。

  1. residual_reversal

     使用 1/3/5 日收益，扣除市场、板块、行业、规模和流动性影响；验证短期反转，不预设所有股票都应反向。

  2. residual_momentum

     使用 20–60 日收益，但跳过最近 3–5 日，并进行波动率缩放；避免把短期过热误认为中期动量。

  3. session_decomposition

     分开隔夜跳空、上午、下午、最后 30 分钟、收盘位置和尾盘量比。直接检验它们对下一交易日收盘的延续或反转，不沿用“尾盘上涨必然加分”的假设。

  4. cost_risk_adjusted

     新增 Amihud 价格冲击、成交容量、个股残差波动、下行半方差、极端收益和回撤恢复速度。容量只决定是否可交易，个股成本从预测收益中扣除。

  5. constrained_ensemble

     使用前四组特征建立两个固定模型：
      - 正则化线性/排序模型作为可解释控制组；
      - 浅层 LightGBM 作为非线性交互挑战者。

     限制树深、叶节点样本和特征集合，使用早停；不在同一窗口反复搜索超参数。

  DeepSeek 不计入这五个本地挑战者。它继续使用结构化事实和现有固定融合公式，但必须单独证明相对 local-only 的同日同股增量；不能证明时继续发布 local-
  only。src/trader/domain/recommendation/fusion.py:91

  ## 新评分的研究形态

  每只股票先输出具有业务含义的预测：

  gross_excess_i     = 预测相对基准超额收益
  estimated_cost_i   = 点差代理 + 冲击成本 + 换手成本
  net_excess_i       = gross_excess_i - estimated_cost_i
  loss_probability_i = 严重亏损概率
  uncertainty_i      = 模型分歧或预测区间
  research_utility_i = net_excess_i - λ * uncertainty_i

  处理原则：

  - loss_probability 用于风险上限和选股门禁，不用自由文本直接扣分。
  - 本地结构化风险仍然只扣一次，不能与模型风险重复扣除。
  - 0–100 分只是 research_utility 的稳定横截面投影，不再是未经校准的人工权重总和。
  - 只有预期净超额下界大于零的股票才进入正式池；允许返回空池。
  - Top6 在行业、板块和成本约束下最大化组合净效用，而不是简单逐股排序后贪心截断。

  ## 实施计划

  ### 批次 0：修复研究证据链

  - 定位 score_p0_v2 缺失计划日的原因。
  - 已错过的正式点时样本不得补写或用收盘数据替代。
  - 判断 P0v2 是否已经因无法达到 40 个完整计划日而永久失败。
  - 如需重启，使用全新研究身份、哈希和不重叠窗口，在任何标签可见前预注册。
  - 补齐点时股票池、历史 ST、行业、退市、公司风险和精确 14:50 输入；消除当前股票池幸存者偏差。

  ### 批次 1：建立原生因子诊断层

  参考 Alphalens/Qlib 的方法，在现有类型架构内实现，不直接引入整个平台：

  - 每日 IC、Rank IC、ICIR；
  - 五分组净超额和单调性；
  - 因子覆盖、缺失率、衰减、换手；
  - 分板块、行业、市值和流动性诊断；
  - 20/50/100bp 成本敏感性；
  - 严重亏损率、MAE/ATR20、集中度；
  - 候选剪枝前后的 oracle recall。

  ### 批次 2：增加点时残差特征

  先修改契约和测试，再增加上述五类 Tomorrow 特征。所有行业、财务和公告字段必须使用当时已公开版本，禁止使用报告期日期代替披露日期。

  ### 批次 3：影子模型与校准

  - 使用滚动/扩展窗口，不随机拆分时间序列。
  - T+1 至少隔离相邻标签；D25 使用更长 embargo。
  - 线性模型和浅层 LightGBM 使用同一数据、标签和成本。
  - 校准净超额预测及严重亏损概率。
  - 保存特征版本、训练窗口、随机种子、模型哈希和完整逐日预测。

  ### 批次 4：成本感知选择

  - 候选层删除趋势、稳定性等 Alpha 重复项。
  - 使用个股成本后的净效用排序。
  - 对 D25 验证“新进入门槛高于维持门槛”；Tomorrow 只有在不破坏固定持有周期时才测试稳定选择。
  - 保留 Top6、单行业最多 2、单板最多 60% 和合法空池。

  ### 批次 5：预注册与前向影子

  沿用现有严格框架：

  - 40 个历史样本外计划日；
  - 20 个连续前向计划日；
  - 至少 300 条可结算同日同股配对，前向至少 100 条；
  - 五个挑战者组成唯一 Holm 检验族；
  - 不允许根据中间结果增加第六个变体或修改阈值。

  ### 批次 6：人工晋级

  只有全部门禁通过后，才能另立高风险交付批次修改活动配置、策略文档和版本。生产不自动学习、不自动调权、不自动降低 78 分门槛。

  ## 晋级门禁

  候选方案必须同时满足：

  - 20bp 成本后，相对生产基线平均净超额增量至少 20bp；
  - Holm 校正后的区块 bootstrap 95% 下界大于 0；
  - 严重亏损率不高于基线；
  - 换手不高于门禁上限，并完整报告 50/100bp 压力结果；
  - 候选 TopK oracle recall 不低于 99%；
  - 最高分组净超额高于最低分组，平均 Rank IC 大于 0；
  - 删除贡献最大的月份后累计增量仍为正；
  - 删除贡献最大的板块后结果不为负；
  - 单股正贡献不超过 10%，前五只合计不超过 30%；
  - DeepSeek hybrid 必须单独证明相对 local-only 的增量下界大于 0，否则不启用 hybrid。

  ## 开源库使用建议

  截至 2026-08-26：

  - Qlib (https://github.com/microsoft/qlib) 约 47.9k stars：借鉴 Alpha158、IC/Rank IC、模型与组合双评价流程；不作为生产运行依赖。
  - LightGBM (https://github.com/lightgbm-org/LightGBM) 约 18.7k stars：仅作为影子浅层挑战模型。
  - vectorbt (https://github.com/polakowo/vectorbt) 约 8.8k stars：只用于快速敏感性探索，不能把大规模参数扫描结果直接作为晋级证据。
  - Alphalens (https://github.com/quantopian/alphalens) 原项目约 4.4k stars但维护较旧；可参考仍在维护的 Alphalens Reloaded
    (https://github.com/stefan-jansen/alphalens-reloaded)，或原生实现精简 tear sheet。

  - cvxportfolio (https://github.com/cvxgrp/cvxportfolio) 约 1.3k stars：借鉴显式收益、风险和交易成本目标；因 GPL 和依赖较重，不建议直接进入生产包。

  ## 任务状态（2026-08-28）

  - **批次 0：修复研究证据链**
    - [阻塞] 已确认缺失日没有 committed observation 或 V2 正式决策；因对应运行日志不存在，运行级直接原因仍待验证，不得把单日数据平面隔离记录写成确定因果。
    - [已完成] 已错过的正式点时样本由固定计划日、14:50 截止和不可替换契约禁止补写或用收盘数据替代。
    - [已完成] `score_p0_v2` 已永久无法完成 40 个历史计划日；2026-08-28 只读实测为 1 个有效记录、4 个已错过日期，最大可达 36/40，状态为 `historical_collection_failed`。
    - [等待前置条件] 新研究身份只能在新评分规范和完整未来窗口冻结后、任何标签可见前预注册；当前不得创建含义未定的占位身份。
    - [未开始] 补齐点时股票池、历史 ST、行业、退市、公司风险和精确 14:50 输入；消除当前股票池幸存者偏差。
  - **批次 1：建立原生因子诊断层**：[工程能力已完成] 已实现 `score_native_factor_diagnostics_v1`；真实 R2 覆盖不足 40 日，报告仍只能是 `exploratory`，不具备生产晋级资格。
  - **批次 2：增加点时残差特征**：未开始
  - **批次 3：影子模型与校准**：未开始
  - **批次 4：成本感知选择**：未开始
  - **批次 5：预注册与前向影子**：未开始
  - **批次 6：人工晋级**：未开始

  ## 本轮独立问题状态（2026-08-28）

  - **Web 推荐漏斗数据异常**：[待运行实证] 统一 `runtime` 诊断无法连接 `http://127.0.0.1:5000`，当前没有运行服务可采样；源码链路显示页面仅使用同策略、同交易日的 `scheduler.input_quality`，否则降级为市场预热摘要，但实际异常根因尚未确认。
  - **Skill 未触发**：[已纠正] 本轮已按 `AGENTS.md` 加载仓库级 `trader-delivery` skill，并按其影响矩阵、统一诊断和交付证据要求执行；此前未加载属于交付流程遗漏，不是 Web 或运行时会触发的产品能力。
