# 2026-07-20 全链路对照 TradingAgents 优化审查

## 背景

对照 `X:\github\TradingAgents` 开源库，对本地流水线各阶段（API 接口调用、标准化、性能、荐股评分、策略、过滤标准、DeepSeek API 使用、SQLite 使用、Web/SSE）进行系统性审查，提取可借鉴模式与优化建议。

---

## 一、当前流水线与代码映射（v2 活动代码）

| 阶段 | 主要实现位置 | 关键类/函数 |
|---|---|---|
| ① 全市场行情事件 | `infrastructure/market_data/eastmoney.py` `EastmoneyClient.fetch_market` | 多页并发抓取 + `_DIRECT_PROXIES` |
| ② 标准化 | `application/candidate_features.py` `fetch_strategy_features` | 把多源 quote 标准化为 `FeatureSnapshot` |
| ③ 硬过滤 | `domain/filters.py` `hard_filter` | `missing_ratio(CORE_FIELDS)>0.30` 截断 |
| ④ 特征快照 | `application/pipeline_stages.py` `_refresh_candidates_on_workers` | `preselect()` 限制 candidate pool size |
| ⑤ 三策略评分 | `domain/strategies/{today,tomorrow,d25,long}.py` → `domain/strategies/__init__.score_strategy` | `RecommendationEngine.preselect/build_snapshot` (`application/recommendations.py`) |
| ⑥ DeepSeek 五维批量复核 | `infrastructure/deepseek/reviewer.py` `DeepSeekReviewer._review` | `ReviewCache` + `DeepSeekBudgetStore` + `DeepSeekHttpClient` |
| ⑦ veto/风险合并 → TopK | `domain/risk.py` `derive_local_risk_facts` → `domain/ranking.py` `select_top_k` → `domain/fusion.py` `fuse_score` | 融合公式: `clamp(local*0.68 + deepseek*0.32 - deepseek_risk_penalty, 0, 100)`（`docs/need.md` §3） |
| ⑧ 单写线程 → SQLite + JSON | `infrastructure/persistence/writer.py` `SnapshotRepository.publish/freeze` | `_atomic_replace` + WAL 持久化 |
| ⑨ Web/SSE 只读发布 | `web/app.py` + `web/routes.py` + `web/sse.py` | `SnapshotPublisher` (`application/publisher.py`) |

`bootstrap.py:66-210` 是唯一组合根；事件调度在 `application/cadence.py`，冻结边界在 `application/schedule.py`。

---

## 二、与 TradingAgents 库的可借鉴机制对应（按阶段）

### A. 候选报价 / 新闻风险事件 API 调用

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `dataflows/interface.py` 的 `VENDOR_METHODS` + `route_to_vendor`：方法名→(vendor1, vendor2) 显式链；`OPTIONAL_CATEGORIES`（macro/prediction）失败降级为 sentinel 而非 raise；非 optional 类 raise | `infrastructure/market_data/gateway.py` `_fetch_market_once`：循环 `("eastmoney", "sina")` 写死 + 单 `tencent`；异常处理一致 | **抽 `MarketDataRouter`（`domain/ports.py` 协议 + `infrastructure/market_data/router.py` 实现）**：方法表 `{"quote.full_market": [eastmoney, sina, tencent], "news": [akshare, ...], "fundamentals": [akshare, ...]}`，支持 `optional`/`required` 标志 → 失败降级 sentinel（本地行情是 required，但新闻/基本面可降级）。**news risk** 现在是单 `AkshareResearchClient.fetch_news`，未来加新浪/腾讯新闻源零成本 |
| `dataflows/market_data_validator.py` `build_verified_market_snapshot`：把 stockstats wrap + 选定指标 + 最近 N 行渲染成"权威真值"字符串（**确定性 + 抵御 LLM 幻觉**） | 没有同类组件；DeepSeek 看到的 prompt 内部数值可能与 ground truth 不一致 | **新增 `infrastructure/market_data/ground_truth.py` `GroundTruthRenderer`**：把 `FeatureSnapshot` 序列化为 DeepSeek 可见的"权威数字快照"——OHLCV 关键位、ma5/ma20、量比、换手等只读字段——与"指标列名/计算口径"分离。这能直接压缩 DeepSeek 返回"看起来对但其实是幻觉"的边界 |
| `dataflows/interface.py` `route_to_vendor` 显式保留 first_error（**不让 fallback 静默吃掉 primary 错误**） | `gateway.py:_record(...)` 用滑动窗口（256 个延迟样本）已实现；`_fetch_market_once` 把 last_exception 抛 | 已对齐。**唯一补充**：`_is_open(name)` 用 `open_until > monotonic` 即可，**建议**增加 half-open 探测（一次成功调用后切回 closed 而非等超时） |

### B. 标准化 / 性能

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `dataflows/interface.py` 的 `TOOLS_CATEGORIES`/`VENDOR_METHODS` 字典 + `OPTIONAL_CATEGORIES` frozenset | 当前是写死函数调用 | **抽象 `StandardizedFeatureBuilder` 协议**：`def build_market_quote(raw: RawPayload) -> MarketQuote`、`def build_research_evidence(raw) -> Evidence`；各 provider 实现同一接口 → 让"标准化"成为可单测、可替换的横切关注点 |
| `dataflows/stockstats_utils.load_ohlcv` 用本地缓存 + 一致 schema | `infrastructure/market_data/features.py` `FeatureBuilder` | **抽 `FeatureSchema` dataclass**：列名/类型/缺失策略作为契约；FeatureBuilder 内部向量化（`pandas.Series` 一次计算所有指标而不是 `for col in cols`） |
| `dataflows/market_data_validator.py` 用 `pd.to_datetime` + dropna + 日期过滤 + 排序作 4 步防御 | 行情标准化已分散在 eastmoney/sina/tencent 三个客户端 | **抽 `infrastructure/market_data/normalize.py` `normalize_raw_payload(raw, source, now)` 单一入口**——三客户端把 `raw dict` 喂给同一标准化器，不再各自维护 `_quote_from_row`/`_object_mapping`（issue.md R20 已识别） |

### C. 荐股评分 / 策略

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `agents/utils/rating.py` `parse_rating`：决策文本 → `Rating` 枚举 | 本地 `domain/risk.py` 自由文本解析 + schema 校验 | **抽 `Rating` 枚举 + `parse_rating(text, fallback=NEUTRAL)`**，与 `RecommendationAction` 对齐；DeepSeek 自由文本的 rating 与本地"动作建议"映射变显式（issue.md R13 已识别，可顺势用此） |
| `agents/utils/structured.py` 结构化输出回填：JSON + repair prompt 循环 | `infrastructure/deepseek/schema.py` 已有 `classify_review`/`parse_reviews`/`build_repair_messages` | **借鉴**：把 schema 校验和"修复型 prompt"抽成可复用的 `JSONRepairLoop(max_attempts=2)`；当前 reviewer 的 `max_tokens=...` 与 `RETRYABLE_STATUS_CODES` 已经是工业级（见 `client.py:47`），保持现状 |
| `agents/utils/memory.py` `TradingMemoryLog`：append-only 日志 + 原子替换 + 旋转 + 幂等 | `infrastructure/persistence/writer.py` `_atomic_replace` 是同类实现 | **正向发现**：本地 `runtime_json.atomic_write_json`（已在 issue.md R25 标记为正面范例）已对齐 TradingAgents 的"temp + os.replace"模式。建议在 `infra/market_data/akshare.py` `fetch_news` 的 evidence 落盘处统一调用（目前 4 处 evidence 落盘，2 处直 `json.dump`） |
| `agents/schemas.py` 把"决策/反思/标签"作为 frozen dataclass + 显式 schema_version | `domain/models.py` 已有 `RecommendationSnapshot`（frozen） | 已对齐。**补充**：与 `core_stock_tools.py` 类似，**抽 `RecommendationSchema` registry**（`{"v13": fields_v13, "v14": fields_v14, ...}`），让 fusion/freeze/ranking 三处不再硬编码字段名 |
| `agents/utils/core_stock_tools.py` 工具函数分类（technical_indicators/fundamental_data/news_data/...） | `application/pipeline_stages.py` 的 `process_event_on_workers` 是 116 行巨型 if-elif（`pipeline_stages.py:67-115`）按 task 分发 | **借鉴 `TOOLS_CATEGORIES`**：把 `_score_strategies_on_workers`/`_refresh_*_on_workers`/`_run_market_data_task` 改为 `@register_task(PipelineTask.STOCK_RISK)` 装饰器注册；`process_event_on_workers` 退化为查表 `TASK_DISPATCH[event.task](pipeline, event)`——自然解耦 task→handler 关系（issue.md R11/R12 已识别） |

### D. 过滤标准

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `interface.py` 的 `OPTIONAL_CATEGORIES` 区分"必需 vs 选填" → 选填失败降级 sentinel | 当前 `domain/filters.py` `hard_filter` 是 **必须通过**（一个原因不过即拒绝） | **抽 `FilterRule(name, severity=required|optional, predicate, message)` registry**；`hard_filter` 改为 `apply_filters(snapshot, rules)` 聚合。`severity=optional` 失败 → 记 FilterAudit 但不拒绝；这与 TradingAgents 选填降级语义一致 |
| `market_data_validator.py` 失败 detail 透传给上游（"latest row is 2025-06-11 ... stale"） | `domain/models.py` `FilterAudit` 已含 `actual` 字段 | 已对齐 |
| `interface.py` `first_error` + `last_no_data` 区分"没有数据"与"出错了" | 本地 `MarketDataUnavailable` 是单一异常 | **抽 `MarketDataNoData`(symbol, detail) / `MarketDataFailed`(vendor, error)` 两种 sentinel**；`gateway.fetch_candidates` 用 sentinel 决定走 cache fallback 还是抛（已部分实现，但语义不显式） |

### E. DeepSeek API 使用

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `llm_clients/base_client.py` `BaseLLMClient` ABC + `validate_model` + `warn_if_unknown_model` | `infrastructure/deepseek/client.py` `DeepSeekHttpClient` 不是 ABC，是 concrete | **抽 `DeepSeekClientBase(ABC)`**：`complete(messages, *, timeout, max_tokens, model)`；`DeepSeekHttpClient` 与未来的 `MockDeepSeekClient`/`LocalDeepSeekClient`(vLLM) 都是同一接口。本地测试可注入 mock |
| `llm_clients/capabilities.py` 声明式 `ModelCapabilities` 表（`supports_tool_choice` / `requires_reasoning_content_roundtrip` / `requires_reasoning_split`） | 本地硬编码 `response_format={"type": "json_object"}`（`client.py:96`） | **借鉴**：`infrastructure/deepseek/model_capabilities.py` 声明 `MODELS = {"deepseek-chat": ModelCapabilities(thinking=False), "deepseek-reasoner": ModelCapabilities(thinking=True, ...)}`；`DeepSeekHttpClient.complete` 读 capabilities 决定是否发 `reasoning_effort` / 跳过 `tool_choice`。**这是 issue.md R9 config 分区的直接落点**——把 model 能力做成 config 而不是硬编码 |
| `llm_clients/factory.py` 惰性导入 provider（避免测试 collection 拉重 SDK） | `client.py:50` `cast(PostFunction, requests.post)` 是单后端 | **借鉴**：当未来接入 vLLM/Together 时，`create_deepseek_client(provider="http"\|"vllm", ...)` 工厂可零成本扩展 |
| `llm_clients/openai_client.py` `DeepSeekChatOpenAI._get_request_payload` 处理 `reasoning_content` round-trip | `client.py:88` `messages: Sequence[Mapping[str,str]]` 只接 str，thinking 模型会 400 | **强烈建议**：`messages` 改为 `Sequence[Mapping[str, Any]]` + `_normalize_messages` 处理 `reasoning_content` 字段——`deepseek-reasoner` 才能落地（issue.md R13 验证时如果开 reasoner 会立刻触发） |
| `llm_clients/openai_client.py` `NormalizedChatOpenAI.with_structured_output` 按 `ModelCapabilities.preferred_structured_method` 自动选 `function_calling`/`json_mode`/`json_schema` | 本地固定 `response_format=json_object` | **借鉴**：注册 capabilities 后，可对 `deepseek-v4-flash`（`json_schema=False`）自动改走 function_calling 路径 |
| `llm_clients/validators.py` + `model_catalog.py` 已知模型白名单 + `warn_if_unknown_model` | 无校验 | **借鉴**：`infrastructure/deepseek/model_catalog.py` 维护已知模型清单；`complete()` 入口校验，未知模型 `warnings.warn(RuntimeWarning)`——不阻塞但提示 |

### F. SQLite 使用

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `llm_clients/openai_client.py` 用 `with_structured_output` 把 LLM 返回强制 schema 化，再走 SQL | `infrastructure/persistence/sqlite.py` `connect` + `infrastructure/deepseek/budget.py` 用 `connection.executescript` 建表 | **借鉴**：抽 `infrastructure/persistence/sqlite.py` `init_schema(connection, version: int)` + `MIGRATIONS` 注册表（`{1: "CREATE TABLE ...", 2: "ALTER TABLE ... ADD COLUMN ...", ...}`）——现在 `budget.py:60-95` 是单一 `executescript`，未来加迁移要靠手工 `try: ALTER TABLE except`。这是 issue.md R20 的直接前置 |
| `budget.py:39-48` 已经做了 `0 ≤ daily_hard_limit ≤ 188` 强约束 + `stage_limits must sum to daily_hard_limit` 不变量校验 | 已对齐 TradingAgents 风格 | **正向发现**：本地 `DailyHardLimit` + `StageTargets/StageLimits` 已具备 OpenBB/TradingAgents 那种"声明式不变量"。**建议**把不变量校验抽成 `BudgetInvariant.validate(dict)` 通用入口，所有未来加 bucket 都走同一道 |
| `writer.py:_atomic_replace` + 目录布局（`published/`/`frozen/{strategy}/{date}/{snapshot_id}.json`） | 已工业级 | **借鉴 `memory.py` 的 rotation**：增加 `quarantine` 自动归档策略（已有目录，未见自动迁移）。**借鉴**：`TradingMemoryLog._parse_entry` 的 `[date \| ticker \| rating \| pending]` tag 模式——可应用到 DeepSeekReview 的"pending→success" 终态变更的 idempotency guard（避免 freeze 重复落地） |

### G. SSE / Web 路由

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `reporting.py` 把多 agent 决策 → markdown 报告 | `web/schemas.py` JSON 序列化 | **借鉴**：抽 `web/serializers.py` `serialize_recommendation(snap)` / `serialize_status(state)` / `serialize_event(event)`——`web/routes.py` 的 `routes.py:50-100` 错误响应拼字典重复 4 次以上，可收敛到单一 `error_envelope(code, message, **context)` |
| `graph/signal_processing.py` 把"图节点状态变化"翻译为"事件" | `application/events.py` + `SnapshotPublisher` | **借鉴**：抽 `web/sse.py` `SSEEvent.from_pipeline_event(event)` 适配器——SSE 不直接消费 PipelineEvent，先 map 成 `SSEEvent(type, payload, version)`，前端只关心 version 增量 |

### H. 组合根 / 应用容器

| TradingAgents 模式 | 本地现状 | 借鉴/落地建议 |
|---|---|---|
| `main.py` 启动：导入 → `default_config` → graph setup → 启动 | `bootstrap.py:66-210` `build_system` 是 145 行的 flat 工厂 | **抽 `bootstrap.container.ContainerBuilder`** 链式 API：`builder(settings).with_market_data().with_deepseek().with_repository().with_publisher().build()`——目前 21 个对象直接 `=` 赋值，缺分层；issue.md R16 已识别 |

---

## 三、关键优化点（按 ROI 排序）

### 第一档：直接解锁 plan.md 收益杠杆

| # | 优化点 | 文件 | 借鉴 TA 模式 | 预期收益 |
|---|---|---|---|---|
| 1 | `DeepSeekClientBase` ABC + `create_deepseek_client` 工厂 + 惰性 SDK 导入 | `infra/deepseek/client.py` | `llm_clients/base_client.py` + `factory.py` | 测试可注入 mock；未来 vLLM 零成本 |
| 2 | `ModelCapabilities` 声明表 + 自动 `tool_choice`/`json_schema` dispatch | `infra/deepseek/` (新增 `model_capabilities.py`) | `llm_clients/capabilities.py` | reasoner 模型可上线，json_schema 不可用时自动降级 |
| 3 | `messages` 支持 `reasoning_content` round-trip | `infra/deepseek/client.py` | `llm_clients/openai_client.py:88-129` | `deepseek-reasoner` 不再 400 |
| 4 | `GroundTruthRenderer` 渲染 FeatureSnapshot 权威数字快照 | `infra/market_data/` (新增 `ground_truth.py`) | `dataflows/market_data_validator.py` | DeepSeek 减少幻觉；与 `need.md` §3 "DeepSeek 自由文本不得直接扣分" 强对齐 |
| 5 | 抽 `MarketDataRouter`（方法表 + optional 标志） | `infra/market_data/router.py` (新) | `dataflows/interface.py:VENDOR_METHODS` | 新闻/基本面可降级为 sentinel；增删 provider 不动业务代码 |
| 6 | `Bootstrap ContainerBuilder` 链式 API | `bootstrap.py` (改) | `llm_clients/factory.py` | 21 个对象分层装配；可测性↑ |

### 第二档：解耦与可观测性

| # | 优化点 | 文件 | 借鉴 TA 模式 | 预期收益 |
|---|---|---|---|---|
| 7 | 任务分发改为 `@register_task` 装饰器 + `TASK_DISPATCH` 表 | `application/pipeline_stages.py` | `agents/utils/core_stock_tools.py:TOOLS_CATEGORIES` | 116 行巨型 if-elif → 20 行表查询；新增 task 不改 dispatcher |
| 8 | 抽 `FilterRule(name, severity, predicate)` registry；`hard_filter` 改为 `apply_filters` 聚合 | `domain/filters.py` | `interface.py:OPTIONAL_CATEGORIES` | 选填过滤不再阻塞；与"必填/可选"语义对齐 |
| 9 | 抽 `infrastructure/persistence/migrations.py` + `MIGRATIONS` 表 | `infra/persistence/` | `llm_clients/openai_client.py` schema 化思路 | 加列/索引不用手工 try-except |
| 10 | 抽 `StandardizedFeatureBuilder` 协议 + `FeatureSchema` dataclass | `infra/market_data/features.py` | `dataflows/interface.py` | 标准化可单测、可替换 |

### 第三档：性能向量化

| # | 优化点 | 文件 | 借鉴 TA 模式 | 预期收益 |
|---|---|---|---|---|
| 11 | `FeatureBuilder` 指标计算改 pandas 一次性向量化 | `infra/market_data/features.py` | `dataflows/stockstats_utils` | 候选构建延迟降 40-70% |
| 12 | `MemoryLog` 风格 append-only + 原子替换 + 旋转，应用到 evidence 落盘 | `infra/market_data/akshare.py` | `agents/utils/memory.py` | evidence 落盘故障率→0；统一调用 `runtime_json.atomic_write_json` |
| 13 | `BoundedExecutor` 改用 `concurrent.futures.ThreadPoolExecutor(max_workers=...)` 替代当前手写 queue | `application/workers.py` | 隐含在 TA 各模块 | 维护成本↓；现有 `borrow_executor` 已接近 |

### 第四档：契约/可观测性

| # | 优化点 | 文件 | 借鉴 TA 模式 | 预期收益 |
|---|---|---|---|---|
| 14 | `Rating` 枚举 + `parse_rating(text, fallback)` | `domain/risk.py` | `agents/utils/rating.py` | DeepSeek 文本与本地动作显式映射 |
| 15 | `web/serializers.py` 收敛 4+ 处错误响应重复 | `web/routes.py` → `web/serializers.py` | `tradingagents/reporting.py` | 路由代码量-30% |
| 16 | `MarketDataNoData` vs `MarketDataFailed` sentinel 区分 | `domain/ports.py` | `interface.py:first_error/last_no_data` | 失败原因可观察；前端可分别处理 |

---

## 四、对 `docs/need.md` 边界的严格遵守

- **不引入 TradingAgents 为运行依赖**（`need.md` §2 末段）；上述借鉴只抽取**模式/接口**到本地实现，不复制源码
- **如确需引用具体代码片段**（例如 `reasoning_content` round-trip 实现思路），固定 TradingAgents 当前 commit 到 `docs/ATTRIBUTION.md` 并注明许可证（need.md §2 明文要求）
- **每日 DeepSeek 188 次物理上限**保持不变（`bootstrap.py:131-137` 已有 `DeepSeekBudgetStore` 强约束）；**模型能力表是配置而非配额**——不会突破 daily limit
- **冻结边界** `today 11:20`/`tomorrow 14:50` 保持不变（`schedule.py`）；**`long` 仍不持久化历史**
- **本地推荐在 DeepSeek 失败时不能阻塞**（`pipeline_stages.py:197` 已有 `review_enabled` 短路；继续保留）

---

## 五、建议落地批次（与 `docs/issue.md` 一致性优先）

```
第一批(1-2 周, 收益杠杆):
    P1-DeepSeekClientBase + 工厂 (本表 #1)
    P2-ModelCapabilities + dispatch (本表 #2)
    P3-reasoning_content round-trip (本表 #3)
    P4-GroundTruthRenderer (本表 #4)
    + 同步推进 issue.md R6/R9/R14/R15 (config 改 + 退出策略类化 + 因子注册表)
    → 直接解锁 plan.md 阶段0/1

第二批(1-2 周, 流水线解耦):
    P5-MarketDataRouter (本表 #5)
    P6-Task dispatcher 装饰器 (本表 #7)
    P7-FilterRule registry (本表 #8)
    + 同步推进 issue.md R13 (BaseAlphaModule) / R11 (app_support 拆分)

第三批(1 周, 性能/可观测):
    P11-FeatureBuilder 向量化 (本表 #11)
    P12-evidence 落盘统一 (本表 #12)
    P14-Rating 枚举 (本表 #14)
    P16-MarketData sentinel (本表 #16)

第四批(1 周, 组合根与 Web 收尾):
    P6-Bootstrap ContainerBuilder (本表 #6)
    P9-migrations registry (本表 #9)
    P10-FeatureSchema (本表 #10)
    P13-BoundedExecutor 替换 (本表 #13)
    P15-web/serializers 收敛 (本表 #15)
```

---

## 六、风险与不做的事

- **不**直接 pip install TradingAgents：会导致 `need.md` §2 末段"如采用其他项目源码，必须固定 commit、核实许可证并记录归属"强制要求被绕过。本计划只搬运模式。
- **不**改 `docs/need.md` 公式（`local*0.68 + deepseek*0.32 - deepseek_risk_penalty`）或冻结时间（`today 11:20` / `tomorrow 14:50` / 188 上限）。
- **不**为 7 个策略模块在 v2 引入新接口（`issue.md` R13 BaseAlphaModule 是 plan 阶段 1 工作，不在本批 review 范围）。
- 当前 batch 已补齐前端可观测性入口：`/api/status` 路由健康 `route` 字段新增尝试计数字段后，前端状态摘要新增“路由健康”展示，支持 used_vendor、fallback_reason 与 vendor 级异常快速定位，路由收敛后端依赖与前端展示同时闭环。
- 借鉴时优先在 `infra/` 层落地（解耦基础设施），避免对 `application/` 的接口契约形成 break。

---

## 七、示意代码（仅作为说明，非本次交付）

如要落地 **第一档 P1 `DeepSeekClientBase` + 工厂**，本地实现可参照以下轮廓（**仅作为评审用的草图，不写文件**）：

```python
# infrastructure/deepseek/base_client.py
from abc import ABC, abstractmethod
from collections.abc import Mapping, Sequence
from dataclasses import dataclass

@dataclass(frozen=True)
class ModelCapabilities:
    supports_tool_choice: bool
    preferred_structured_method: str  # "json_object" | "function_calling" | "none"
    requires_reasoning_roundtrip: bool = False

class DeepSeekClientBase(ABC):
    @abstractmethod
    def complete(self, *, model, messages, timeout, max_tokens, capabilities: ModelCapabilities) -> "DeepSeekHttpResult": ...
    @abstractmethod
    def capabilities(self, model: str) -> ModelCapabilities: ...

# infrastructure/deepseek/factory.py
def create_deepseek_client(provider: str = "deepseek_http") -> DeepSeekClientBase:
    if provider == "deepseek_http":
        from .http_client import DeepSeekHttpClient
        return DeepSeekHttpClient()
    if provider == "mock":
        from .mock_client import MockDeepSeekClient
        return MockDeepSeekClient()
    raise ValueError(provider)
```

`review.py:53-99` 的 `DeepSeekReviewer._review` 不变，但构造函数由 `client: DeepSeekClientBase` 取代 `client: DeepSeekHttpClient` 具体类型；测试用例注入 `MockDeepSeekClient` 不再需要 monkeypatch。

---

## 八、2026-07-20 落地执行记录（本轮）

### 目标与状态
- 已完成：与 `docs/need.md` 核心约束一致的低风险落地（不引入新外部运行依赖、无 API/模型行为破坏、保持 long 冻结策略不变）。
- 本批范围限定为「评分与荐股策略优化」，不做历史数据策略验证。
- 已补充回归边界：本轮新增本地评分链路回归用例，直接验证 `local_strategy_weights` 传入会改变 `score_strategy` 结果并透传到 `RecommendationEngine` 的 snapshot 重放策略快照。
- 已落地项：
  - 本轮评分优化（本次本任务新增）：`infrastructure/settings.py` 增加 `local_strategy_weights` 配置，`RecommendationPolicy` 与 `recommendation_replay_policy` 增补本地策略组件权重注入与回放持久化，`application/recommendations.py` 在 `_local_candidate()` 与 `_merge_reviewed_candidates()` 中透传该权重链，确保 `today/tomorrow/d25/long` 本地评分组件权重完整落地闭环。`domain/models.py` 的 `FrozenReplayPolicy` 已补齐对应快照字段并保持旧快照兼容，`config/v2/strategy.json` 已新增 `local_strategy_weights`。
  - 过滤标准：`domain/filters.py` 通过 `FilterRule`/`FilterSeverity` 引入可选告警链路，`hard_filter` 现在返回 `FilterResult.optional_flags`；
    `application/recommendations.py` 在 `prepare_snapshot()` 中落地收集非阻塞告警，最终快照 `filter_details` 会同时保留必需原因和可选告警。
  - 市场数据路由：`infrastructure/market_data/gateway.py` 的全量行情回退改为使用 `MarketDataRouter` 的 `VendorRoute` + `route`（保留候选行情 `cross_source` 合并、熔断与缓存回退）。
  - 市场数据路由优化：`gateway.py` 现在一次性把供应商路由表交给 `route()`，改为顺序执行 `eastmoney -> sina` 的同一次路由尝试，减少循环组装开销并保留每个供应商的 `_record` 计数与熔断状态。
  - 市场数据异常分类：`infrastructure/market_data/router.py` 现在聚合 `MarketDataNoData` 与 `MarketDataFailed` 失败链路，required 供应商空数据会汇总到 `MarketDataNoData`，required 供应商异常统一为带 vendor 信息的失败摘要，网关可据此区分“无可用数据”和“上游故障”。
  - SQLite：`infrastructure/persistence/sqlite.py` 的 `initialize_database()` 增加 `apply_migrations(connection)` 调用，形成 `MIGRATIONS` 执行入口。
  - Web：新增 `web/serializers.py`，将路由层 `error`、`recommendation_dates`、`events` 的响应组合收敛到统一序列化入口；`routes.py` 保持只做输入校验与状态分支。
  - DeepSeek 评级审计：`infrastructure/deepseek/schema.py` 解析 `results[*].rating` 并通过 `domain.risk.parse_rating()` 转为受控枚举值；该字段只持久化和展示，不参与动作、分数、扣分、veto 或 TopK 排序。
  - DeepSeek 客户端抽象：新增 `infrastructure/deepseek/factory.py` 提供 `create_deepseek_client(provider)`，唯一组合根 `bootstrap.py` 通过工厂创建 `DeepSeekClientBase`，供应商替换不改变应用层依赖。
  - 第三档 P12 证据落盘统一：原始 payload 写入 `runtime/evidence_cache/raw/<source>/<code>.json`，标准化观察缓存写入 `runtime/evidence_cache/observations/<scope>/<code>.json`；两者共用流水线唯一持久化 worker，避免覆盖和并发写入。
  - 标准化收敛：新增 `infrastructure/market_data/normalize.py`，引入共享 `to_float`、`normalize_quotes`、`MarketQuoteInput` 与 `build_market_quote`；`eastmoney.py`、`sina.py`、`tencent.py` 统一用该入口构建 `MarketQuote`，非有限数值统一转为 `None` 丢弃，保持已有字段名与评分计算口径不变。
  - 第四档：补齐 `StandardizedFeatureBuilder` 协议并注入到 `MarketFeatureService`，为未来扩展独立标准化构建器预留契约。
  - 已闭环：`FeatureBuilder` 特征契约统一为 `FEATURE_SCHEMA`，并在启动配置校验 `factor_contract` 时完成 `feature_schema_version` 与 `FEATURE_SCHEMA` 的版本比对（含可选 `feature_names`/`feature_schema_expected` 一致性验证）。

### 实际行为差异（与原逻辑相比）
- 评分/荐股策略：本地评分 now 使用 `local_strategy_weights`，通过配置驱动 `today/tomorrow/d25/long` 各自组件权重，不再固定编译期常量；`strategy.json` 中新增权重即刻生效；回放快照会序列化该字段，兼容缺失时回退默认权重（未改变既有默认策略口径）。
- 评分回归：新增单测覆盖 `domain/test_strategies.py` 与 `application/test_recommendations.py`，确认权重覆盖可把某组件权重抬升到单权重通道并直接改变评分曲线，同时保证 snapshot 重放策略字段保留覆盖后的今日权重与排序结果一致。
- 过滤：可选问题（如跨源偏离）不再直接打回，仅进入告警列表；必需项仍会阻断候选。
- 行情回退：全量行情在 `eastmoney`、`sina` 间仍保持“前者失败则尝试后者”的顺序策略，失败样本转入“计划/错误计数”并保留缓存回退；`circuit_open` 仍会跳过当前供应商。
- 路由执行：全量行情改为一次性提交路由表给 `route()`，保留同样的 required 语义与熔断短路行为，行为差异仅在于降级/错误处理路径更集中。
- SQLite：数据库启动时会执行迁移注册表；`MIGRATIONS` 从 2/3 可持续用于 schema 版本演进。
- 性能：`FeatureBuilder` 现在调用 `infrastructure.market_data.history.summarize_history_metrics` 一次性获取每只票 `MA/波动率/回撤/中位数/上涨一致性`，减少 `_raw_features()` 内对 `history` 函数的重复遍历。对外计算口径与字段名不变。
- 异常分类：`router.py` 的路由返回会在 required 全部耗尽但仅遇到无数据时抛 `MarketDataNoData`，并在失败时汇总各供应商原因为 `MarketDataFailed`；测试补充 `tests/component/test_v2_market_data.py` 对路由级失败/无数据路径做回归。
- DeepSeek 评级：`parse_reviews` 支持审计字段 `rating`，缺失或异常值回退为 `neutral`；生产动作仍只由规范化最终分、时间窗口、行情新鲜度、硬过滤和经本地规则验证的 veto 决定。
- DeepSeek 供应商解耦：`bootstrap.py` 到 `DeepSeekReviewer` 的注入路径通过 `create_deepseek_client()`，同时保持 `bootstrap.py` 为唯一组合根。
- Web：`routes.py` 的错误响应、recommendation_dates、events 列表改为通过 `web/serializers.py` 输出，接口字段与错误码与之前一致。
- P12 证据落盘统一：`MarketFeatureService` 在候选研究获取链路中先尝试 `research_cache`，命中后直接恢复 `ResearchObservation`（包含 evidence、错误路径与有效期）；`AkshareResearchClient` 的新闻/财务/公告/质押/解禁 payload 均已写入统一 `atomic_write_json`，用于重启恢复和问题复盘。
- 标准化入口收敛：三类行情适配器共享同一行级转换器，`to_float` 对 `inf`/`nan`/空值统一丢弃，`normalize_quotes` 在生成器场景可直接消费 `payload` 迭代器，避免重复逐字段 `if` 分支；在保持字段映射与时钟行为一致前提下降低 parser 漏报/分支漂移风险。

## 九、2026-07-20 落地执行记录（续：标准化/路由/数据库）

### 目标与状态
- 继续执行 `docs/issues/2026-07-20.md` 第二档/第三档中可验证的后续项，重点补齐标准化、路由、SQLite 的可回归边界。
- 当前批次仍不进行历史数据策略验证，延续“本地推荐可用 + 外部失败降级 + 仅历史回放验证”的现网约束。

### 已落地
- 新增 `tests/unit/test_v2_market_data_router.py`：
  - 覆盖 `required` 与 `optional` 混合链路；
  - 覆盖 required 全耗尽时 `MarketDataNoData` 的汇总透传（含 no-data 与失败串行细节）；
  - 覆盖 required 全失败时 `MarketDataFailed` 的 vendor 摘要和错误归并顺序。
- 新增 `tests/unit/test_v2_sqlite_migrations.py`：
  - 覆盖 `initialize_database()` 首次建库后 schema 版本写入 `schema_meta`；
  - 覆盖重复初始化幂等性（schema 版本与已建库状态不重复漂移）；
  - 覆盖版本 1 的精简历史表在 `MIGRATIONS`+`_ensure_column` 下的向上迁移路径，确认 `schema_version/anchor_json/last_error` 可补齐。
- 标准化/路由/数据库继续推进（本轮）：
  - 标准化增强：`normalize.py` 将 `MarketQuoteInput` 固化为可校验的入参对象，强制校验 `code` 为 6 位数字、`source`/`data_version` 非空且去空白、`source_time`/`received_time` 必须为 timezone-aware；并在 `normalize_quotes()` 内对非法 `MarketQuoteInput` 进行隔离。坏行在标准化阶段被降级丢弃，不影响同批其余代码。
  - 路由增强：`router.py` 将空结果判断收口到 `_is_empty_payload()`，避免字符串/二进制等非标准集合类型被误判，统一空结果语义。
  - 路由细化（本批补充）：`router.py` 将可选源无数据场景归为 `status="skipped"`，`gateway.py` 中腾讯候选路径熔断时也记入 `skipped`，用于区分“功能性失败”和“回退跳过”。
  - 路由可观测性细化：`router.py` 的 `VendorResult` 与 `RouteOutcome` 增补 `status`、`severity`、`duration_ms` 及 `fallback_reason`，`circuit_open` 与当前熔断态 vendor 标记为 `skipped`；`gateway.py` 在 `health()` 下新增 `route` 子字段，记录最近一次路由尝试的 `attempted_count/success_count/failure_count/no_data_count/skipped_count`、`used_vendor`、`degraded` 与 `fallback_reason`，以及逐 vendor 的 `status/severity/error/duration` 明细。
  - 数据库增强：补充 `tests/unit/test_v2_sqlite_migrations.py` 对损坏/空白 `schema_version` 的自恢复回归（覆盖 `N/A` 与 `''`），确保版本兼容路径不会因异常或空白元数据卡住初始化。
  - 数据库增强（深度补齐）：`infrastructure/deepseek/budget.py` 增加 `schema_meta` 持久化版本字段与自恢复逻辑；`tests/component/test_v2_deepseek.py` 新增 `schema_version` 缺失/无效恢复回归，确保 DeepSeek 调用预算库在重复初始化与脏元数据场景下不会进入不可恢复状态。
  - 数据库增强（闭环补齐）：`infrastructure/persistence/sqlite.py` 与 `infrastructure/persistence/writer.py` 增加 `data_source_health` 的路由级审计字段（`route_json`、`route_status`、`route_fallback_reason`、`route_degraded`），在每次 `pipeline._record_health_snapshot()` 持久化时落盘 `health()["route"]` 的聚合状态，完成「路由可观测性细化」到持久化的闭环，便于重启后做路由健康趋势回放。

### 实际行为差异（与上轮基线相比）
  - 标准化/契约边界测试未改动行情输出字段口径，只提高路由与迁移错误语义回归覆盖率，降低回归遗漏。
  - 因子特征注册表闭环测试新增：`feature_schema_version` 错误配置会拒绝启动；显式提供 `feature_names/feature_schema_expected` 时会与注册表全集一致性校验（无缺失/多余）。
- 路由可观测性回归补齐了 vendor 级失败链路与耗时细节：`health()` 的 `route` 字段可直接显示最近一次路由失败/降级来源、每个 vendor 的 `status/severity/error/duration`，含 `attempted_count/success_count/failure_count/no_data_count/skipped_count`；`record_data_source_health()` 持久化时会把该 `route` 快照写入 SQLite `data_source_health`（含状态、回退原因、是否降级），前端状态摘要同步展示本次路由健康态势（包括 `used_vendor` 与 `fallback_reason`），后续可直接用于故障归因面板和测试契约。
- SQLite 迁移回归覆盖了“重复初始化 + 老 schema 升级”两个高风险操作分支，并新增对空白 `schema_version` 元数据的回退修复，补齐了 `schema_meta`/`MIGRATIONS` 的演进入口稳定性。

## 十、DeepSeek 审计字段闭环（2026-07-20）

### 目标与范围
- 落实 DeepSeek 审核可观测字段在“模型输出解析 + 审核终态注入 + 快照持久化 + API 输出”全链路闭环。
- 继续沿用 `today/tomorrow/d25` 评分公式与冻结边界，不做收益率/策略验证行为变更。

### 已完成（本轮）
- 已完成 schema 层审计字段默认回退：
  - `parse_reviews` 已支持 `review_stage/challenger_status/requested_model/actual_model/thinking_mode/raw_confidence/calibrated_confidence/evidence_manifest_hash/calibration_version/rating`；
  - 缺省时回退 `review_stage=primary`、`challenger_status=not_run`、`rating=neutral`；
  - `evidence_manifest_hash` 缺失时回退为 `build_review_manifest_hash(candidate)`，避免链路中断。
- 已完成可读/审计数据闭环：
  - `infrastructure/deepseek/reviewer.py` 审核短路与降级路径均通过 `_annotate_review` 写入审计元信息；
  - `infrastructure/persistence/snapshots.py` 持久化/反序列化扩展 `review_stage/challenger_status/requested_model/actual_model/thinking_mode/raw_confidence/calibrated_confidence/evidence_manifest_hash/calibration_version`，并对旧快照字段缺失提供向后兼容默认值；
  - `web/schemas.py` `review` JSON 节点透出新增审计字段。
- 已补充回归测试：
  - `tests/component/test_v2_deepseek.py`：schema 缺省与显式字段解析、`enabled=False` 快速降级时审计元数据注入；
  - `tests/component/test_v2_persistence.py`：带审计字段的 `DeepSeekReview` 持久化往返与字段缺失回退；
  - `tests/contract/test_v2_web_api.py`：API `recommendations` 返回中的 review 审计字段透出校验。

### 当前行为确认（与上一轮相比）
- 审计字段未参与融合计算，仅用于可观测和风险回归统计，`local_score/deepseek_score/final_score` 计算路径保持不变。
- 无需新增外部配置，仅在现有 DeepSeek 路由和快照结构中扩展字段，符合 `docs/need.md` “禁止重返配置文件”约束。

### 后续残余
- 审计信号保持只读；任何进入生产排序的提案必须先更新唯一业务契约并完成离线点时验证。

## 十一、审计信号生产隔离（2026-07-20）

### 目标与范围
- 审计信号（`challenger_status`、`review_stage`、`rating`、置信度）只用于 API、持久化和离线 shadow 观察，不进入生产排序或动作。
- 生产排序严格保持 `final_score -> local_score -> code`，风险影响只允许来自经 schema 和证据验证后由本地规则映射的 risk fact。

### 已完成（本轮）
- `src/trader/domain/ranking.py` 保持固定生产排序键，不读取 review 审计字段。
- `tests/unit/domain/test_ranking.py` 验证不同审计状态不会改变同分候选的本地分与代码顺序。

### 当前行为确认（与上一轮相比）
- 排序严格使用 `final_score`、`local_score`、代码，审计字段不会改变同分候选顺序。
- 无任何公式参数变化，`local_score/deepseek_score/final_score` 与融合路径、冻结策略、`minimum_top_k` 阈值均未变。

### 当前残余（下一步）

- 已完成收益型 shadow 影子实验（不改生产公式）：新增 `domain/strategies/shadow.py` 的 V10 候选路径（行业/领导对照、流动性稳定性、可执行性与样本完整度）并在 `application/recommendations.py` 的快照元数据注入 `shadow_scoring`；本批仅产出可观测报告，不改变 `minimum_score`、`select_top_k` 门限、`68/32` 融合公式、冻结时间与 `today/tomorrow/d25` 风险边界。
- 影子实验对齐 v10 研究草案的 `1/3/5`、`5/20`、`20/60` 同行收益差与板内对照计划，仅用于离线解读 `top_shadows`/`rank_gap` 与 `coverage.overlap_codes`，不触发动作与阈值改写。
- 当前可用后续入口：`/api/recommendations` 返回 `metadata.shadow_scoring`，用于 tomorrow/d25 今日到 2-5 日收益策略效果观察，不影响现网推荐动作序列。
- 剩余项（待验证）：本批仅完成影子打分与可观测埋点，尚未定义收益/回撤/覆盖率阈值与真实交易日对照闭环，后续需用独立章节补齐 `shadow` 观察指标与发布条件。
