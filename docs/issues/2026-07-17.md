# 2026-07-17 用户 Bug 记录

本文件归纳 2026-07-17（Asia/Shanghai）用户反馈、当前判断、修改说明和剩余工作，用于交付追踪，不替代 `docs/need.md` 的业务与验收契约。状态为“待验证”的内容不得视为已确认根因或已完成修复。

## 状态说明

- `已修复`：代码、回归和交付已完成。
- `待运行生效`：代码已交付，但旧服务进程尚未加载变更。
- `待验证`：仓库已有相关实现或测试，但用户报告的真实场景尚未闭环。
- `待实现`：尚未进入对应独立交付批次。

## 问题与修改说明

| ID | 用户提出的问题 | 原因或现状判断 | 修改说明与当前状态 | 后续验收 |
| --- | --- | --- | --- | --- |
| BUG-20260717-01 | 东方财富出现 TLS EOF/远端断开，新浪分页出现 504，导致 `market data degraded`。 | 外部行情连接与网关存在瞬时故障；本机代理还可能放大行情域名的 TLS 不兼容。 | `已修复`：行情请求显式直连，东方财富/新浪增加有界重试；双源失败时保留最近有效快照并显式降级，不阻塞只读页面。 | 真实交易日继续观察来源成功率、行情年龄和降级恢复。 |
| BUG-20260717-02 | 今早、明日、2-5日三个按钮切换时长期停留“正在读取推荐快照”。 | 页面曾在每次切换时重复等待日期和快照请求，缺少内存快照优先展示与同键请求合并。 | `已修复`：三策略启动后预取，日期与推荐并行请求，相同策略/日期复用内存、ETag 和在途请求。 | 真实服务下观察首次加载与后台刷新耗时。 |
| BUG-20260717-03 | 多个未获取指标和 DeepSeek 字段被显示为 `0.00`，无法区分真实零分与缺数据。 | 浏览器执行 `Number(null)` 得到 `0`；未复核模型字段也沿用了数值格式化。 | `已修复`：普通缺失显示 `-`，缺失数据标记“未获取”，未执行 DeepSeek 复核显示“未复核”，真实数值零仍显示为零。 | 刷新后的页面核对表格与明细抽屉。 |
| BUG-20260717-04 | 明细页没有展示 `missing_fields` 及缺失原因。 | API 只有缺失字段列表，前端缺少原因映射和展示区。 | `已修复`：推荐 API 已增加 `missing_reasons`，抽屉已增加缺失字段区；服务已重启并加载新序列化。 | 真实 tomorrow 响应已确认同时包含 `missing_fields` 和 `missing_reasons`。 |
| BUG-20260717-05 | 新闻数据存在真实结构化失败，要求修复 AKShare JSONP 响应解析。 | `待验证`：仓库变更日志已有 JSONP 规范化组件覆盖，但用户报告的真实响应仍未闭环，不能据此宣告生产问题已解决。 | `待验证`：本批次未修改 AKShare；下一 P1 批次需要保存脱敏响应形态、复现解析失败并补回归。 | 使用真实但脱敏的 JSONP/异常响应验证解析、超时、缓存和降级。 |
| BUG-20260717-06 | DeepSeek 分数为零且处于 `local_degraded`，此前提供的有效密钥没有产生真实调用。 | 密钥有效性不是当前问题；活动服务状态曾显示未配置，说明密钥没有注入实际服务进程，且尚无非零物理调用证据。 | `待实现`：本批次未注入密钥、未重启服务、未发起真实 DeepSeek 请求，也未记录任何密钥内容。 | P1 批次将密钥安全注入实际进程，验证 `configured=true`、物理调用计数非零、schema 结果和阶段总结。 |
| BUG-20260717-07 | 财务、公告、质押、减持解禁和尾盘分钟指标长期为占位零值。 | 这些上游数据尚未完整接入，部分零值属于硬编码占位而非成功结构化的真实数据。 | `待实现`：本批次未开始 P2 数据接入；当前页面仅先保证缺失值不再伪装为 `0.00`。 | P2 批次接入真实数据、删除占位零、记录数据时间与来源，并覆盖失败降级和缺失字段。 |
| BUG-20260717-08 | 11:20 后 today 没有展示冻结数据。 | 冻结事件只在 11:20-11:21 触发，下午启动会永久错过；现场也确认当日没有 today published/frozen 文件。 | `已修复`：启动时补提交当日截止前 30 秒内最后有效草稿，截止后查询只接受 committed 冻结记录，禁止用旧数据或迟到行情补造。 | 重启后 today 明确返回 `not_ready`，状态记录缺少截止前快照；当前日无法合规重建，后续交易日验证 11:20 冻结。 |
| BUG-20260717-09 | tomorrow 与 d25 看起来相同，刷新或切换时又出现另一波数据。 | 现场两份冻结快照 ID、data version 和分数不同，只是共享候选池最终选出同 3 只股票；前端会先显示不限年龄的旧草稿缓存，再被新响应替换。 | `部分修复`：API 截止后强制读取各策略冻结记录；前端校验策略/日期，只复用冻结快照或 30 秒内草稿；策略真实数据缺口另见下方审计队列。 | 2026-07-17 当次连续查询中，tomorrow/d25 分别读取 `6c28e031aa547eceab294bf9` 和 `fe5a72cd180202157ef08c28` 且未漂移；具体 ID 只作为当次证据，新交易日或新冻结必须生成新 ID。 |

## need.md 符合性审查与修复队列

本节记录 2026-07-17 对 `docs/need.md`、活动代码、配置、测试和真实服务状态的重新审查。具体 snapshot_id 只作为当次运行证据，不是固定配置、代码常量或长期验收值。同一 committed 冻结快照连续读取时 ID 必须不漂移；新交易日、新数据版本或新合法冻结必须产生新 ID。

### AUDIT-20260717-01 冻结 ID 证据措辞误导

**需求条款**：第 18 节冻结不可变与第 25 节冻结一致性。

**证据与影响**：旧记录使用“ID 分别稳定为”描述一次查询结果，容易被理解为永久固定 ID，不能证明后续交易日更新正确。

**修复步骤**：1. 将旧记录改为“当次连续查询未漂移”。2. 明确具体 ID 仅用于当次取证。3. 增加跨交易日和新冻结后 ID 必须变化的契约测试。

**验收条件**：同一 committed 快照重复查询 ID 相同；新交易日或新合法冻结的 ID 与上一份不同；配置和代码中不存在固定业务 snapshot_id。

**交付章节**：第 18 节。**状态**：`已修复`。

### AUDIT-20260717-02 today 观察期可能产生可执行动作

**需求条款**：第 5 节 09:30-09:36 只观察；第 17 节动作门槛。

**证据与影响**：动作逻辑把非 late 的 today 阶段映射到 `today_main`，`today_observe` 高分候选可能错误标为 executable。

**修复步骤**：1. 已建立阶段到动作资格的显式表。2. `today_observe` 任意分数最多返回 observe；安全 veto 仍可返回 unavailable，但绝不 executable。3. 已覆盖观察期、主窗口、降级窗口和跨策略阶段边界。4. 快照中的动作与原因统一由同一函数生成。

**验收条件**：观察期任意分数均无 executable；09:36 起才按 70 分门槛判断；冻结和历史记录保留当时动作原因。

**交付章节**：第 17 节。**状态**：`已修复`。

### AUDIT-20260717-03 TopK 使用低于门槛候选补满

**需求条款**：第 17 节允许 0 只、行业最多 3 只、不足不得降低门槛补满。

**证据与影响**：当前 TopK 对全部候选排序，未先排除 `unavailable`，可能把低于门槛股票当成推荐并掩盖真实空推荐日。

**修复步骤**：1. TopK 前按策略和阶段计算最低观察边界。2. 低于动作门槛 5 分的候选不进入推荐；高分但因 veto、新鲜度或核心字段被安全拦截的候选可保留展示为 unavailable。3. 合格候选排序后再执行行业上限，禁止降低门槛回填。4. 已增加 0 只、少于 K、行业集中和并列分测试。5. 新增冻结输入阈值预注册报告，覆盖完整候选分布、推荐数、空推荐比例、TopK 变化、DeepSeek 覆盖、降级比例和风险拦截率。

**验收条件**：无人达到最低观察边界时返回 0；低于门槛 5 分的 unavailable 不进入 items；行业限制不会触发降门槛回填。

**交付章节**：第 17 节。**状态**：`已修复`。

### AUDIT-20260717-04 生产策略输入存在 None 和硬编码零占位

**需求条款**：第 6、8、10-13 节真实数据、缺失策略和四策略评分。

**证据与影响**：尾盘、新闻、财务、公告、质押、解禁及 long 五类因子未接入；部分风险字段硬编码 0，使 tomorrow 尾盘结构、long 排序和本地风险失真，并放大 tomorrow/d25 同股现象。

**修复步骤**：1. 建立每个字段的数据源、时点、单位和缺失原因映射。2. 接入候选尾盘分钟数据及财务、公告、质押、减持解禁。3. 将新闻证据结构化为情绪和新鲜度。4. 删除占位零，失败时保留 null。5. 用录制响应覆盖成功、缺失、过期和结构化失败。

**验收条件**：生产 FeatureBuilder 不再生成占位风险零；真实可用数据产生非空值；不可用时 API 为 null 并说明原因；四策略使用各自真实组件。

**交付章节**：第 6、8、11-13 节，按整节分批。**状态**：`待修复 P0`。

### AUDIT-20260717-05 冻结校验使用快照时间而非逐条报价时间

**需求条款**：第 7、18 节 today 20 秒、tomorrow/d25 30 秒锚点年龄。

**证据与影响**：补冻结统一检查快照 `published_at` 距边界 30 秒，没有验证每条推荐的 `quote.source_time`；today 还错误放宽到 30 秒。

**修复步骤**：1. 为策略定义 20/30 秒冻结新鲜度。2. 在冻结事务前逐条计算边界时报价年龄。3. 任一可执行项超龄时降为观察或拒绝整版冻结，并记录原因。4. 补迟到报价、混合年龄和重启补冻结测试。

**验收条件**：today 锚点全部不超过 20 秒，tomorrow/d25 不超过 30 秒；冻结 manifest 记录来源时间和年龄；迟到数据不得改写冻结。

**交付章节**：第 18 节。**状态**：`已修复`。

### AUDIT-20260717-06 冻结后 live_overlay 和收盘更新缺失

**需求条款**：第 5、7、18 节冻结记录不可变但展示报价继续更新，15:00 保存收盘行情。

**证据与影响**：FROZEN/AFTER_CLOSE 阶段没有 TopK 定向报价分支，状态统计直接排除冻结策略，页面只能看到冻结锚点而不是独立最新报价。

**修复步骤**：1. 新增不写推荐历史的 live_overlay 模型和存储。2. 冻结后只对 TopK 定向刷新腾讯报价。3. SSE 发布 overlay 版本但不改变 snapshot_id、排名、分数和动作。4. 15:00 保存收盘 overlay。5. 增加哈希不变和报价可变测试。

**验收条件**：冻结 JSON/manifest 哈希不变；最新价、今日涨跌和锚点至今持续更新；15:00 收盘值可恢复；慢源失败保留最后 overlay。

**交付章节**：第 18 节。**状态**：`已修复`。

### AUDIT-20260717-07 有效 DeepSeek 密钥未进入实际服务进程

**需求条款**：第 14-16、22、25 节，密钥有效且有候选时物理调用不得为 0。

**证据与影响**：真实状态为 `enabled=true`、`configured=false`、used=0、原因 `api_key_missing`；密钥有效性与进程注入被错误混为一谈。

**修复步骤**：1. 确定受保护的本地密钥加载方式且不写入 Git。2. run 脚本在启动边界显式注入或读取保护文件。3. 状态只报告 configured 布尔值。4. 在有候选阶段完成一次受控真实调用并核对预算、schema、证据和缓存。5. 轮换密钥后验证旧进程不泄漏。

**验收条件**：实际进程 configured=true；候选存在时 physical attempts 非零；密钥不出现在日志、快照、页面、提交或进程参数中。

**交付章节**：第 14-16 节。**状态**：`待修复 P0，真实交易时段验收`。

### AUDIT-20260717-08 候选池在历史因子加载前形成

**需求条款**：第 6、8 节预热、候选分和点时历史因子。

**证据与影响**：全市场构建只使用已有历史缓存，未入旧候选的股票通常没有历史；候选选出后才加载历史，形成自我强化和候选池偏差。

**修复步骤**：1. 盘前按全市场或分层预选加载必要历史摘要。2. 将候选预选需要的历史字段放入版本化缓存。3. 候选计算只使用同一 data_version 的完整横截面。4. 记录历史覆盖率，覆盖不足时不生成可执行候选。5. 增加冷启动和缓存过期测试。

**验收条件**：冷启动候选池不会因无历史而默认中性；120 只候选的历史字段覆盖达到契约；缓存失败显式降级且不猜测。

**交付章节**：第 6、8 节。**状态**：`待修复 P0`。

### AUDIT-20260717-09 20 日成交额缺失被硬过滤放行

**需求条款**：第 9 节 20 日成交额中位数至少 5000 万，过滤结果可审计。

**证据与影响**：当前只拒绝存在且低于阈值的值；缺失值直接通过，后续核心字段缺失比例也可能不足 30%，导致缺历史股票进入动作判断。

**修复步骤**：1. 将流动性门槛字段列为硬过滤必需值。2. 缺失生成独立 filter_code 而非当作零。3. 保存阈值、实际 null、来源和时间。4. 增加缺失、4999 万、5000 万和非有限值边界测试。

**验收条件**：缺失和非有限中位成交额均不可进入生产评分；5000 万精确通过；过滤详情可在冻结输入复算。

**交付章节**：第 9 节。**状态**：`待修复 P0`。

### AUDIT-20260717-10 横截面未执行截尾且部分缺失被当作零

**需求条款**：第 8 节 2.5%/97.5% 截尾、平均秩、非有限和缺失规则。

**证据与影响**：winsorize 仅定义未进入生产链；市场宽度把缺失涨幅计为非上涨，可能系统性压低宽度和市场状态。

**修复步骤**：1. 按同 trade_date、phase、data_version 建立横截面。2. 对登记因子先截尾再平均秩。3. 缺失从分母或按登记策略处理，禁止隐式 0。4. 保存截尾边界、样本数和缺失掩码。5. 增加极值、并列、NaN、单样本测试。

**验收条件**：生产分位严格使用截尾后有效样本；缺失不改变为数值零；冻结 JSON 能重现边界与排名。

**交付章节**：第 8 节。**状态**：`待修复 P1`。

### AUDIT-20260717-11 因子与风险配置不是完整唯一真相源

**需求条款**：第 8、10、13 节逐因子登记、风险触发表和不可变版本。

**证据与影响**：strategy.json 只有通用 contract、权重和风险扣分，缺少逐因子公式及风险阈值；风险阈值和市场状态分类仍硬编码，strategy_version/config_version 也未从完整契约生成。

**修复步骤**：1. 定义并校验 factor registry schema。2. 登记所有生产因子的输入、公式、方向、时点、复权、窗口、样本、截尾、缺失和版本。3. 展开风险适用策略、触发公式、阈值和事实 ID 字段。4. 从规范化配置生成版本哈希。5. 禁止未登记因子进入评分。

**验收条件**：启动时拒绝缺登记、权重错误和无效区间；代码不再自行选择风险阈值；冻结快照保存完整版本和可复算策略。

**交付章节**：第 8、10、13 节，按整节分批。**状态**：`待修复 P1`。

### AUDIT-20260717-12 调度频率与线程边界未按契约执行

**需求条款**：第 4-7 节独立有界 worker、队列和各数据刷新频率。

**证据与影响**：normalization/strategy/deepseek worker 数及 candidate_refresh_seconds 仅被解析，实际主链为单 pipeline 线程串行评分和复核，TopK 报价没有独立频率。

**修复步骤**：1. 把全市场、候选、TopK、研究、评分和冻结拆成独立事件。2. 为数据、标准化、三策略、DeepSeek、合并和单写者建立有界执行器。3. 实现按阶段独立 cadence 和错过不补跑。4. 加入乱序版本门、背压合并和可停止生命周期。5. 用虚拟全日时间线验证每类计划次数。

**验收条件**：配置的 worker 和 refresh 参数均有真实消费者；频率与第 6 节逐格一致；队列满、停止和重启不丢冻结/风险事件。

**交付章节**：第 4-7 节，按整节分批。**状态**：`待修复 P1`。

### AUDIT-20260717-13 DeepSeek 批次终态和阶段预算编排不完整

**需求条款**：第 14-16 节批次与候选状态分离、阶段目标、共享缓存和 emergency。

**证据与影响**：批次状态使用了候选态 `late`；未形成完整 failed/abandoned 闭环，阶段级上限、emergency 触发和四 worker 编排没有生产证据。

**修复步骤**：1. 建立 batch 与 candidate 两套枚举和持久化状态机。2. 将迟到候选汇总为 partial/failed，而非 batch late。3. 实现阶段目标/上限和优先级调度。4. 仅在规则条件下启用 emergency。5. 覆盖 429、超时、非法 schema、部分返回、重启 abandoned 和预算竞争。

**验收条件**：所有 batch 进入规定五种终态；所有候选进入规定四种结果；188 全局和分桶上限并发不可突破；迟到结果只审计不融合。

**交付章节**：第 14-16 节。**状态**：`待修复 P1`。

### AUDIT-20260717-14 持久化审计和可观测指标不完整

**需求条款**：第 18、22 节 deepseek_calls、data_source_health、队列、策略和冻结指标。

**证据与影响**：data_source_health 表缺少生产写入，DeepSeek 只有预算预留而非完整调用审计；状态缺少 P50/P95、每策略耗时和全部终态累计。

**修复步骤**：1. 落实 deepseek_calls 和 data_source_health 单写路径。2. 对延迟、年龄、成功率和熔断做有界聚合。3. 增加每策略候选、过滤、评分、TopK、版本指标。4. 记录冻结报价年龄、来源、模式和哈希。5. 设置日志、审计和状态载荷容量上限。

**验收条件**：重启后审计可查询；状态包含第 22 节全部类别；指标写入不阻塞发布线程且不泄漏密钥或完整外部载荷。

**交付章节**：第 22 节。**状态**：`待修复 P1`。

### AUDIT-20260717-15 跨交易日旧冻结可能被当作当前快照

**需求条款**：第 7、18-21 节最近有效快照降级、当前/历史身份和前端缓存隔离。

**证据与影响**：新交易日冻结边界前可能直接返回上一交易日 latest；前端对 frozen 缓存不设年龄限制，可能把昨日记录当作今日当前数据且没有显式跨日降级。

**修复步骤**：1. 当前查询比较交易日和 snapshot trade_date。2. 上一交易日快照只能作为明确 stale fallback。3. envelope 返回 fallback 日期和原因。4. 前端显示“上一交易日快照”而不是当前推荐。5. 增加跨午夜、节假日和次日开盘测试。

**验收条件**：旧冻结不会冒充当日快照；数据源失败时仍可展示但明确 stale/date/reason；缓存键和响应身份同时校验。

**交付章节**：第 18-21 节，按整节分批。**状态**：`第 18 节后端查询身份已修复；第 19-21 节 API/UI/缓存表达仍待修复 P1`。

### AUDIT-20260717-16 测试和最终验收未覆盖关键生产契约

**需求条款**：第 23、25 节单元、并发、集成、UI、真实运行和发布门禁。

**证据与影响**：现有测试大量直接注入理想因子，缺少观察期动作、0 推荐、全频率、逐条冻结年龄、overlay、真实数据接入、真实 DeepSeek、健康持久化和截图证据。

**修复步骤**：1. 为上述每个审计项先补失败回归。2. 建立录制脱敏数据的完整日纵向链。3. 增加并发终止点和恢复测试。4. 用真实交易日记录 TopK P95 与 DeepSeek 调用。5. 保存三档桌面截图并校验布局。6. 仓库外 wheel 启动 server/CLI。

**验收条件**：第 23 节测试清单逐项有自动或受控人工证据；第 25 节每条可追踪到日志、快照、测试或截图；缺任一证据不得宣告发布完成。

**交付章节**：第 23、25 节。**状态**：`部分完成；第 17 节动作/TopK 和第 18 节逐条冻结年龄、恢复、overlay、跨日身份已有自动回归，其余仍为 P1/发布阻塞`。

## 修复批次顺序

1. 第 17 节：AUDIT-02、03，荐股动作、TopK 和阈值预注册报告已完成。
2. 第 18 节：AUDIT-01、05、06 及 AUDIT-15 后端冻结/查询部分已完成。
3. 第 8 节：AUDIT-08、10、11，完成候选字段、横截面和因子登记。
4. 第 9 节：AUDIT-09，完成全部硬过滤和可审计边界。
5. 第 10 节：AUDIT-04、11 中的风险、缺失和本地基础公式部分。
6. 第 11 节：AUDIT-04 中的 today 真实输入和评分闭环。
7. 第 12 节：AUDIT-04 中的 tomorrow 尾盘数据和评分闭环。
8. 第 13 节：AUDIT-04、11 中的 d25、long 和市场状态闭环。
9. 第 4 节：AUDIT-12 中的线程、队列、事件和单写者生命周期。
10. 第 5 节：AUDIT-12 中的完整交易时间线和单点任务。
11. 第 6 节：AUDIT-04、08、12 中的数据源和逐阶段刷新频率。
12. 第 7 节：AUDIT-05、12 中的实时性、限流、熔断和降级。
13. 第 14 节：AUDIT-07、13 中的 DeepSeek 调用资格和任务状态。
14. 第 15 节：AUDIT-13 中的五维 schema、证据、覆盖和风险映射。
15. 第 16 节：AUDIT-07、13 中的阶段预算、共享缓存、重试和 emergency。
16. 第 19 节：AUDIT-15 中的 API 当前/历史身份和错误契约。
17. 第 20 节：AUDIT-15 中的当前/历史信息架构和明细表达。
18. 第 21 节：AUDIT-15 中的缓存、局部刷新和桌面交互。
19. 第 22 节：AUDIT-14，完成持久化审计、状态指标和安全上限。
20. 第 23 节：AUDIT-16，补齐单元、契约、并发、集成和 UI 测试。
21. 第 25 节：AUDIT-16，收集真实交易日、DeepSeek、截图和安装验收证据。

以上每个编号都是一个独立交付批次；完成该整节的实现、回归、Review、CHANGELOG、单一提交和推送后必须停止，等待下一次用户指令，不得与相邻编号合并。

## 当前批次边界

本批次只完整交付第 18 节：逐条校验冻结报价年龄，固化配置/schema/锚点审计，恢复时隔离 staged/committed 损坏项并回退有效冻结，新增独立可恢复 live overlay、15:00 收盘固化、SSE/ETag 版本，以及跨交易日 stale fallback 后端身份。未开始第 8 节因子数据、第 19-21 节剩余 API/UI 契约、AKShare 真实响应、真实 DeepSeek 调用或 P2 数据接入。

## 敏感信息

本记录不保存 DeepSeek 密钥、完整外部请求/响应、运行数据库或快照；后续验证只记录脱敏错误类别、调用计数、配置状态和结论。
