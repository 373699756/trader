# 荐股策略文档

版本：v2（活动策略 `strategy_review30_top6_observe6_2026_07`）

引擎：`engine_review28_2026_07`

融合：`fusion_local68_deepseek32`

状态：活动策略契约；V2-only 最终发布边界

本文是候选、过滤、因子、评分、风险、DeepSeek、融合、动作、排名和策略验收的唯一
权威。产品、架构、时间线、发布、API、运维和工程路线以
[软件业务设计文档](software-business-design.md) 为准。策略用于研究，不构成投资建议，
不保证收益，不触发真实交易。

最终 V2 评分口径只产生 `ScoredDecision`，Long 只产生无评分 `LongProjection`；新 release
不得构造或读取旧 `RecommendationSnapshot`，也不回放旧策略、旧引擎或旧 schema。当前
交付已完成 V2-E0 至 V2-E10，唯一入口已收敛到 V2 命名空间；旧链已从活动树物理删除，不定义候选、公式、
风险、融合、动作或排名。历史问题与实施过程只进入 `CHANGELOG.md` 和报告。

## 1. 策略目标与共同纪律

- `today`：盘中短线，面向 T+1；11:20 冻结。
- `tomorrow`：上午生成滚动本地草稿并复用跨策略模型 facts，13:00 后增加尾盘量价和
  增量模型复核，面向 T+1；14:50 冻结。
- `d25`：上午生成滚动本地草稿并复用跨策略模型 facts，13:00 后增加增量模型复核，
  面向 T+2 至 T+5；14:50 冻结。
- `long`：固定长期潜力龙头研究池，只展示当前行情快照；warmup、上午和下午都可随统一
  行情生成当前观察快照，不运行荐股策略、不冻结、不产生交易动作、不写推荐历史。

允许 0 只推荐。所有计算只使用观察时点已经生效的数据；实时价格、涨跌停和冻结锚点
使用未复权价格，历史收益、均线、波动和回撤使用点时可得的前复权序列。行业、证券
状态、财务和规则按实际生效时间进入特征。每次输入绑定交易日、阶段、数据、规则、
行业、配置、因子 schema、策略和引擎版本。

缺失原始值保持 `null` 并记录 `missing_fields` 与 `missing_reasons`。允许用于确定性评分
的中性占位为 50，但不得把占位序列化成真实观测，也不得通过缺失字段获得额外优势。
非有限数值、未来数据、无时区数据、截止后结果和无法验证的证券状态不得用于可执行
推荐。

### 1.1 tomorrow v2 收益与选择目标

tomorrow 是目标架构唯一最高优先级的生产选择任务，在交易时段滚动发布本地预览并于
14:50 冻结。每个决策允许 0 到 6 只正式推荐和 0 到 6 只观察项；候选不足或全部未通过数据、风险、动作和集中度
门槛时必须返回真实空结果，禁止为凑数降阈值。

首要收益标签固定为 14:50 锚点至下一交易日收盘的相对全市场等权净超额收益，并同时
报告下一交易日最低价对应的 MAE/ATR20、换手和行业集中度。成本敏感性统一使用
20bp、50bp、100bp 三档往返成本，不得只展示最有利口径。

DeepSeek 继续参与融合分，但只能复核候选池内股票、返回经 schema 与证据校验的 facts，
不能放宽硬过滤、引入池外股票或用自由文本直接扣分。正式融合在新挑战者通过样本外和
前向影子门禁前继续使用当前固定 68/32 公式。普通阶段全策略合计目标 36、硬上限 66，
其中 tomorrow 独占目标 21、硬上限 38；失败、超时和重试同样计数，耗尽后保留 local
决策，不阻塞发布或冻结。独立 emergency 桶和全局 168 次灾难保护线以第 11 节为准。

## 2. 板块身份与交易规则

板块固定为 `main`、`chinext`、`star` 和 `unsupported`。识别顺序为带生效时间的证券
主数据、交易所清单、行情源市场字段、六位代码前缀降级。前缀降级记录
`board_source=code_prefix_fallback` 和 `board_reliability=degraded`；来源冲突记录
`board_classification_conflict`，只能观察。

每个观察点保存：板块、来源、可靠度、交易所、上市日期、上市交易日龄、是否有价格
限制、交易所限制比例、策略热度上限、规则版本和生效日期。

首次上市第 0-5 个交易日、重新上市首日、退市整理首日直接排除，第 6 个交易日起才用
普通规则。上市日期或交易日龄不可验证时只能观察。普通热度边界：主板 8.00% 通过、
8.01% 剔除；创业板和科创板 16.00% 通过、16.01% 剔除。无价格限制状态不计算普通
`limit_proximity`。

## 3. 统一行情与点时合并

策略只消费不可变统一行情，不直接读取供应商对象。相同有效输入无论来源完成顺序都
产生相同字段归属、来源版本、冲突集合、缺失集合、合并 epoch 和规范 JSON 哈希。
合并 epoch 绑定排序后的已接受观测身份、点时、版本、载荷哈希、缺失原因和定向范围；
最终规范 JSON 哈希继续覆盖完整统一快照，两者不得用不稳定对象地址或输入完成顺序生成。

同来源先按来源时间、接收时间选择较新字段；跨来源同时点按既定优先级。新鲜腾讯
候选价只能在与东方财富或新浪至少一个全市场观测偏差不超过 0.50% 时完成冲突复核，
不得自证。慢数据不得覆盖更新的实时价格。全源失败时沿用最近有效统一快照并降级，
不能生成空快照清除推荐。
未准入影子来源不得写入统一行情字段，也不能参与候选、过滤、评分、DeepSeek 证据、
冻结或收益结算；通达信/mootdx 在权威准入前只可作为独立影子观测，不构成候选 fallback。

## 4. 硬过滤

候选依次执行带严重级别的确定性过滤。当前 `FilterSeverity.REQUIRED` 必需阻断包括：

- 非六位受支持 A 股代码或 `unsupported` 板块，ST/`*ST`/名称含“退”、停牌；
- 价格或成交额非有限、非正，行情时间无时区、来自未来或超过本阶段动作年龄；
- 跨源偏差字段本身非法，开高低收结构非法或当日涨跌幅缺失/非有限；
- 上市第 0-5 个交易日、重新上市首日、退市整理首日；
- 20 日成交额中位数缺失、非有限或低于 5000 万元；
- 一字涨跌停、配置黑名单或行情已标记重大监管风险；
- 超过板块热度上限；
- 经点时 schema 和官方证据验证的 `major_shareholder_reduction`、
  `financial_fraud_history`、`official_investigation_history`、
  `major_illegal_history`、`fund_occupation_history`、`illegal_guarantee_history` 或
  `forced_delisting_risk` 命中；
- `unlock_risk > 0`、`pledge_risk > 0` 或 `financial_deterioration > 0.5`。

当前可选告警/观察限制包括：未复核的跨源价格偏差超过 0.50%、
`board_classification_conflict`、`board_identity_degraded`、`missing_listing_date`、
`missing_listing_age_sessions`、`structured_risk_unavailable` 和
`corporate_risk_history_unavailable`。这些事实写入 `optional_flags`；统一行情同时写入
`execution_restrictions` 时，最终动作即使达到阈值也会改为 `observe`。候选必需字段缺失
比例超过 30% 属于板内预选剔除，不冒充硬过滤；
板块数据可靠度低于 0.85 属于动作门和 DeepSeek 资格限制，同样保留本地候选但只能观察。

硬过滤是结构化规则，不能由 DeepSeek 放宽、删除或新增池外股票。可选告警进入
`optional_flags`，不冒充阻断原因；必需阻断保存在 `filter_reasons`。原因码必须细分到
具体板块和事实，例如 `chinext_board_too_hot`、`star_board_too_hot`，不能用模糊成长
板标签替代。

严重公司风险只接受发行人、交易所、监管或司法结构化披露及其稳定证据 ID；新闻标题、
DeepSeek 自由文本和普通风险提示、问询函、一般诉讼、亏损预告不能创建或解除硬事实。
规则固定为：

- 大股东指控股股东、实际控制人和持股不低于 5% 的股东；减持计划从公告起阻断，至官方
  完成或终止后 90 天解除，无终态则持续阻断；
- 官方决定确认的财务造假、虚假记载、重大遗漏、重大违法、资金占用和违规担保永久阻断；
- 正式立案调查和强制退市程序在存续期及官方结案、撤回或终止后三个自然年阻断，满三个
  自然年当日解除。

风险事实随研究缓存持久化合并，旧命中不能因本轮来源失败消失。来源失败或覆盖状态不可
确认时不能猜成 0：已确认事实继续阻断，其余候选保留本地快照但动作降为观察，并记录
`corporate_risk_history_unavailable`。减持、解禁、质押必须保持独立字段。
财务、公告/公司风险历史、质押和解禁是四个独立覆盖分项；程序运行时按分项 TTL 复用缓存，
并非每次评分都重新请求，也不是进程一生只请求一次。批次超时只延后未完成股票，已经完成
股票和分项立即形成新研究版本。上述采集与覆盖状态独立于 DeepSeek 模型复核。一般负面新闻、
普通问询、一般诉讼和未经正式财报确认的亏损预告仍只作研究提示，不能新增硬过滤；正式财务
报告触发 `financial_deterioration > 0.5` 或官方结构化风险事实命中时才按本节既有阈值阻断。
CNInfo 公告增量链只保存带稳定公告 ID 的结构化披露、公告组件状态和来源游标；重复页按公告
ID 去重，空增量只更新游标和审计，不能把既有 `known_risk` 覆盖为 `known_clear`。交易所公告
交叉校验未形成正式来源前，CNInfo 证据的 `exchange_cross_check_status` 固定为 `pending`，
可进入本地风险事实注册表和观察降级判断，但不得宣称已完成交易所级复核。

## 5. 板内总体与成交因子

横截面总体固定为：

```text
trade_date + phase + board + data_version
```

流动性、换手、波动、回撤、趋势和相对强度均在板内按
2.5%/97.5% 截尾、归一化和稳定排序。有效板内横截面至少 100 只；不足时最多回退最近
5 个有效交易日的同板同阶段总体，不能借用其他板块。

板内总体与本轮候选是两个不同集合：全市场特征只用于按
`trade_date + phase + board + population data_version/merge_epoch` 构建和缓存总体、
分位与归一化基准；候选定向报价完成后，只把每板最多 120 只的新鲜候选投影到该基准并
计算候选分和本地分。不得再次对约 5500 只全市场股票逐只执行九组候选选择和本地评分，
也不得先从全市场选出另一批股票再与本轮候选代码求交集。候选报价版本变化只失效相应
候选批次和逐股本地分，不得无条件失效未变化的板内总体。候选预选与正式评分必须沿用
同一份全市场特征自带的 `merge_epoch`；缓存只保留总体参数和参考分布，命中后不得为
每个策略重复序列化全市场，正式评分只投影有界候选并直接执行纯本地评分函数。

行业只用于展示、审计、DeepSeek 行业政策零权重标签和最终集中度标识，不进入候选分、
本地分、DeepSeek 总分、动作门或活动下行保护。特征服务仍计算并保存
`industry_strength`、`industry_breadth`、`industry_trend` 和
`industry_policy_score`，通用 DeepSeek ground-truth 也可携带这些已存在字段；活动板内
评分不读取它们，`industry_policy` 维度权重固定为 0，活动下行保护调用固定
`require_industry_breadth=false`。`peer_gap`、`leader_gap` 不属于活动评分输入。修改同一
股票的行业数值不能改变其候选分、本地分、DeepSeek 总分、动作或下行保护；行业名称本身
只可能改变最终每行业最多 2 只的集中度选择。

换手和成交冲击：

```text
turnover_shock_20 = current_turnover / median(turnover, 20)
amount_shock_20 = current_amount / median(amount, 20)
volume_price_confirmation = sign(return) * normalized_amount_shock
```

分母缺失、非正或非有限时保持缺失。换手使用带通函数
`B(x;0.8,1.1,2.0,4.0)`，成交额使用 `B(x;0.8,1.2,3.0,6.0)`。所有成交量、成交额和
换手指标统称 `flow_proxy`，不得解释为主力、机构或散户资金。

## 6. 候选预选

每个 `strategy + board` 在硬过滤后独立计算候选分。九组活动权重为：

| 板块 | today | tomorrow | d25 |
| --- | --- | --- | --- |
| 主板 | 流动35.2941、日内29.4118、换手23.5294、完整11.7647 | 流动41.1765、趋势29.4118、稳定17.6471、完整11.7647 | 流动37.5、趋势25、稳定18.75、执行12.5、完整6.25 |
| 创业板 | 流动35.2941、日内29.4118、换手23.5294、完整11.7647 | 流动28.5714、趋势35.7143、稳定21.4286、完整14.2857 | 流动28.5714、趋势28.5714、稳定14.2857、执行21.4286、完整7.1429 |
| 科创板 | 流动35.2941、日内29.4118、换手23.5294、完整11.7647 | 流动29.4118、趋势35.2941、稳定23.5294、完整11.7647 | 流动29.4118、趋势35.2941、稳定17.6471、执行11.7647、完整5.8824 |

权重以小数写入唯一策略配置，每组精确合计 1.0。today 虽同权重，三板仍分别计算总体、
可靠度和排名。候选要求 `candidate_score >= 50`、核心缺失不超过 30%，随后按候选分
降序、代码升序稳定排列，每板最多 120 只且不补满。

活动候选子组件固定为：today 日内结构按 5 分钟涨幅 35%、涨速分位 25%、当日涨幅 20%、
量比 20%，换手状态取换手冲击与成交额冲击已知值的平均；tomorrow 趋势直接使用
`trend_score`，稳定为低波动/低回撤已知值平均；d25 趋势同样使用 `trend_score`，执行为
容量、适中振幅和价格可执行性已知值平均。完整度按各策略登记候选字段的缺失比例计算。

`board_data_reliability >= 0.85` 才能进入可执行链和 DeepSeek 队列。低可靠候选只能排在
可靠候选之后作为观察，不消耗 DeepSeek 预算。DeepSeek 不得新增池外股票。

冷启动历史预热只改变数据就绪过程，不改变候选公式或阈值。实时全市场行情先形成当前
epoch；固定 360 个预热槽只按主板、创业板、科创板均衡稳定轮询，每板最多 120 个，
`unsupported` 证券不占槽且不进入远端历史重试，从而允许每个活动板块达到至少 100 个
可用横截面样本。预热代码在独立历史池连续分批取得历史日线。120 积分
Tushare SDK `daily` 批量数据必须标记 `raw`/`unadjusted_daily`，不得进入候选和评分所需
的收益、均线、波动、回撤或 ATR；成交量从手换算为股、成交额从千元换算为元，仅保留
来源能力审计。只有积分档达到 2000 且明确支持 `pro_bar(qfq)` 时才允许 Tushare 日线进入
历史特征缓存。默认以腾讯完整日 K 前复权响应作为历史主来源（成交量手转股、成交额万元
转元），东方财富前复权作为第二回退。历史不足 20 个有效交易日的
代码记录为 `history_warming`，不得作为有效候选，也不得清除上一份有效推荐；已成功的
单只历史立即进入下一次筛选，同批其他代码超时不能回滚。达到策略数据条件后必须继续
完成硬过滤、九组预选、三板评分、全局 TopK 和统一决策提交，不能停在行情或历史内存池。
生产 `versioned_dag` 只在候选行情、市场新闻或公司风险输入完成后触发评分；同一输入链
尚未开始的旧评分由新 `data_version` 替换，不再与周期评分重复排队。已经开始的评分可
完成，但冻结 CAS 和基础快照身份仍拒绝迟到结果。三板单 worker 通道的 latest-wins
范围固定为 `strategy + board`：不同策略不得互相替换；多个策略在同一板等待时按
tomorrow、d25、today 顺序轮转，保证明日荐股不会被同轮其他策略挤出，持续新 epoch
也不得饿死 d25 或 today。
09:30-11:20 的输入触发评分同时生成 today、tomorrow 和 d25；09:30-09:36 只复用
预热 canary，09:36 后三策略通过原始 facts 身份单飞共享模型结果，并分别执行本地策略
投影。tomorrow 上午允许尾盘分钟字段按缺失规则取中性值并记录
`tomorrow_tail_data_incomplete`。13:00 后执行有界尾盘分钟刷新，只对证据、结构化风险或
受控价格反映身份变化执行增量复核，使午后审核比上午更完整；
本地失败降级仍不得清空最近有效草稿。上午和午后均使用各策略既有分数门槛、硬过滤、
0.85 可靠度和风险动作门，不为增加数量放宽标准。
11:20-13:00 通常只保留上午草稿并更新报价；若服务午间冷启动且同日 tomorrow、d25 或
long 当前快照缺失，允许各执行一次纯本地补算，生成后不在午间重复评分，不新增
DeepSeek 请求或尾盘分钟增强。
当远端历史暂不可用时保持 `history_warming` 或最近有效进程内值，不读取本机 SQLite
历史种子。每只代码临时取得最多 61 根 qfq 日线，计算现有 MA60、60 日收益、MA20 斜率
和 20 日风险摘要后，仅在内存保留最近 20 根原始日线与紧凑摘要；最多 360 只、7200 根
原始记录，重启后重新预热。该收敛只改变数据生命周期，不改变因子公式、阈值、缺失策略
或策略版本。全市场发现以本轮接收时间判断链路新鲜度，候选复核和评分
继续保留并检查原始来源时间，旧行情只能观察，不能因此获得可执行资格。
15:00 后任意启动时，只允许为缺失正式快照的 tomorrow/d25 执行冷启动收盘补算，并必须沿用相同数据
就绪门槛：主板、创业板、科创板分别至少 100 只
具备有效 `amount_median_20d` 和不少于 20 个历史交易日，才允许执行本地三板评分并固化
`close_fallback`。任一板仍在 `history_warming` 时只能退避重试，不得把板块样本不足
或由历史缺失造成的空结果冻结为当日正式推荐；板块人口已经满足但策略字段可靠度低于
0.85 时允许完成降级计算，但观察动作只保留在本轮内存，最终固化正式推荐为空，不得提升
为可执行或进入 DeepSeek。
若一轮全市场硬过滤因 `history_warming`、缺失/非法流动性历史或行情接收时间陈旧而全部
为空，候选状态必须保留最近有效身份并记录降级，等待候选定向报价与历史预热恢复；只有
存在完成数据条件的股票且仍无候选时，才允许用真实空结果替换候选池。保留身份不得绕过
后续原始 `source_time` 新鲜度、硬过滤、评分或动作门。

## 7. 本地评分

定义带通函数 `B(x;l,o1,o2,u)`，其中 `l < o1 <= o2 < u`：`x <= l` 或 `x >= u`
得 0，`o1 <= x <= o2` 得 100，两侧区间线性变化。参数非法时配置拒绝启动，缺失或
非有限输入按缺失规则处理。`P(x)` 是 0-100 横截面平均秩，反向因子必须登记并计算
`100-P(x)`。

统一计算边界：

```text
base_score = sum(component_score * component_weight)
local_score = clamp(base_score - local_risk_penalty, 0, 100)
```

组件和子组件权重分别精确合计 1.0，三板单独计算，输出统一为 0-100。活动权重：

| 板块 | today | tomorrow | d25 |
| --- | --- | --- | --- |
| 主板 | 日内37.5、换手25、流动执行25、稳定12.5 | 尾盘16.6667、换手量价5.5556、趋势22.2222、稳定27.7778、市场11.1111、入场16.6667 | 趋势29.4118、质量价值29.4118、稳定17.6471、量价流动11.7647、入场11.7647 |
| 创业板 | 日内37.5、换手25、流动执行25、稳定12.5 | 尾盘25、换手量价18.75、趋势18.75、稳定12.5、市场6.25、入场18.75 | 趋势28.5714、质量价值14.2857、稳定14.2857、量价流动28.5714、入场14.2857 |
| 科创板 | 日内37.5、换手25、流动执行25、稳定12.5 | 尾盘16.6667、换手量价5.5556、趋势27.7778、稳定27.7778、市场5.5556、入场16.6667 | 趋势35.2941、质量价值29.4118、稳定17.6471、量价流动11.7647、入场5.8824 |

today 子组件：日内结构使用 5 分钟涨幅 30%、涨速分位 20%、当日涨幅 20%、量比 15%、
`relative_strength_3d` 15%；换手状态取换手冲击、成交额冲击和量价确认已知值的平均；
流动执行使用 20 日成交额分位 60%、适中换手 20% 和距价格限制安全度 20%；稳定为低波动
与低最大回撤已知值的平均。

tomorrow 子组件：尾盘使用 30 分钟收益 35%、尾盘量比 30% 和收盘位置 35%；换手量价为
换手 35%、成交额 35%、量价确认 30%；趋势为均线结构 37.5%、斜率 37.5% 和突破 25%；稳定为
低波动/低回撤各 50%；市场状态固定映射 risk-on 60、neutral 50、risk-off 40。

d25 子组件：趋势为均线结构 7/17、斜率 6/17 和突破 4/17；质量价值为质量 50%、价值 30%、
成长 20%；稳定为低波动/低回撤各 50%；量价流动为 20 日成交额分位 50%、换手 25%、
成交额 25%；不过热不再是活动正向组件。

d25 禁止再用 `base_score * overheat_factor * market_regime_factor` 双乘。市场状态只进入
结构化风险上下文和审计，不直接缩放活动 d25 总分。代码中保留的旧通用 d25 评分函数及其
`not_overheated`/双乘逻辑只服务旧策略身份回放；活动三板路径始终传入
`BoardStrategyPolicy` 并调用上述板内评分。

### 7.1 确定性入场质量

tomorrow 和 d25 的 `entry_quality` 取缩量回踩与放量突破两类确定性形态的
较高值。热度本身不阻止入选；只有形态、下行保护和既有硬过滤共同决定是否可执行。

- 缩量回踩：`MA5 >= MA10 >= MA20`、MA20 斜率向上，价格距离 MA5 不超过 1% 或距离
  MA10 不超过 2%，点时 `volume_to_5d_average` 不高于 0.70，且价格不低于 MA20。
- 放量突破：价格不低于前 20 个完整交易日最高价，点时 `volume_to_5d_average` 不低于 2.0，
  收盘位置不低于 70，突破偏离不超过 5%。
- `volume_to_5d_average` 优先使用供应商点时 `volume_ratio`；供应商未提供时，使用同日
  点时成交额除以最近 5 个已完成交易日平均成交额派生，不使用同日历史 bar。
- 完整形态为 100，趋势前提成立但入场确认不完整为 50，趋势破坏为 0；必要输入缺失
  保持 `null`，不能以中性值冒充确认。

行业组件移除后，其权重按各策略剩余组件原比例归一化；`entry_quality` 保持上述确定性
形态定义。过热事实继续审计和软扣分，不再作为 d25 独立正向组件。

### 7.2 tomorrow v2 确定性本地选择

tomorrow v2 旁路选择只消费同一数据平面快照：昨日及更早历史、证券主数据和结构化风险来自
`DailyFeaturePack`；全市场当前报价来自其父版本匹配的 `MarketEpoch`；当日尾盘、入场、
执行质量和日内结构风险只能由 `CandidateQuoteEpoch.CandidateFeatureRow` 覆盖。实时特征
白名单不得包含财务、公司风险或证券身份字段。候选价格必须携带本轮跨源偏差并满足
`cross_source_verified=true`、偏差有限且 `<= 0.50%`；不得沿用旧全市场价格的复核状态。
候选单股来源时间早于父市场同股报价时，候选价格及同批实时特征都不得覆盖更新值。

该快照只通过应用层 `DataPlaneReadPort` 读取。`DailyFeaturePack` 发布前必须证明潜在可执行
代码的证券主数据覆盖为 100%，以及本轮候选至少 99% 具备不少于 20 个有效历史交易日和
核心历史摘要；不满足时保持最近有效决策输入并报告未就绪/降级，禁止把覆盖不足或无效空
输入解释成策略真实空集。

逐股过滤状态固定为：

- `reject`：任一必需硬过滤失败，不进入板内候选分；
- `observe_only`：硬过滤通过但存在可选告警，或板块数据可靠度低于 0.85；保留本地分和
  审计，不进入 DeepSeek 或正式 TopK；
- `pass`：必需过滤通过、无可选告警且板块可靠度达标，可继续参加正式本地选择。

每板先按可靠度是否达标、候选分降序、代码升序稳定截取最多 120 只，再计算本地六组件和
本地风险。本地风险只从 `base_score` 扣一次，输出 `local_score`；DeepSeek 后续融合不得
再次扣 `local_risk_penalty`。全局按本地分降序、候选分降序、代码升序稳定排序，只从
`pass` 且达到 `tomorrow 阈值 - observation_margin` 的候选中选最多 6 只，每行业最多
2 只。允许真实空结果；`observe_only` 候选在独立观察集合中展示和审计，不占正式 TopK。

候选核心缺失超过 30%、候选分低于 50、每板 120 上限、本地风险 veto、本地门槛、行业上限
和 TopK 上限分别记录稳定原因码，不得合并成“本轮没有可评分候选”。相同 epoch、配置、
fallback 和评估时间无论输入顺序如何都必须产生相同逐股状态、分数和选择顺序。

### 7.2.1 d25 v2 原生决策

D25 活动生产只消费同一批 `D25NativeInput`，沿用第 6、7 节的 d25 板内候选权重、专属
五组件评分、风险、动作、集中度和稳定排序，不得借用 tomorrow 分数或旧通用 d25 双乘函数。
local 与 hybrid 都写入统一 `ScoredDecision`，但身份、CAS 父版本、事件、正式记录和查询必须
按 `strategy=d25` 与 tomorrow 隔离。结构化 DeepSeek facts 只允许升级当前 D25 local，失败、
迟到、代码或证据哈希错配时保留 local，不新增模型请求链。

D25 在 14:50 冻结当前同日正式结果；允许合法空结果，不为凑数放宽候选、可靠度、评分或
动作门槛。15:00 后同日正式记录缺失时，只能在没有待重试封口的前提下优先固化本次运行
D25 current，或在冷启动时按完整同日收盘点时输入执行一次纯本地补算并创建
`close_fallback`。正式记录一经创建不可覆盖；tomorrow 不得作为 D25 输入或降级源，其
current、冻结和恢复身份均与 D25 隔离。

### 7.3 long 固定研究池

long 不是荐股策略，不计算价值、成长、质量、风险保护或任何旧长期评分，不调用
DeepSeek，不执行 TopK/行业集中度选择，也不把研究字段缺失标记为策略降级。long 只从
版本控制的 `long_watchlist.json` 读取固定股票池和分组，按配置顺序生成 `observe` 观察项，
不生成评分字段并标记 `score_status=not_applicable`。long 不借用评分字段伪造荐股决策形状，
也不代表投资判断。运行时不得把 long 送入任何策略评分或 finalize 链。

long 固定池的准入语义是“长期潜力赛道中的头部或弹性龙头观察标的”：卡脖子行业优先
关键环节国产替代龙头，高成长赛道优先产业趋势清晰且全市场关注度靠前的龙头，低价潜力
股优先芯片、智能制造、算力卫星、材料资源和种业方向中价格弹性更强的细分观察标的。该准入语义只约束固定名单维护，
不构成收益承诺，也不允许运行时按旧 long 评分、DeepSeek 或实时价格自动换股。

长期分组分三类：

- `chokepoint`：卡脖子行业分组，来源为 `main` 历史中的
  `CHOKEPOINT_INDUSTRY_LEADERS` 并按关键核心技术、关键零部件、关键基础材料、关键软件和
  产业链供应链安全口径校正；脑机接口按公开年报和公告证据维护独立分组，AI 基础设施拆为
  `AI算力`、`液冷` 和 `数据中心电源` 三个独立分组；固态电池按长期细分行业量化复核报告
  的正式通过结果维护独立分组，不把未达标候选继续留在高成长混合分组。2026-07 战略方向
  合并进一步把混合的科学仪器/医疗装备拆为 `高端科学仪器` 与
  `生命科学/高端医疗装备`，把精密零部件校正为 `高端传感器/精密测量`，并新增
  `航空发动机/燃气轮机`、`新型电力系统/储能` 和 `可控核聚变关键材料/装备`；
  迁移已有股票时保持全局唯一，新补充股票必须有交易所公告、定期报告或公司正式披露支持
  其关键环节关系，不能用市场概念标签代替。卡脖子每个行业只保留
  一个左侧分组按钮；组内股票按 `document_scan` 与 `current_leaders` 两个来源 section 排序：
  先保存 `docs/reports/a-share-long-industry-research-2026-07-24.md` 的正式通过名单，再保留
  当前固定龙头补充；每个行业合计最多 5 只固定观察股。
- `future_growth`：高成长赛道分组，按 2026-07-24 可见官方产业政策口径和全市场长期涨幅
  复核形成固定观察池，覆盖低空经济、商业航天、光模块、具身智能、智能电动汽车、
  创新药/高端医疗、生物制造和量子通信/6G 等方向；每个赛道最多 5 只。
- `low_price_potential`：低价潜力股从芯片、智能制造、算力卫星、材料资源和种业等相关
  战略行业中维护价格弹性较强的固定观察标的，并按 `芯片与电子`、`智能制造与软件`、`算力与卫星`、`材料与资源`、
  `种业与生物育种` 五个固定子分组展示；每个子分组最多 8 只、合计最多 26 只且不得重复。
  该分类不是运行时低价筛选器，不因实时价格变化自动调入或调出。

整个 long 固定池执行全局唯一归属：每只股票必须且只能出现在一个长期分组中，不能跨
卡脖子、高成长和低价潜力三类重复展示，也不能在卡脖子同一行业的不同来源 section 之间重复展示。
删除重复席位时必须补充新的、行业语义匹配且代码和简称已通过行情源校验的固定观察标的；
配置加载和打包静态名单契约必须拒绝跨组重复、非法来源分区或未分组股票。

long 完整当前快照从固定池定向行情专线携带价格、涨跌幅、成交额、换手率、总市值、来源
和时间；部分代码暂时缺报价时必须按配置保留完整名单，优先沿用该代码同交易日最近有效
观察，否则保留显式缺失占位，不得因部分失败自动换股或缩短列表。该专线为单 worker、
单 latest-wins 待处理请求，交易活动阶段每 1 秒、午间每 10 秒刷新，15:00 最后刷新一次
后停止；它不与候选报价或三策略 TopK 共用请求，也不触发新闻、风险、参考数据、历史准备、
评分、DeepSeek、冻结、推荐历史或结算。long 不受 today/tomorrow/d25 的评分时段选择限制。
股票增删、行业归属和低价潜力名单只能通过配置变更和同批测试/文档更新完成。

#### 7.3.1 long v2 原生当前投影

long v2 只发布 `LongProjection` current，不生成任何荐股评分或动作 envelope。
投影按 `long_watchlist.json` 项目顺序保留完整固定名单，每个代码必须且只能映射到一个配置分组；
同轮可用报价标记 `live`，同交易日最近有效报价标记 `retained`，无有效报价标记 `missing`。
部分失败、整体失败、未来报价、未知代码或非正价格均不得自动换股，也不得删除席位、缩短
列表或跨组移动；“未来报价”以注入的本轮处理完成时刻为界，请求后至完成前正常收到的行情
仍属于本轮有效输入；下一交易日不得沿用前一日价格。

long current 的 `score_status` 固定为 `not_applicable`，item 只包含固定身份与当前行情事实；不得
出现候选分、本地分、DeepSeek 分、融合分、风险扣分、动作阈值、TopK 或结算结果。Long runtime
不持有 reviewer、冻结、正式记录、历史或结算端口，不发布评分 committed event；DeepSeek
高价值集合固定为空，物理 HTTP 请求始终为 0。

## 8. 执行质量、可靠度与容量

板内容量层级固定为：低于 P50 为 `liquidity_observe`，P50 至 P80 为
`normal_capacity`，P80 及以上为 `high_capacity`。板内样本不足时保持保守或缺失，
不能跨板借用阈值。

可靠度由来源新鲜度、字段完整度、板块身份、横截面规模和冲突状态构成，
范围 0-1。可靠度低于 0.85、行情超动作年龄或价格冲突未解决时，不产生新的可执行动作，
但可展示最近有效推荐并标记观察/降级。

执行质量只描述在公开行情下的相对容量和价格限制风险，不预测真实成交，不生成仓位、
资金规模或下单建议。

## 9. 本地风险与结构化事实

本地风险先从 `base_score` 扣除，结果已经包含在 `local_score` 中。风险事实按规范键去重，
同一事实不因多来源或模型重复描述而重复扣分；本地风险总扣分有界，活动测试要求覆盖
七类事实、去重和 25 分截断。

活动短线风险及固定扣分为：

| 风险代码 | 含义 | 扣分 |
| --- | --- | ---: |
| `near_limit_crowding` | 接近板块价格限制的追高拥挤 | 5 |
| `price_volume_divergence` | 价格与成交量/成交额确认方向背离 | 4 |
| `high_volatility` | 20 日波动率进入登记的极端区间 | 3 |
| `short_term_overheat` | 5/10 日收益或 MA20 偏离极端 | 3 |
| `intraday_reversal` | 冲高回落且收盘位置低；today 至少 30 个有效分钟 | 4 |
| `liquidity_contraction` | 通过绝对成交额门后相对流动性骤降 | 3 |
| `trend_breakdown` | 价格、MA5/MA20 和短期收益共同破位 | 3 |

### 9.1 活动下行保护

融合公式保持不变，下行保护在融合后、动作和 TopK 前独立执行。保护结果为 `pass` 或
`observe`，高分和高热度不能覆盖 `observe`。下列任一条件将可执行候选降为观察：

- 从当日最高价回落至少 1 个 ATR20，且收盘位置不高于 35；
- `trend_breakdown` 命中；
- 板内 `low_volatility_score <= 20` 且 `low_drawdown_score <= 20`；
- `market_regime=risk_off`、市场上涨宽度不高于 30，且 tomorrow/d25 尾盘收益为负或
  收盘位置不高于 50；today 使用 5 分钟收益为负或收盘位置不高于 50；
- ATR20、20 日波动、20 日最大回撤、低波动分或低回撤分缺失、非有限或过期。

`short_term_overheat`、高波动或临近涨停单独出现时仍只按既有风险表软扣分，不构成
下行保护降级。结构化监管、财务和交易规则 veto 保持 `unavailable`，不降格为观察。

七项理论合计和截断上限均为 25；无事实时扣 0。普通连续弱势只由基础组件反映，不能
重复触发离散扣分。缺失、非有限或样本不足不触发扣分，只记录缺失。long 不使用这组
短线结构扣分。风险事实保存实际值、阈值、方向、严重度、证据、生效时间、互斥/叠加
组和本地规则版本。

模型自由文本、评级、置信度、情绪形容词或未经证据验证的结论不能直接决定扣分、veto、
动作或排名。

## 10. DeepSeek 结构化复核

DeepSeek 是可选复核器，不是候选生成器。默认使用受控双模型：V4-Flash 非思考主审，
V4-Pro 思考挑战者仅处理预注册的不确定、高风险或边界案例。供应商不可用、密钥缺失、
超时、预算耗尽或 schema 失败时回退本地推荐，并明确 `local_degraded` 或相应终态。
若硬过滤、板块样本或可靠度门槛使本轮没有可复核候选，必须标记
`deepseek_skipped_no_eligible_candidates`；未发生物理请求时不得标记
`deepseek_incomplete`。后者只表示已有可复核候选但请求、预算、响应或校验未完整完成；
long 永久不进入复核，因此不得附加任一 DeepSeek 降级原因。

活动结构化事实接缝固定为 `deepseek_v4_review_facts_v1`。该接缝只允许模型返回可校验
事实，不允许自由文本、评级或置信度直接决定分数、风险、动作或排名。事实
集合固定包含：催化方向、重要度、确认状态、周期和引用；价格反映桶；基本面状态；行业
政策状态；监管、减持、解禁、质押、诉讼和业绩风险；事实冲突；证据覆盖；以及
`abstain`。每条事实必须引用本地 manifest 中的证据 ID、来源等级、发布时间和摘要哈希；
无法用 schema 与证据核对的字段必须拒绝或转为 abstain。

每只候选最多使用 12 条点时证据，首轮槽位仍按结构化行情 1、尾盘分钟 1、财务与研究
3、监管/质押/解禁等风险 4、新闻与公告 7 的顺序依次填充，达到 12 条即停止。空槽按
监管风险、财务研究、分钟量价、新闻公告的顺序补给；补位后监管最多 6、财务最多 5、
新闻公告最多 8，行情和分钟仍各最多 1，总数仍不得超过 12。跨来源同主体、事件 key
和证据分类的记录只保留质量更高且更早接收的一条；存在有效相反材料时至少保留一条
反证。证据 manifest 必须携带 `source_tier` 和 `event_key`，正向加分只接受官方来源或
两个独立可信来源支持。

V4 事实只由本地纯函数映射，原始维度从 50 分开始并限制在 30-70；模型不能直接返回
维度分、生产扣分或 veto：

| 结构化事实 | 本地原始变化 | 投影维度 |
| --- | --- | --- |
| 催化方向与重要度 | 高/中/低按方向记 `±15/±8/±3` | `value_quality` |
| 价格反映状态 | 未反映/部分反映/充分反映按方向记 `±12/±5/0` | `market_flow` |
| 基本面 | 改善 `+15`、稳定 `+3`、恶化 `-18` | `financial_health` |
| 行业政策 | 仅形成审计标签，不进入分数、覆盖率或已知维度数 | `industry_policy` |
| 风险事实 | 维度保持 50，只经本地风险规则形成 penalty/veto | `risk_quality` 与独立风险通道 |

证据质量同时作为维度 confidence，使变化值在融合前向 50 收缩：官方来源 `1.00`、两个
独立可信来源 `0.85`、单一可信来源 `0.65`、软信息 `0.40`，事实冲突时最高 `0.25`。
正向催化必须已确认且质量至少 `0.85`；正向基本面和政策同样要求质量至少 `0.85`，
否则不得加分。负向事实不因来源质量不足被反向提升，但仍必须引用合法点时证据。

五维固定为价值质量、财务健康、资金量价、行业政策和风险质量。每维输出 0-100 的
`score`、0-1 的 `raw_confidence`、flags、evidence_ids 和 assessment。有效分与总分为：

```text
effective_i = 50 + (score_i - 50) * raw_confidence_i
deepseek_score = sum(effective_i * w_i)
confidence_coverage = sum(raw_confidence_i * w_i)
```

未知维度固定为 `score=50, raw_confidence=0, effective=50`，其余权重不重归一化：

| 策略 | 价值质量 | 财务健康 | 资金量价 | 行业政策 | 风险质量 |
| --- | ---: | ---: | ---: | ---: | ---: |
| today | 11.7647% | 11.7647% | 47.0588% | 0% | 29.4118% |
| tomorrow | 18.75% | 25% | 31.25% | 0% | 25% |
| d25 | 25% | 31.25% | 25% | 0% | 18.75% |

仅当 schema 和证据有效、结果未迟到、非 abstain、至少两个维度已知且
`confidence_coverage >= 0.50` 时应用模型分。高风险证据有效且置信度不低于 0.7，且
本地风险表登记了证据类型、有效期和 veto 资格时，才允许本地映射 veto。
任何提高或降低该门禁都必须提升策略、prompt/schema 和缓存版本并重新执行第 15 节验证。

输出必须通过严格 schema，包含代码、维度分、结构化风险事实、证据引用、主审/挑战者
状态、请求/实际模型、thinking 模式、原始与校准置信度、证据 manifest 哈希、校准版本
和受控评级。缺失、越界、代码错配、证据错配或未知字段按规则拒绝或修复；修复重试也
计入物理请求。

高价值复核输入版本固定为 `p4_p5_high_value_review_manifest_v1`，必须携带本地候选身份、
证据 manifest、价格反映桶、归属策略、预算桶和 deadline；long 的高价值集合固定为空。
新 V4 facts 先由本地确定性映射形成受控模型分和 `deepseek_risk_penalty`，再进入固定融合
公式；任何实现不得在公共接缝之外建立第二套 schema、owner strategy 或风险处罚来源。

审计字段 `review_stage`、`challenger_status`、`rating` 和置信度只用于持久化和离线
观察，不参与生产分数、动作或排序；普通 Web 推荐响应只投影复核 `outcome/error`。
挑战者不能放宽硬过滤；冲突时只可维持或更保守。模型提出的风险必须经 schema、证据
引用和本地规则映射后才形成 `deepseek_risk_penalty`。

### 10.1 tomorrow v2 待审与复核终态

tomorrow v2 在同一只读快照上为每只候选建立证据 manifest：当前规范行情固定贡献一条
`structured_point_in_time`，不早于父市场同股报价的候选批次可再贡献一条
`intraday_tail`，其余财务、监管、公告和新闻证据只来自匹配的 `ResearchEpoch`。未来证据、
过旧候选尾盘、交易日或配置错配研究均不得进入 manifest。最新研究历史不完整时不能清除
昨日已确认风险。

待审与保护集合由同一个确定性函数产生，只接受 `pass`、无 veto 且已有本地分的候选，固定
最多 28 只；优先级为新高风险、距 78 分门槛 5 分以内、TopK 边界前后 2 名、证据冲突、
本地分/候选分和代码。`observe_only`、`reject`、低可靠、未评分或 veto 候选不得消耗
DeepSeek。集合为空时标记 `deepseek_skipped_no_eligible_candidates`，不能标记
`deepseek_incomplete`，也不能发起物理请求。

响应可以只返回请求集合的合法子集：`applied/abstain` 逐股接纳，缺失、拒绝或迟到股票保持
本地分，整批标记 `deepseek_incomplete`。返回池外代码或 key/code 不一致时整批拒绝；
合法结果的 evidence manifest 哈希与当前候选不一致时同样整批拒绝。传输失败、deadline
已到、全迟到或没有任一合法结果时只保留 local 决策，不创建伪 hybrid。
挑战者状态、评级、自由文本和置信度不得改变待审集合、融合、动作或排序。

## 11. DeepSeek 全局预算与缓存

Flash 主审生产批次固定 4 只股票、健康 canary 固定 2 只，Pro 挑战者每次最多 4 只。
每日物理 HTTP 请求全局硬上限 168，失败、超时和 schema 修复均计数。预算通过 SQLite
原子预留，进程并发、重启和多策略共享同一总账。168 是不可突破的灾难保护线，正常运行
采用以下明显更小且可观测的策略与阶段上限：

| 桶 | today | tomorrow | d25 | shared_preheat | emergency | 行合计 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 策略/共享硬上限 | 8 | 38 | 16 | 4 | 5 | 71 |

挑战者全日上限为 2 次，全部保留给 tomorrow，且仍计入 tomorrow 与所属阶段上限；
long 永久不调用主审、挑战者或任何预算桶。候选不足时不得为空耗目标，未用挑战额度
不能转给 long。emergency
仅在正常桶耗尽且出现新高风险或冻结边界变化时使用，并同时计入对应策略和全局用量。
普通阶段目标合计 36 次，正常阶段硬上限合计 66 次，加入 emergency 后计划内最坏上界
为 71 次；目标是质量健康时允许的正常停止线，不是必须耗完的配额。阶段目标/硬上限为：
shared preheat `2/4`、today main `3/5`、today late `2/3`、tomorrow morning
`6/10`、afternoon `10/18`、final `5/10`、d25 morning `2/4`、afternoon `4/8`、
final `2/4`、emergency `0/5`。未用额度不得从后段倒借给前段，确保 tomorrow 最终补审
仍有独立空间。
long 不保留策略评分槽或模型阶段槽，物理请求始终为 0。

today 主审与挑战者均在 11:18 停止提交并受 11:20 冻结 compare-and-set 保护；
tomorrow/d25 的主审、挑战者和 emergency 均在 14:46 停止提交，14:48 后拒绝所有新请求。
已在途结果也必须通过
各策略 deadline 和冻结 compare-and-set，迟到结果只记脱敏审计。
预算预留发生后无论 HTTP 成败都不退回，避免重启或竞态重复消费。

通过硬过滤且板块可靠度不低于 0.85 的待审集合，每策略每次投影最多 28 只；主审优先级
固定为：新高风险、距动作门槛 5 分以内、全局 TopK 边界前后 2 名、证据冲突、本地排名。
待审集合与决定快照能否进入 hybrid 的保护集合必须由同一确定性选择函数产生。软桶耗尽时
未处理候选保持本地推荐，不能为补满 TopK 或普通利好使用 emergency。Pro 挑战者只处理
高风险、动作边界、方向/证据冲突或排名保护集合；只能确认、删除或降低正向事实，不能
解除硬过滤、下行保护或新增无证据利好。

主审使用 1-4 股批次，canary 使用 1-2 股，Pro 最多 4 股。响应可以只返回请求集合的合法子集：已返回股票逐只
接受，缺失股票按拒绝/本地降级处理；任一返回对象违反 schema 时不部分接纳该响应，整批
最多执行一次结构修复。连接失败或超时不得立即重试；只有 HTTP 200 且 schema 非法时
允许一次结构修复。单次网络 timeout 为 20 秒。每次 Flash、Pro、失败、超时或 schema
修复都按真实 HTTP 计数；缓存命中和本地策略投影不计数。

每日从 2 只高质量候选 canary 开始；canary 必须 HTTP 200、schema 合法、至少一只形成
`applied/abstain` 且在 timeout 内完成。连续两个传输失败、连续两个 schema 失败，或最近
5 个完成批次的候选应用率低于 40% 时，SQLite 原子健康门熔断 15 分钟；半开只允许一个
2 股 canary。11:15 和 14:43 后不得半开。至少 3 个合法批次且候选应用率不低于 60% 才
视为健康；熔断只停止物理请求，不阻止本地推荐、缓存投影或只读 Web。

原始 facts 缓存键至少包含股票、交易日、阶段、证据 manifest、模型、thinking 模式、
prompt、schema 和配置版本，不包含普通报价 `data_version`；同股跨 today/tomorrow/d25
在证据 manifest 未变时共享同一原始 facts。普通价格 change 命中缓存时只触发本地投影、
融合和决策/SSE 发布，不新增 DeepSeek HTTP；证据、结构化风险、模型、prompt、schema 或
配置版本改变才进入复核路由。动作和策略解释必须按策略隔离。缓存淘汰不回退已发生的
预算计数。

## 12. 融合、动作与稳定排名

固定公式：

```text
final_score = clamp(
    local_score * 0.68
    + deepseek_score * 0.32
    - deepseek_risk_penalty,
    0,
    100,
)
```

`local_score` 已扣本地风险，融合时禁止再次扣 `local_risk_penalty`。所有输入使用十进制
确定性计算，最终以 `ROUND_HALF_UP` 保留两位小数。固定验收向量结果必须为 `83.40`。
未应用有效 hybrid 时（无复核、复核拒绝/弃权/迟到、覆盖率或已知维度不足，或当前模式
不是 hybrid），`final_score = local_score`，模型风险 penalty 不从分数扣除；已通过
schema、证据与本地规则映射的独立 veto 仍可保持更保守动作。

生产排序固定为：`final_score` 降序、`local_score` 降序、股票代码升序。模型评级、
置信度、挑战状态和自由文本不得改变同分顺序。

本地评分完成后必须立即生成 local `ScoredDecision` 并尝试提交 `UnifiedDecisionIndex`，不能
等待 DeepSeek。复核结果随后只对未冻结、仍为 current 的同日 local 父版本生成 hybrid
`ScoredDecision`；两阶段必须有不同决策版本和 ETag，迟到结果不得覆盖冻结或更新版本。
失败、超时、预算耗尽或空响应保留已经发布的 local。`LongProjection` 不含评分阶段且不调用
DeepSeek。

活动动作阈值：

- today 09:36-10:30：`final_score >= 70` 可执行；
- today 10:30-11:20：`final_score >= 76` 可执行；
- tomorrow：`final_score >= 78` 可执行；
- d25：`final_score >= 76` 可执行；
- 距对应门槛 5 分以内为观察，其余不可执行。

可执行项还必须通过硬过滤、时间窗口、新鲜度、周期匹配且无 veto。Tomorrow/D25 的
`close_fallback` 使用各自正常门槛；Today 不允许 `close_fallback`。已有正式记录冻结后动作
不可改变。
旧动作和旧阈值只供带旧策略/引擎/schema 身份的冻结回放，不得成为活动默认值。

### 12.1 tomorrow v2 local/hybrid ScoredDecision

tomorrow v2 本地选择完成后先生成 local `ScoredDecision`，其 `final_score=local_score`；
存在至少一个合法 `applied/abstain` 结果时，再生成引用 local 父版本的 hybrid
`ScoredDecision`。逐股只对合法 review 执行固定 68/32 和本地证据规则映射的模型风险；
缺失、拒绝、迟到和池外 review 不改变本地分，也不能创建模型 penalty 或 veto。固定向量
仍必须为 `83.40`，本地风险只扣一次。epoch 完整条目固定只覆盖每板最多 120 只、全局最多
360 只已评分候选；其余全市场过滤结果以总数和原因计数保存。

融合后动作固定为：`pass` 且无 veto、最终分不低于 78 为 `executable`；不低于 73 为
`observe`；`observe_only` 且无 veto、同时达到观察线时保持观察；`reject`、未评分、
低于观察线或任一 veto 为 `unavailable`。活动正式池最多 6 只、观察池最多 6 只，两池分别按最终分、本地分、代码稳定
排序，并分别执行单板最多 `ceil(pool_limit * 60%)`、单行业/`unknown` 最多 2 只。被容量、
板块或行业约束跳过的候选保留原动作和独立原因，不从池外补数。

73 不是独立规定的收益最优分，而是活动 tomorrow 执行门槛 78 减固定观察余量 5 得到的
观察线，用于保留接近正式门槛但暂不可执行的样本。是否调整 78 或余量 5 必须通过预登记的
样本外收益与稳定性门禁，不能把当前固定值表述为已证明的最高收益参数。

### 12.2 tomorrow v2 风险事实时间规范化

tomorrow v2 原生输入接受任意带时区、且不晚于本次评估时间的点时特征，但进入本地风险
推导和 `ScoredDecision` 前必须把候选特征、报价、证据及外部风险事实的时间转换为
`Asia/Shanghai`；全市场人口在组装规范 market epoch 时完成同样转换。该转换只统一同一
绝对时刻的表示，不改变证据年龄、风险事实身份、触发规则、罚分、veto 或排序；仅时区表示
不同的输入必须产生相同本地风险事实和决策结果。

任何规范化后的特征、报价、证据或风险事实晚于 `evaluated_at` 都属于未来数据，必须拒绝
整份 v2 原生输入，不得通过改写时区、截断时间或忽略事实继续评分。无时区时间同样拒绝。
DeepSeek review 继续在既有 schema、证据和截止校验后独立规范化，不受本节影响。

### 12.3 tomorrow v2 原生直投影与一致性比较

生产原生路径直接从深层不可变的 `TomorrowNativeInput.market_features` 建立全市场选择人口，
不得先合成再拆解 daily/market/candidate epoch。全市场人口仍逐项应用
`preselection_replay_feature`，并只把发现行情的 source time 规范到不晚于批次评估水位；
显式 `candidate_features` 是唯一可评分集合。共享选择函数必须与常规数据平面路径使用同一
`TomorrowSelectionPolicy`、硬过滤、板内横截面、风险推导、动作和稳定排名实现。

原生决策的 market/candidate epoch 字段是从规范输入哈希稳定派生的审计身份，不代表重新
物化的数据平面 epoch。相同规范输入、策略和 sequence 必须得到相同本地决策版本；直投影
前后候选代码、local score、风险事实、动作、排名和硬拒绝计数必须完全一致。

确定性一致性按规范输入、策略和 sequence 复算：硬过滤原因名称与计数、选股结果和正式排名
后的代码元组都必须完全一致，顺序变化仍是不一致。发布和决策年龄从原生批次
`evaluated_at` 起算，行情自身的新鲜度继续由点时过滤和特征审计约束。

### 12.4 tomorrow v2 临时空集与风险覆盖作用域

tomorrow v2 必须在本地 decision 生成后、CAS 前按显式候选集合判断空集性质。候选因
`stale_quote`、`missing_liquidity_history`、`invalid_liquidity_history` 或
`candidate_core_missing` 未能形成任何本地分时，属于输入未就绪，不是策略真实空集：
热运行保留最近同日有效决策，冷启动返回 `not_ready`。仅当候选输入完整且零评分、零入选
完全由 ST/退市、停牌、价格/成交额非法、涨幅过热、已确认结构化风险或候选分门槛等业务
规则造成时，才允许发布 ready 的真实空结果。已有本地评分但因 TopK、行业/板块集中度或
动作资格没有正式入选时仍是 ready 的非空评分结果，不归类为业务空集。

全市场人口只承担硬过滤和板内横截面，不要求对 5533 只股票逐只完成公司研究。
`structured_risk_unavailable` 和 `corporate_risk_history_unavailable` 的决策门禁只按显式
候选判断；缺失时仍可计算本地分，但 disposition 固定为 `observe_only`，不能进入
DeepSeek 或正式执行池。板块身份、上市日期和上市交易日龄同样只在候选作用域决定动作
资格；没有真实免费主数据时禁止补造。过滤审计必须分别输出 population 与 candidate
计数，聚合总数只作摘要展示，不能作为空集性质判断的唯一证据。

### 12.5 today v2 local/hybrid 与冻结

Today 原生输入直接使用本章既有候选、板内策略、本地风险和固定融合规则生成统一
`ScoredDecision`。`today_observe` 的合格结果只进入观察动作；`today_main` 使用 70 分执行门槛，
`today_late` 使用 76 分执行门槛，观察线仍为对应门槛减 5 分。合法结构化复核只能升级仍为
current 的同日 local 父版本；11:18 起停止新提交，完成时间等于或晚于 11:20 的结果一律迟到。

11:20 精确关闭 Today 决策日：有不晚于边界的 current 时只冻结 official-only 投影；没有时
保持 `not_ready`。边界时或之后启动不得用启动检查点、旧发布链或收盘行情补造 Today，同日也不允许
`close_fallback`。已有正式记录不可被重新评分；后续行情只更新报价 overlay，且只覆盖正式入选
代码，不改变股票、分数、风险、动作和排名。

## 13. TopK 与集中度

today、tomorrow、d25 各自在主板、创业板和科创板三个草稿合并后全局稳定选择；全体共
九个 `strategy + board` 评分组，不能按板先填满再拼接。
活动正式推荐最多 Top6，观察池最多 6，当前只读接口总上限为 12，`top_n=0` 合法；允许
不足或为 0，不为满足数量放宽过滤和可靠度。动作在选择前分池，观察候选不得占用正式
推荐名额；两池分别应用相同板块和行业集中度约束，响应按正式推荐在前、观察在后
稳定合并为单一数组。普通 Web 在 today、tomorrow、d25 当前视图中把该数组拆成正式推荐
主表和独立观察池表。观察池是纯内存的盘中数据：today 只在未冻结的 09:30-11:20 展示，
tomorrow/d25 只在未冻结的 09:30-14:50 展示，11:20-13:00 午间仍保留；冻结、盘后和
显式历史均不展示、不写检查点、冻结 JSON、SQLite 推荐明细、归档或收益结算。旧策略冻结
文件保持不可变，但普通读取只投影其中的 `executable`；long 显示固定研究池。

`close_fallback` 继续使用正常动作、风险和集中度规则，但提交前必须执行正式投影，只把
`executable` 写入不可变记录；本地-only 恢复产生的 `observe` 只能存在于本次计算内存，
提交前立即丢弃。没有正式推荐时仍创建可审计的空冻结记录，不得用观察候选补数、不得降低
硬过滤或板块人口创建门槛。

集中度约束固定放在最终稳定排序之后：单一板块最多 `ceil(top_n * 60%)`，同一行业最多
2 只。空白或缺失行业统一归入一个 `unknown` 组并同样最多 2 只；竞争组字段只作审计标签，
不得覆盖行业上限。行业过度集中时按稳定次序保留更高分候选，并保存被跳过候选的原排名、
限制值和原因。约束只在已通过硬过滤和动作质量门的候选间选择，不能引入池外票；不足时
保持较少结果。正式池与观察池分别计数，互不占用对方的行业名额。

long 使用固定研究池和当前报价，不参与三策略 TopK、最终集中度选择、冻结和历史日期列表；
页面展示数量只受配置分组上限控制。

## 14. V2 冻结与回放

冻结只保存正式推荐自身的输入摘要、本地分、模型分、风险事实、最终分、动作、排名、
板块、证据、数据/规则/配置/因子/策略/引擎/融合/schema 版本和规范 JSON 哈希。正式记录
校验必须确认不可变文件哈希、正式字段和重放输入中均没有非正式代码；由于观察池身份已在
写盘前删除，冻结后不得声称还能重放完整候选横截面或回测观察池。评分正确性由冻结前同一
决策投影、正式条目字段一致性和发布门禁保证。

today 11:20、tomorrow/d25 14:50 后，迟到行情、模型结果、风险和补算不得改变已经存在的
记录或 JSON。today 在 11:20 冻结时立即持久化；错过该边界后不得通过启动检查点、旧发布链或
收盘行情追补，15:00 只为已有正式记录保存报价 overlay。tomorrow/d25 同日记录缺失时，
连续运行直接保留本次进程 V2 current 的股票、分数、动作和排名并换入收盘锚点，冷启动则先查正式记录，仍
缺失才用完整同日收盘行情重新执行硬过滤、九组预选、三板本地评分和 TopK。冷启动补算
不新增 DeepSeek HTTP，结果标记 `close_fallback`、`official_close` 和 `local_only` 后进入
正常历史；提交后不可覆盖。新 release 只读取当前 V2 schema 的冻结记录，不导入或解释
旧冻结；旧数据只能由完整旧 release 离线查看。long 不进入冻结和回放。

冷启动的冻结资格与持续运行的迟到 tick 分开判断：持续运行可在边界后短暂延迟时提交边界
前已经封存的相同对象；进程在边界时刻或之后才创建时，today 永久记为 missed，
tomorrow/d25 仅在 14:50（含）至 15:00（不含）可恢复有效检查点，15:00 起只走
`close_fallback` 判断。每个 `FreezeAttempt` 先完成 official-only 投影并固定 snapshot/decision
ID、规范载荷和 SHA-256；队列拒绝、SQLite/JSON 临时错误或正式记录提交暂时失败只能按
1/2/5/10/30 秒重试这一对象，不得重新评分、重新选择或让收盘补算抢占 pending 14:50
attempt。不同内容争用相同策略交易日时必须冲突并停止自动重试。

### 14.1 tomorrow v2 冻结选择与锚点

tomorrow v2 在 14:50 封口时只接受 `observed_at <= 14:50` 且已经通过
`UnifiedDecisionIndex` CAS 的最新完整 `ScoredDecision`。local 与 hybrid 都可冻结；若
deadline 前完成的 hybrid 已经接纳则冻结 hybrid，否则冻结 local，不等待模型，也不把
迟到 review 的分数、风险或 veto 写入冻结。封口后同日任何新 sequence 均只供审计或下一
交易日研究。

冻结投影先删除观察池，只保留 `selected=true` 且 `action=executable` 的正式条目；冻结
锚点只覆盖这些正式代码，价格必须为正且来源时间不能晚于冻结提交时间。正常冻结和检查点
恢复沿用决策内点时报价；`close_fallback` 使用正式代码的显式同日收盘锚点，但不得改变
正式决策的分数、动作、排名或风险。
冷启动收盘补算只能提交 local，不能新增或复用一次新的 DeepSeek HTTP；运行中 V2 current 固化可
保持边界前已经合法接纳的 hybrid。

检查点、正式冻结和 `close_fallback` 都绑定决策内容哈希、配置/策略/融合/schema 版本和
自身规范 SHA-256。相同身份重试必须幂等，不同内容争用同一交易日必须冲突；检查点只有在
正式冻结提交并进入当前索引后才能消费。持久化、哈希或恢复失败保留最后有效决策并显式
返回降级状态，不允许空结果覆盖。

## 15. 策略验证与发布门禁

每条冻结推荐由后台结果结算器生成不可变 `RecommendationOutcome`，HTTP 请求不得触发
行情抓取、结算或写库。today/tomorrow 记录 T+1 最低价、收盘价、全市场等权基准收益、
20bp 往返模拟成本后的净超额收益、MAE 百分比和 `MAE/ATR20`；d25 分别记录 T+2、T+3、
T+5 收益及 T+1 至 T+5 的最低价和 MAE。停牌、交易日缺失、价格不连续或公司行为无法
可靠排除时标记 `insufficient_data`，不得猜测补值。

T+1 的严重回撤定义为 `MAE/ATR20 <= -1.5`，d25 定义为 `MAE/ATR20 <= -2.5`。活动运行只
对正式冻结推荐执行结果结算，不生成同期基线/挑战者双跑。盘中当前快照在内存保留过滤
审计、候选和评分输入摘要；冻结记录只保留正式推荐的本地/模型风险、动作、排名、版本、
正式重放输入与规范哈希，并通过正式 `selection_diagnostics` 解释空结果。这些活动能力
不等同于完整的影子策略决策轨迹、候选召回审计或收益晋级系统。

### 15.1 待验证收益路线

待验证收益路线不改变当前生产策略、安全规则、冻结记录、DeepSeek 预算或普通 Web 推荐。
当前代码尚未实现独立的收益挑战者版本、候选乐观上界、按覆盖率向 50 分收缩的统一组件
公式、连续入场形态分、热度与弱结构组合观察规则、候选召回率审计或配对 bootstrap 晋级；
运行配置名称中的版本号也不表示这些策略变体已经上线。

Score-R0（评分科学化研究，非生产）已预注册以下固定边界，先实现契约与审计再切入实现：

- 研究评价固定最多 60 个不同交易日，不允许“挑日”与“后验参数重估”；
- 固定为最多 40 个历史回放交易日 + 20 个连续前向影子交易日；
- 研究链只对硬过滤通过总体做逐股审计，硬过滤失败的逐股数据不纳入候选/评分/收益轨迹；
- 目标变体为 `continuous_entry`、`coverage_shrink`、`candidate_upper_bound`、`heat_weak_structure`
  与 `combined_v1`，先并行补齐研究端口与固定决策轨迹（不改活动决策、冻结、发布或 Web）。

Score-R0 的窗口和统计常量固定如下，后续批次不得根据收益修改：

- 历史主窗口为 2026-06-15（含）至 2026-08-10（含），按当时有效的 A 股交易日历应覆盖
  40 个交易日；2026-06-19 休市，不得生成样本。主窗口内点时输入无效或缺失时，按交易日历
  从 2026-06-12 起逐日向前扩展，最早不得早于 2026-05-18，只取距离主窗口最近的前序有效日，
  直至取得 40 个有效日；达到下界仍不足 40 日时只输出探索报告，不得晋级。无论日历跨度
  如何扩展，历史评价样本不得超过 40 个交易日。
- 前向窗口为 2026-11-02（含）至 2026-11-27（含）的 20 个连续计划交易日。数据失败、
  服务中断或无效批次保留原日期与失败身份，不向前后顺延、不以其它盈利日替换；不足 20 个
  连续有效交易日即不得晋级。
- `bootstrap_master_seed = 20260811`，`bootstrap_repetitions = 10000`。每个变体与区块长度的
  独立种子取 `score_p0_v1|20260811|{variant}|{block_days}` 的 UTF-8 SHA-256 摘要前 8 字节，
  按无符号大端整数解释，避免运行子集改变随机流。
- 配对移动区块 bootstrap 固定以交易日为采样单位、有放回抽取并截断至原日数；5 日为主区块，
  3 日和 10 日只作预注册敏感性报告。五个挑战者的主要 20bp 日组合收益假设构成唯一检验族，
  以家族错误率 0.05 执行 Holm 校正；50bp、100bp、区块敏感性和其它指标完整报告但不新增
  事后选优检验族。

独立研究证据只允许保存活动硬过滤通过总体的逐股研究身份、候选/评分轨迹及后续结算引用。
硬拒绝股票代码、简称、逐股事实、分数和未来收益均不得写入研究证据；拒绝侧只保存按交易日、
板块和细分拒绝原因聚合的数量、输入 manifest 哈希、聚合哈希、版本和失败状态。原始输入按
规范哈希去重，研究证据不得复制 DeepSeek 自由文本或完整外部响应。
研究链不得新增 DeepSeek 物理 HTTP 请求。

G1 只冻结 Score-R2 的两阶段点时读取 schema，不执行 40 日提取或收益回放。第一阶段按交易日
返回硬过滤通过候选的紧凑组件、三板点时覆盖和硬拒绝的板块/原因聚合，不得返回硬拒绝逐股
身份；第二阶段只为排序后且唯一的明确请求代码返回完整候选字段、日线、分钟、共享前置因子
窗口和结算证据。完整字段必须精确覆盖请求代码，三板结算覆盖完整；候选、行情和因子等决策
输入不得晚于候选点时截点，结算证据只能在标签日后形成且不得反向进入决策输入。该扩展继承
唯一 `DataPlaneReadPort`，不授权独立供应商读取或第二套数据平面。

tomorrow v2 必须先在不可变点时输入上补齐紧凑决策轨迹，至少关联硬过滤、候选组件与
缺失、板块可靠度、本地组件和风险、DeepSeek 参与与结构化扣分、动作/下行保护、TopK
跳过原因，以及数据、配置、规则、策略、融合、schema 和引擎版本。同一输入同时生成当前
生产基线和不影响正式快照的影子结果；影子复用已有 DeepSeek facts，不得增加物理 HTTP
请求、修改统一决策索引、冻结记录、冻结 JSON 或普通推荐响应。

Score-R1 的配对版本固定为 `production_local` 与 `research_shadow`。后者只复用本输入已经存在且
通过 schema/证据校验的结构化 facts；没有可复用 facts 时保存 control 副本。
每个 `input_version` 只保存一条配对轨迹，不复制候选输入；不得新增 DeepSeek 物理请求。
硬过滤通过逐股最小轨迹包含候选组件、候选字段缺失掩码、覆盖率、板块
可靠度、候选分/排名、生产 Top120 身份和生产预选状态；已进入完整评分集合的股票再记录
本地组件、组件覆盖率、基础分、本地风险代码/扣分、本地分、融合状态、结构化模型风险代码、
动作、排名和跳过原因。Score-R2 才计算乐观上界和上界保护集合，Score-R1 必须以
`not_computed` 明示，不得用默认满分或当前最终分伪装上界。研究轨迹不参与生产排序、动作、
统一决策提交、冻结、API 或收益结算。

第一轮只允许预注册并分别验证以下变体，禁止边运行边试参：

1. tomorrow/d25 的缩量回踩与放量突破按各自必需字段独立判断，并使用连续形态分；
2. 一般评分组件按覆盖率向中性 50 分收缩，关键交易、风险或下行字段缺失仍转观察；
3. 候选增加最终分乐观上界和 TopK 召回保护，但保持活动 50 分、30% 与每板 120 只规则
   不变，直到挑战者通过晋级门禁；
4. 普通高热度改为“热度 + 弱收盘/回撤结构”的组合观察条件，一字板和现有硬风险不放宽；
5. 上述四项的合并挑战者。

每个变体严格使用上述窗口，历史回放最多 40 个有效交易日、前向影子固定尝试 20 个连续
计划交易日，总评价样本不得超过 60 个交易日。报告 tomorrow 的净超额收益、严重回撤、
候选召回、换手、集中度及 20bp、50bp、100bp 成本口径，并对 local-only 与 hybrid 做
同日同股配对。通过历史样本外门禁后才允许进入前向窗口；全程至少形成 300 条可结算的
同日同股配对，前向阶段至少 100 条。

晋级必须同时满足：

- Holm 校正后的 20bp 每日组合净超额收益差 95% bootstrap 置信下界大于 0；
- 严重回撤率不高于基线且报告区块置信区间；
- 硬过滤通过总体内最终 TopK 候选召回率不低于 99%；
- 删除贡献最好的单个月份后累计收益差仍为正，删除贡献最好的单一板块后不为负；
- 单只股票不超过全部正向超额收益的 10%，前五只合计不超过 30%；
- 最高分组净超额收益高于最低分组、平均日内 Spearman Rank IC 大于 0，且高分区不得持续
  出现更高严重回撤；
- 相同决策绑定同一数据、规则、配置、策略、统计和报告哈希，并同时报告三档成本。

DeepSeek 融合还必须证明相对 local-only 的独立增益，未通过时继续发布 local。

只有历史样本外与前向影子门禁全部通过，才允许另立交付批次修改活动策略与版本。未达
门禁时继续使用当前生产策略，影子结果只能作为研究证据；运行时不得自动调权、自动降低
门槛、自动晋级或自动回退。

#### 15.1.1 研究身份、状态与生产隔离

Score-R0 研究身份固定为 `score_p0_v1`。一次研究决策必须同时绑定研究 spec、交易日、观察点、
`input_version`、生产 `decision_version`，以及数据、日历、规则、配置、策略、融合、schema、
引擎和统计版本；规范载荷生成 SHA-256，写入时钟、文件路径和执行顺序不得参与内容哈希。
研究状态固定为：

```text
draft -> extracted -> historical_rejected
                   -> historical_passed -> forward_collecting
                                        -> forward_rejected
                                        -> promotion_eligible
```

状态可以因证据不完整或门禁失败单向终止，不得把失败批次删除后重跑成成功。`promotion_eligible`
只表示满足研究门禁，不表示已上线；研究状态变化不得直接写活动策略配置、规则注册表、融合配置、
冻结记录或统一决策索引。

研究轨迹迁移到 V2 后只允许由已经提交的 `V2DecisionCommitted` 及其同批不可变研究审计投影形成，
observer 不得重新读取行情、重新评分或重新调用模型。研究审计至少包含硬拒绝聚合、硬过滤通过
候选的板块和行业键、候选组件、缺失掩码、覆盖率、板块可靠度、生产候选分/排名/Top120 身份，
以及已进入完整评分集合股票的本地组件、风险、动作与跳过原因。研究审计使用独立哈希，不得改变
活动 `decision_hash`；身份变化必须显式升级研究 schema。

同输入的 hybrid 只允许引用当时已经存在且通过 schema/证据校验的结构化 facts；没有可复用
facts 时必须保存 local control 副本。研究链和所有挑战者的 DeepSeek 物理 HTTP 请求增量必须为 0。

#### 15.1.2 历史点时覆盖与两阶段裁剪

Score-R2 开始提取前必须先形成历史证据覆盖审计，逐日验证实际交易日历、三板点时总体、候选
摘要、日线、分钟线、共享前置因子窗口、公司行为处理、标签日后结算和全部来源时间。不得从当前
供应商响应反推历史时点，不得把标签日后形成的事实写回决策输入。主窗口缺失只能按第 15.1 节
已预注册的日期向前扩展；达到 2026-05-18 仍不足 40 个有效日时状态只能停在探索报告。

第一阶段只读取硬拒绝聚合和硬过滤通过总体的紧凑组件。硬拒绝代码和逐股载荷在投影边界立即
丢弃。第二阶段只为明确且排序唯一的保护集合读取完整字段；保护集合从活动每板 Top120 起步，
再加入任何仍可能进入研究 TopK 的上界候选。相同代码/交易日/分钟或共享窗口出现同身份不同内容
时整日失败，禁止按到达顺序选择。

一般评分组件的已知覆盖率和统一收缩分固定为：

```text
coverage = sum(known_weight)
known_weighted_mean = sum(known_weight * known_score) / coverage  # coverage > 0
coverage_shrunk_score = 50 + coverage * (known_weighted_mean - 50)
```

`coverage == 0` 时 `coverage_shrunk_score = 50`，但所有原始字段仍保持缺失；该公式等价于
`sum(known_weight * known_score) + (1 - coverage) * 50`。关键交易、可执行性、风险或下行保护
字段缺失仍按现有规则转观察或拒绝，不得用收缩公式绕过。该统一公式只属于
`coverage_shrink` 研究影子；通过完整晋级与生产发布前，`production_local` 继续使用当前活动
缺失策略。

组件值域固定为 `[0, 100]`，因此单个评分族的乐观组件上界固定为：

```text
optimistic_component_score = sum(known_weight * known_score) + sum(missing_weight * 100)
mandatory_known_local_risk_penalty = sum(confirmed_non_disappearing_local_risk_penalties)
local_upper_bound = clamp(optimistic_component_score
                          - mandatory_known_local_risk_penalty, 0, 100)
hybrid_upper_bound = clamp(local_upper_bound * 0.68
                           + recorded_deepseek_score * 0.32
                           - recorded_deepseek_risk_penalty, 0, 100)
```

上界假定未知一般组件取允许最大值，但不得假定已确认风险消失。`mandatory_known_local_risk_penalty`
只汇总在该点时身份下即使未知组件取最优值也仍然成立、且不会被字段补齐撤销的本地风险扣分；
可被后续合法证据解除的风险必须以状态分支分别计算上界，不能直接归零。硬过滤、关键缺失、veto、
`observe_only`、动作资格和集中度约束始终生效，不能通过分数上界绕过。有合法可复用 DeepSeek
facts 时，hybrid 上界使用既有 `deepseek_score` 和已验证模型风险扣分代入固定 68/32 公式；没有
facts 时 `hybrid_upper_bound = local_upper_bound`，不得假设一次新的模型满分。

候选召回保护使用约束感知 `active-set`，不得采用事后分差或固定多取若干只：

1. 先完整评分活动每板 Top120，并用正式池、观察池分离的生产选择器形成当前边界；
2. 按乐观最终分降序、代码升序检查未加载候选，判断其在最佳允许状态下能否穿过分数、板块
   60%、每行业最多 2 只、`unknown` 行业上限和 TopK 排序约束；
3. 仍可能进入任一池的最高上界候选加入完整字段集合，重新评分和选择；
4. 重复直到每个未加载候选都有 `upper_bound < constrained_selection_frontier` 的不可进入证明。

证明只保存候选研究身份、上界、边界、规则版本和哈希。任一上界无法复算、组件范围不成立或选择
约束无法证明时，该日召回审计无效，不得用估算值补齐。最终候选召回率按所有有效日的研究 oracle
TopK 微平均计算；证明排除项属于 oracle 已证实不可能入选的部分，而不是未观察样本。

#### 15.1.3 五个挑战者的唯一行为

五个挑战者都以 `production_local` 为唯一基线，使用相同硬过滤通过总体、点时输入、结算标签和
确定性平局键；每个变体拥有独立 `variant_version`，不得共享可变权重、阈值或缓存状态。

1. `continuous_entry`：只改变 tomorrow/d25 缩量回踩和放量突破的研究形态值。两种形态分别
   使用自己的必需字段；每个布尔边界改为以当前生产阈值为中心的预注册分段线性 `[0,100]`
   隶属分，单个形态取其必需条件子分的最小值，两种形态取最大值。各字段过渡宽度和端点必须
   在读取任何收益前以表格签入本文及机器契约；关键字段缺失仍转观察。
2. `coverage_shrink`：所有允许中性缺失的一般组件统一使用第 15.1.2 节的向 50 分收缩公式；
   不改变硬过滤、关键字段缺失、风险 veto 或下行保护。
3. `candidate_upper_bound`：只在研究影子中使用 active-set 扩展完整评分保护集合；活动 50 分、
   30% 缺失、每板 120 只和生产 TopK 保持不变。研究保护候选仍必须满足候选分不低于 50、缺失
   不超过 30% 及所有既有资格门；影子只额外评估被上界证明可能进入 TopK、但位于生产 Top120
   外的候选。
4. `heat_weak_structure`：仅研究硬过滤通过总体内、未超过板块硬热度上限但落入预注册高热带的
   候选。高热带必须与弱收盘、尾盘转弱或日内回撤至少一项同时成立才形成 `observe_only`；未伴随
   弱结构的带内高热度自身不产生观察。超过主板 8.00% 或创业板/科创板 16.00% 的股票仍按硬拒绝
   聚合且不保存逐股研究身份；一字板、交易所限制状态、公司硬风险和其它 veto 同样不放宽。
   高热带、弱收盘、尾盘和回撤的准确阈值必须在收益读取前签入本文和机器契约。
5. `combined_v1`：同时启用上述四项，除此之外不得包含隐藏权重、额外阈值或新的风险规则。

Score-R4 在过渡宽度和弱结构阈值尚未完成预注册前只能实现类型、注册表和失败契约，不得运行收益比较；
参数一经进入首个历史回放 manifest 即冻结，后续不得根据收益修改。

#### 15.1.4 同日同股收益与每日组合口径

主评价策略固定为 tomorrow，主标签继续使用 14:50 决策锚点至下一有效交易日收盘相对全市场
等权基准的收益。每个基线/挑战者以硬过滤通过候选并集形成同日同股贡献配对；挑战者和基线
未选中股票的组合权重为 0，不能因为两边名单不同而删除股票。全部输入、决策和结算有效但组合
没有正式选择时，记录合法 `no_decision` 和零暴露，该交易日仍作为有效日保留在日序列中。

若生产策略没有其它已签入的仓位权重，正式选择在各自组合内等权。每只股票在成本 `c` 下的贡献为：

```text
stock_net_contribution(c) = selected_weight * gross_excess_return
                            - selected_weight * turnover * c
daily_portfolio_difference(c) = sum(challenger_contribution(c) - baseline_contribution(c))
```

其中 `c` 分别为 `0.0020`、`0.0050`、`0.0100`，`gross_excess_return` 和 `turnover` 必须直接复用
同一 `CostSettlementBasis`，不得在报告层重新选择价格或换手定义。停牌、价格不连续、公司行为
无法可靠排除或标签不完整时保持 `insufficient_data`；历史阶段按预注册日期扩展规则决定整日是否
有效，前向阶段保留失败日期且不得替换。

严重回撤、个股贡献、板块贡献、集中度和 Rank IC 都从同一同日同股表派生。分数组固定为每个
有效交易日按最终分平均秩切成五个尽量等大的组，代码升序处理剩余和完全并列；少于 5 个有效
评分股票的日期不进入分组与 Rank IC，但仍进入组合收益。Spearman 使用平均秩，日内有效配对少于
5 条时该日 Rank IC 缺失且不得补 0。

#### 15.1.5 配对移动区块 bootstrap 与多重检验

统计输入是按交易日升序排列的 `daily_portfolio_difference(20bp)`。区块长度为 `b` 时，从原序列
枚举所有长度为 `b` 的非循环连续区块；使用该变体和 `b` 的独立种子有放回抽取区块，按抽取顺序
拼接并截断到原交易日数。样本日数小于 `b` 时该敏感性结果无效，不得改用循环区块或缩短区块。

每个变体/区块固定生成 10,000 个配对重采样均值。95% 区间采用未中心化配对分布的 2.5% 和
97.5% 最近秩分位；主要单侧 p 值在保持日序和区块结构的前提下先将日差减去原样本均值，再对
零假设分布重采样，固定为：

```text
p_value = (extreme_count + 1) / (10000 + 1)
```

其中 `extreme_count` 为零假设重采样均值不小于原观察均值的次数。五个挑战者的 20bp、5 日主
区块 p 值构成固定家族，按 p 值升序、`variant_id` 升序稳定执行 Holm step-down，家族大小始终
为 5。第 `i` 个有序 p 值只在 `p_i <= 0.05 / (5 - i + 1)` 时拒绝原假设，首个不满足项及其后
全部停止拒绝；历史失败或未进入前向的变体不能通过缩小家族获得优势。晋级同时要求 Holm 校正
通过且未中心化 95% 置信下界大于 0。3/10 日区块、50/100bp、其它指标和消融只报告，不新建
检验族。

严重回撤率差使用相同交易日区块和配对索引报告区间，门禁点估计不得高于基线。删除最佳月份
时先按上海交易日月份汇总挑战者相对基线贡献，再删除正贡献最大的月份；删除最佳板块时按股票
活动板块汇总贡献并删除正贡献最大的板块。没有正贡献组时仍执行并明确记录 `no_positive_group`。

集中度分母是全部正向同日同股超额贡献之和；非正贡献不进入分母。单股占比必须不高于 10%，
按占比降序、代码升序的前五只合计不高于 30%。最高分组净超额必须高于最低分组，平均有效日
Spearman Rank IC 必须大于 0；若最高分组相对最低分组的严重回撤率差在 5 日配对区块 95% 置信
下界大于 0，则视为高分区持续出现更高严重回撤并拒绝晋级。

#### 15.1.6 历史门禁、固定前向封存与完成定义

历史报告必须单独输出 40 日窗口结果和全部失败身份。只有满足全部历史门禁的冻结变体才能进入
`forward_collecting`；进入时绑定相同变体、数据 schema、规则、配置、策略、融合、统计和报告
版本，禁止回看历史收益改参数。

前向 collector 对 2026-11-02（含）至 2026-11-27（含）的每个计划交易日只允许形成一条
`valid`、`failed` 或 `no_decision` 日记录。同一日期相同内容重放幂等，不同内容冲突；服务停机、
数据失败、空选择和零收益必须保持各自真实身份。仅当输入、决策和结算均完整时，`no_decision`
才是有效零暴露日；否则必须记为 `failed`。实现可以将统计引擎、collector 和最终封存拆为独立
工程批次，但不得因实现了 collector 就宣称前向证据完成。

最终封存必须等最后计划日的 T+1 标签完成，且 20 个计划交易日均有有效日证据、总计至少 300 条
可结算同日同股配对、前向至少 100 条。任何计划日无效、身份冲突、参数变化或样本不足都转为
`forward_rejected`；不得向前后顺延、补抓后来数据、删除亏损日或以历史日替换。最终报告分别
列出历史、前向和 40+20 合并结果，并重新按固定五变体家族执行全部晋级门禁。

#### 15.1.7 第二轮权重、风险与门槛研究

Score-R6 只有至少一个 Score-R0 结构挑战者进入 `promotion_eligible` 后才能建立，并必须先创建
新的研究身份、日期窗口、训练/验证切分、候选网格、目标函数和确定性平局规则。Score-R6 不得
复用 Score-R0 的 40+20 评价窗口，也不得把 Score-R0 前向结果重新当训练集。

候选权重必须非负且精确合计 1，并通过下式向当前生产权重收缩：

```text
weight(lambda) = (1 - lambda) * current_weight + lambda * candidate_weight
```

`candidate_weight` 位于同一组件单纯形，`lambda` 候选集合、正则强度和每组件最大偏移必须在新
窗口结果可见前预注册。板块有效样本低于预注册下限时只能回退全局候选权重，不得独立拟合小板。
78 分动作门槛和 3/4/5 分风险扣分只能从预注册有限集合联合评价，仍需同时满足净超额、严重回撤、
换手、召回、稳定性和集中度门禁。

hybrid 相对 local-only 使用同日同股和相同组合口径另做增量检验；未证明独立增益时，结构挑战者
即使通过也只能晋级 local，活动融合继续保持当前 68/32 行为而不扩大模型作用。

#### 15.1.8 人工晋级档案与生产发布

达到 `promotion_eligible` 后只允许生成不可变 `PromotionDossier`。档案必须绑定研究 spec、全部
日级 manifest、原始输入、规则、配置、策略、融合、schema、引擎、变体、统计程序和报告哈希，
列出 20/50/100bp、3/5/10 日区块、全部门禁、失败日、样本计数、消融、集中度和剩余风险。
任一哈希不匹配或报告不能从证据复算时，档案无效。

人工确认档案后仍须另立生产交付批次，先更新本文和对应机器契约，再提升策略、规则、配置、融合
及 schema 中实际变化的版本，最后修改活动实现并完成全量质量、构建、wheel 和桌面验收。研究
通过不授权运行时自动选择挑战者、自动写配置、自动回退或同时保留两套活动策略；未发布或发布
批次失败时继续使用当前生产策略。

### 15.2 活动发布门禁

自动验证至少覆盖：

- 三板识别、冲突、上市第 1/5/6 日和主板 8.00/8.01、成长板 16.00/16.01；
- 行业、同行和竞争组字段变更不改变候选分、本地分、DeepSeek 分、动作或下行保护；
- 2.5%/97.5% 截尾、板内至少 100、最多 5 日回退和跨板隔离；
- 九组候选权重、九组本地权重精确合计 1.0；50 分、30%、120 只、0.85 边界；
- 换手和成交带通边界、缺失分母、d25 无双乘；
- ATR、缩量回踩、放量突破、单纯过热可执行和五类下行保护边界；
- 本地风险去重、上限、模型事实校验和禁止自由文本扣分；
- 168 物理请求、并发原子预留、失败/重试计数、14:48 截止和跨策略共享；
- 固定融合 `83.40`、ROUND_HALF_UP、排序键、TopK 60%、单行业/`unknown` 最多 2；
- 正式/观察分池分别执行行业上限，观察不挤占正式 TopK；
- local 先发布、hybrid 不同身份重发布、复核失败保留 local、冻结拒绝迟到 hybrid；
- 严重公司风险永久/90 日/三自然年边界、官方证据约束、注册表降级和旧命中保留；
- 结果结算幂等和无未来数据；
- 冻结前后竞态、旧版本回放、哈希一致性和 long 不持久化。

性能门禁使用固定 360 候选：单板预选 P95 不超过 250ms、单板单策略评分 250ms、单板
三策略 750ms、三板三策略墙钟 1000ms、360 只全局稳定选择 100ms。线程并行只能宣称
失败域隔离和绝对预算通过；若实际加速比未大于 1，不得宣称 CPU 计算加速。
板内评分性能样本必须传入独立全市场总体和有界候选集合，断言评分回调只接收候选代码；
用全市场集合完成评分后再按候选代码过滤不属于合格实现。

任何候选权重、评分权重、硬过滤、风险映射、动作门槛、TopK、模型路由或融合变化，
必须先更新本文、提升相应版本并通过点时样本外验证。验证需按交易日切分，计入 T+1、
停牌、涨跌停、费用和滑点，预注册指标并控制多重检验；未取得真实前瞻交易日证据时，
只能描述结构、可靠性或性能改善，不能声称收益提高。

## 16. 明确不采用的策略

- 不让 DeepSeek 自由文本、评级或置信度直接改变分数、风险、动作或排名。
- 不由 DeepSeek 新增候选、绕过硬过滤或选择方向。
- 不进行运行时自动调权、自动晋级、在线学习或看完结果后切换动量/反转。
- 不引入多智能体生产编排、第三方项目的交易结论、券商连接或真实下单。
- 不把 `flow_proxy` 解释为不可验证的主力或机构资金。
- 不把历史回测、研究审计或社区 Star 当作生产收益证据。
- 不为补满 TopK 放宽质量门，不把缺失值伪装成 0 或真实中性观测。
