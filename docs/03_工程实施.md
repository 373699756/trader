# 工程实施进度

## 后续阶段记录格式

历史阶段记录保持不变。新增阶段只需使用以下五项，确保中断后可恢复：

```markdown
## 阶段：<阶段号> - <名称>
- 状态：`in_progress` / `completed`
- 范围：<本阶段处理的边界>
- 实际改动：<完成的代码、资源或文档切换>
- 验证：<实际运行的最小门禁；未运行写明>
- 未完成项：<阻塞、未验证项或下一步；没有则写“无”>
```

不要为普通重命名、目录整理或低风险文档改动新增测试；只有荐股核心行为或关键回归风险变化才记录定向测试。

## 阶段：39 - 历史增量批次与 15:00 冻结统一
- 状态：`completed`
- 范围：完整历史增量双路径、临时批次/月份批量落库、历史路径和冻结合同统一。
- 实际改动：新增有界 `download_batch_size`；批次全部校验后再写临时月库，失败不推进批次 checkpoint；完整历史只请求缺口，初始历史逐股回补；统一 `data/history/baostock` 和 15:00 first-wins 文档合同。
- 验证：历史同步/训练输入/行业归档定向测试、文档契约测试、Ruff、format、compileall、`git diff --check` 通过。
- 未完成项：未连接真实 BaoStock；未证明供应商存在全市场单日接口，当前仍逐代码请求。

## 阶段：38 - 修复 TODO 2/4/7
- 状态：`completed`
- 范围：治理顶层脚本、收敛 Web 不可变发布边界、重命名训练模型 bundle owner。
- 实际改动：新增 `scripts/README.md` 清单与质量门禁；决策 GET/SSE 增加 `publication_id` 并校验完整替换和 overlay 父版本；将 `training/infra/artifacts` 迁移为 `training/infra/model_bundles`，切换全部消费者并删除旧路径。
- 验证：78 个定向 Python 测试、Dashboard JS 合同、Ruff、format、mypy、compileall、脚本质量门禁、诊断 CLI 帮助和 `git diff --check` 通过。
- 未完成项：未连接真实常驻服务、供应商或浏览器；当前工作树另有阶段 37 已暂存改动，提交时需保持批次隔离。

## 阶段：37 - 禁用长期观察入口并诊断未启动链路

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`8aeab8c6`；保留用户对 `docs/项目重构详细.md` 的未提交修改和未跟踪 `docs/todo.md`，仅在权威 Web 合同对应段落追加本批规则。
- Regression-Key：`long-observation-trigger-disabled-v1`。
- 用户可见问题：切换到长期固定观察池后，Header 的“观察”按钮仍可打开短线评分链路监控抽屉；同屏短线链路未启动，需要确认是否由 qfq 历史缺口或实时恢复未运行导致。
- 范围：Long 选中后立即禁用观察按钮、关闭既有抽屉并拒绝再次打开；切回 Tomorrow/D25 后恢复入口。按统一运行诊断确认当前服务、候选刷新、历史归档、实时恢复、冻结窗口与版本状态，不在未定位前改供应商或评分链。
- 当前 owner：`web/static/status_view.js` 统一投影健康按钮并创建观察抽屉，`web/static/dashboard.js` 拥有策略切换；推荐历史由 `PublishedHistoryCache` 在已有候选代码后调用 `HistoryRecovery`，离线归档由独立 `download` 进程原子发布。
- 目标 owner：Web 状态投影唯一决定 Long 观察入口可用性，抽屉控制器失败关闭；历史诊断继续读取现有有类型状态与统一只读诊断，不新增状态源或恢复实现。
- 全部消费者：Header 健康按钮、观察抽屉焦点/背景、策略切换、Dashboard JavaScript 契约、三档桌面浏览器门禁；运行诊断消费 `/api/status`、Tomorrow/D25 current、市场健康与历史恢复计数。
- 必删路径：无；不得隐藏 Tomorrow/D25 活动异常，不得让 Long 点击或键盘激活抽屉，不得新增 Web 侧业务判断或第二历史更新通道。
- 影响边界：Long 的 Web 交互和可访问状态；历史部分仅形成只读诊断结论。不改 Long 固定名单、短线观察池、API/SSE schema、候选/评分/冻结、历史归档、下载进程或活动数据。
- 退出条件：Long 切换瞬间按钮原生禁用、抽屉关闭且点击无效；切回短线入口恢复；JS/Web/桌面回归和 `git diff --check` 通过。诊断明确六个检查点中可确认与不可确认项，并区分候选刷新、归档发布和逐股实时恢复。
- 实际改动：Long 选中时原生禁用 Header 观察按钮、显示“观察不适用”、关闭既有抽屉并拒绝再次打开；切回 Tomorrow/D25 恢复入口。未修改下载、历史恢复、候选或评分逻辑。
- 验证：Dashboard JavaScript 契约通过；Web/应用工厂定向契约 `14 passed`；Firefox 1280x720、1440x900、1920x1080 门禁通过，Long 点击不弹出、Tomorrow 可正常打开且无溢出/重叠/脚本错误；Ruff 和 `git diff --check` 通过。
- 现场诊断：宿主 runtime 三次采样可达；Tomorrow/D25 候选数为 0 且 pipeline 未创建，首个断点是候选/盘中 universe 不可用及 market-data deadline。历史 active snapshot 为空，运行中的 `download` 持有维护锁并持续写入 100 个 pending 月分片，checkpoint 已推进到 `978/5558`；逐股 qfq 恢复只在已有候选代码后触发，因此当前无代码可补采。未中止下载或服务。
- Review：以 `8aeab8c6` 为基线复核 Long/短线交互、原生 disabled、抽屉失败关闭、三档桌面和并发工作树；本批不包含并发的训练、API、SSE 或脚本修改，未发现已知问题。
- 下一阶段边界：等待现有首次下载完成并原子发布 active snapshot 后，再以正常重启和 runtime 采样确认候选/评分恢复；不得以 Web 空态代替数据链诊断。

## 阶段：36 - 收敛主页 14 层链路空态

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`7b7632e0`；保留用户对 `docs/项目重构详细.md` 的未提交修改，并仅在其现有 Web 合同段落内追加本批规则。
- Regression-Key：`empty-funnel-summary-compact-v1`。
- 范围：优化主页顶部 14 层荐股评分链路在全部阶段计数未知时的摘要展示；用紧凑待启动文案替代 14 个 `—` 占位符，已有任一真实阶段计数时继续显示完整链路。
- 当前 owner：`web/static/status_view.js` 根据 `stage_snapshots` 拼接 14 个 `output_count`，全未知时仍逐项渲染占位符。
- 目标 owner：同一 Web 投影函数识别全未知阶段快照并输出唯一紧凑空态，不修改或推导后端阶段事实。
- 全部消费者：主页漏斗摘要、Dashboard JavaScript 状态契约、Web 静态资源/应用工厂契约和桌面布局验收。
- 必删路径：无；不得新增第二摘要实现、后端字段或兼容分支。
- 影响边界：仅 Web 可见空态文案及其契约说明；不改 14 层顺序、真实计数、评分、过滤、冻结、发布、API/SSE schema、供应商或活动数据。
- 排除边界：不处理下载失败、历史数据恢复、14:50 草稿可见性或流水线 owner；不把未知计数伪造成零。
- 退出条件：全未知链路显示“14 层链路待启动”，部分或完整已知链路保留逐层计数；定向 JavaScript/Web 契约和 `git diff --check` 通过；本批独立提交并推送到同名上游。
- 实际改动：Web 摘要复用既有评分漏斗就绪判断，将未启动的 14 个未知占位符折叠为“14 层链路待启动”；评分已有进度但规范计数尚未投影时显示“14 层链路计数待就绪”；任一阶段具有真实值时继续展示完整 14 层链。同步 JavaScript 与桌面浏览器契约、Web 权威说明和交付记录。
- 验证：Dashboard JavaScript 状态契约通过；应用工厂、Web 静态资源、推荐区和桌面 runner 单元契约 `14 passed`；桌面 Firefox 门禁在 1280x720、1440x900、1920x1080 三档通过，无浏览器错误、页面级横向溢出或摘要卡重叠；受影响 Python 测试 Ruff format/check 和 `git diff --check` 通过。
- 未验证项：未连接活动 Trader 服务、真实行情供应商或实际评分运行；本批使用确定性本地 Web fixture，不声称运行数据就绪问题已修复。
- Review：以 `7b7632e0` 为基线复核完整 diff、空态/部分已知/完整已知分支、真实零与未知语义、桌面门禁及用户蓝图既有修改；确认仅在用户现有 Web 合同段落追加本批规则，未修改 API/SSE schema、后端阶段事实、评分/冻结/发布或活动数据，未发现已知问题。
- 下一阶段边界：本批已闭合；后续若优化运行数据就绪，必须作为独立业务批次诊断真实阶段 owner，不得在 Web 伪造计数或用空态掩盖供应商失败。

## 阶段：35 - 清理退役测试并收敛测试成本

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`6bb64bf5`；保留用户对 `docs/项目重构详细.md` 的未提交修改，不纳入本批。
- 范围：审计当前测试收集、失败和耗时，删除已退役且无消费者的测试，修复仍保护现行合同但引用已删除 owner 的契约，合并有完整替代覆盖的重复静态扫描，并更新唯一测试优化计划。
- 当前 owner：pytest collection marker 提供 `train`、`history`、`recommendation`、`crosscut` 业务归属；部分契约仍读取已删除文档或迁移前源码路径，多个全树静态扫描存在重复遍历成本。
- 目标 owner：现行业务合同由最接近边界的测试唯一保护；跨业务架构、命名与打包规则由明确的 crosscut 契约持有；退役文档不再形成活动测试真相源。
- 全部消费者：`make test-fast`、三个业务门禁、`make test-full`、阶段发布验收、开发者本地 pytest 选择和 `docs/test.md`。
- 必删路径：只删除读取已退役 `docs/v1v2.md` 且无现行消费者的测试函数；其它删除必须先证明合同退役或存在完整替代覆盖。
- 影响边界：测试选择、契约路径、静态扫描成本和维护计划；不改生产源码、评分/冻结/供应商/API/Web 行为或活动数据。
- 排除边界：不删除唯一失败/恢复/冻结/迁移/schema 回归保护，不因耗时放宽断言，不处理用户正在修改的权威蓝图内容。
- 退出条件：收集与 fast/slow 数量重新实测；退役测试删除和迁移路径修复有覆盖证据；优化后的相关契约、Ruff、`git diff --check` 通过；完整 fast 运行中的既有失败被分类而非隐藏；阶段提交单独推送到同名上游。
- 实际改动：删除只读取已退役 `docs/v1v2.md` 的旧计划测试，将三条重复 recommendation 依赖/退役目录扫描合并进更强的架构契约；修复四组迁移前 owner 路径、qfq 文档 owner、两代 Changelog 记录格式和残留 `__pycache__` 空目录误报。collection hook 改为直接验证，不再嵌套启动六次 pytest；同模块源码/AST 复用解析结果；BaoStock 限速和交易所重试改用虚拟 sleep；两个 intraday deadline 测试消除 10ms 预热/线程启动竞态。隔离 pip 安装退出 fast，全树命名扫描由新增 `slow_static` / `make test-static-contracts` 专项持有；同步唯一测试优化计划。
- 验证：收集为 1853 total、1565 fast、288 slow；优化前串行 fast 为 2 分 57.9 秒/6 failures，最终 4 worker fast 为 1 分 32.5 秒，只剩既有 14:50 draft 产品失败。受影响契约全集（含隔离 pip 安装）通过，`make test-static-contracts` 20 passed；两个 intraday 并行回归连续 5 轮通过；BaoStock/交易所虚拟等待定向 2 passed；受影响 Python Ruff format/check 与 `git diff --check` 通过。
- 未验证项：未运行 `make test-full`、三个完整业务 owner 门禁或 release wheel 构建；未修改或验证运行期间并发出现的 pipeline/Web/权威文档改动。`tests/unit/application/test_decision_queries.py::test_current_never_exposes_observation_draft_after_freeze_boundary` 仍失败：14:50 后查询仍返回 observation draft；该行为属于独立产品缺陷，未通过删除/放宽测试掩盖。
- Review：以 `6bb64bf5` 为基线复核本批删除项、替代覆盖、marker 选择、慢速可达性、AST 缓存、虚拟等待、完整 fast 失败和提交边界；确认评分、冻结、恢复、迁移、API/SSE/schema 的唯一回归保护未删除。用户蓝图及并发出现的 `CHANGELOG.md`、工程设计、pipeline/Web/JS 修改排除在本批提交之外，未发现本批已知问题。
- 下一阶段边界：产品批次优先修复 14:50 freeze boundary 后 draft 可见性并覆盖适用五时段；后续测试优化只处理已剖析的 shadow/holdout/cost-aware 重型 fixture 与诊断 CLI 子进程，不得直接删除其幂等、篡改、冲突或入口可启动证据。

## 阶段：34 - 恢复缺历史实时补采并拆分历史待补状态

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`43404bbb`；保留用户对 `docs/项目重构详细.md` 的未提交修改，不纳入本批。
- Regression-Key：`recommendation-history-recovery-readiness-v1`。
- 范围：active archive 优先的逐股实时历史恢复端口、qfq 校验、deadline/取消/有界并发、当前 feature batch 重建和历史待补状态；不恢复推荐侧历史事实持久化。
- 影响边界：推荐 market-data history read path、组合根资源注入、历史健康投影和缺历史输入质量语义。
- 排除边界：本批不切换 14 层真实 stage owner、不改变评分公式/阈值/模型工件、冻结发布、活动下载归档或 Web 布局；不读取旧 `.runtime` 历史表。
- 退出条件：有 archive 时不产生历史网络请求；无/部分 archive 时只补缺失代码并使用同一 `HistoryContext` 构建字段；超时/失败不伪造字段且保留 `history_data_pending`；恢复结果不写 archive/推荐数据库；定向测试、Ruff、mypy 和 `git diff --check` 通过。
- 实际改动：新增 recommendation-owned `HistoryRecovery`，复用 bootstrap 已组合的 Tencent/Eastmoney 客户端和 data worker pool；`PublishedHistoryCache` 按 active archive 优先、仅恢复缺失代码，并让恢复 bars 通过同一 `HistoryContext` 构建当前 feature batch；增加 qfq 校验、primary/fallback、deadline、取消、有界波次、短时内存加速和健康计数；失败继续返回 `history_data_pending`，不写归档或推荐数据库；同步工程设计、根 changelog 和交付记录。
- 验证：历史恢复定向测试 `4 passed`；受影响历史训练输入/市场组件测试 `31 passed`；受影响源码 Ruff、mypy、compileall 和 `git diff --check` 通过。
- 未验证项：未连接真实 Tencent/Eastmoney/BaoStock，未执行全市场实时恢复、真实服务五时段/浏览器验收、隔离数据迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `43404bbb` 为基线复核完整本批 diff、恢复 owner、组合根资源复用、旧历史事实 owner、qfq 校验、deadline/取消/关闭语义、失败分类、持久化边界、测试和生成物；修正恢复统计和临时 executor deadline 后关闭等待问题，未发现已知问题。用户已有 `docs/项目重构详细.md`、`tests/contract/*` 修改未纳入本批。
- 下一阶段边界：下一独立批次梳理并实现第 1–14 层真实串联，拆分一级稳定过滤与二级动态过滤，清理 `build_first_nine_stage_snapshots()` 合成观测；本阶段不提前修改这些 owner。

## 阶段：33 - 修复上市初期 BaoStock qfq 复权缺口

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`d4b1851e`；保留用户对 `docs/项目重构详细.md` 的未提交修改，不纳入本批。
- Regression-Key：`baostock-early-listing-qfq-reconstruction-v1`。
- 用户可见问题：`./run.sh download` 在 `sz.001239` 上市首日因 BaoStock qfq 响应返回 `adjustflag=3` 而失败，活动快照保持不变。
- 当前 owner：BaoStock 网关把供应商明确表示“无复权结果”的 qfq 行当作普通身份错误丢弃，历史同步随后以 `supplier_data_incomplete` 拒绝整批。
- 目标 owner：BaoStock 网关记录明确的 qfq 复权缺口；仅使用同一股票 raw 的 `preclose/close` 链和后续有效 qfq 锚点按官方六位小数公式重建，链条不能闭合时继续失败关闭。
- 全部消费者：BaoStock row gateway、历史同步完整性校验、下载 CLI 状态投影及 BaoStock 网关/历史同步定向测试。
- 影响边界：上市初期 qfq 缺口的供应商行归一化和同步可发布性；保留 raw/qfq 双侧、停牌、未来行、重复行、活动快照原子发布和失败 checkpoint 合同。
- 排除边界：不改变评分、训练、历史归档 schema、供应商请求参数、qfq 公式、非明确 `adjustflag=3` 的错误处理或活动数据。
- 退出条件：明确 qfq 缺口可在可证明链条闭合时完成；普通 raw 不得伪装为 qfq；无法重建仍返回稳定失败原因；定向测试、Ruff、format、mypy、compileall 和 diff 门禁通过。
- 实际改动：网关将 qfq `adjustflag=3` 行收集为不可用日期；以后续有效 qfq/raw 比值为锚，按 BaoStock `ROUND_HALF_UP` 六位因子反向递推并只缩放同源 raw OHLC，量额与交易状态沿用 raw；缺少 raw、锚点或正值链条时返回 `supplier_adjustment_unavailable`。新增上市初期缺口回归。
- 验证：BaoStock 网关、历史同步和 CLI 定向测试 `34 passed`；受影响文件 Ruff、format-check、mypy、compileall 通过；`git diff --check` 通过。
- 未验证项：未重新执行真实全市场 BaoStock 下载，未验证 `sz.001239` 的实时响应、整批耗时或活动快照发布；未运行阶段 12 全量门禁。
- Review：以 `d4b1851e` 为基线复核 qfq/raw 字段身份、历史代码窗口、完整性拒绝路径、公式方向和暂存范围；确认未放宽 qfq 合同、未新增兼容 owner、未触碰用户蓝图修改。
- 下一阶段边界：本阶段已闭合；后续若发现其他供应商缺口，必须先证明同源 raw、官方复权算法和后续 qfq 锚点，不能扩大为任意 raw→qfq 推导或跨源拼接。

## 阶段：32 - 按业务 owner 重组测试门禁

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`df7d3f44`；保留用户对 `docs/项目重构详细.md` 的未提交修改，不纳入本批。
- 范围：依照 `docs/test.md` 为测试增加 `train`、`history`、`recommendation` 与 `crosscut` 业务归属 marker；将 Makefile 一级测试任务调整为训练、历史、推荐、快速与全量，并将运行时/供应商保留为推荐专项子集。
- 当前 owner：测试选择以 `slow_history`、`slow_runtime`、`slow_supplier` 等执行特征为主，业务 owner 不可查询；Makefile 把 runtime、suppliers 作为与 history 并列的一级门禁。
- 目标 owner：pytest collection 唯一提供测试的业务 owner 与执行特征 marker；Makefile 仅以 marker 选择业务任务，`runtime`、`supplier` 作为 recommendation 的可选子集。
- 全部消费者：开发者 Makefile 命令、pytest marker collection、测试命令契约、`docs/test.md` 测试优化计划与阶段 12 全量测试入口。
- 影响边界：测试收集和命令可发现性；不改生产代码、业务断言、运行数据、评分、冻结、供应商协议或公开 API/Web。
- 排除边界：本批不删除测试、不替换固定等待、不迁移测试物理目录、不运行发布级全量门禁。
- 退出条件：每个收集测试都有一个业务 owner marker，快速门禁只排除 `slow`，训练/历史/推荐命令可选择各自集合，推荐运行时/供应商命令只选择相应子集，命令契约与 collection 门禁通过。
- 实际改动：新增 `train`、`history`、`recommendation`、`crosscut` marker，并在 pytest collection 中给每个测试精确附加一个 owner；保留 `slow` 与四类 `slow_*` 作为正交运行特征，清理指向已删除 `test_market_history.py` 的 marker 路径。Makefile 新增 `test-train`、`test-recommendation` 及推荐运行时/供应商子集，`test-history` 改为按业务 marker 选择，删除并列的 `test-runtime`、`test-suppliers`。删除被 `pytest.ini` 覆盖的 `pyproject.toml` marker 重复定义，更新命令契约与测试计划状态。
- 验证：测试命令契约 `5 passed`；受影响 Python Ruff 与 format-check 通过；四个 owner collection 分别为 train 451、history 154、recommendation 943、crosscut 300，合计 1848，与全量 collection 1848 一致；推荐 runtime 子集 collection 80、supplier 子集 collection 127；`git diff --check` 通过。
- 未验证项：`make test-history` 实际执行在工具 30 秒观察窗口结束时已到 93%，未取得退出码；未执行完整 recommendation 业务门禁或发布级全量门禁。`make test-train` 实际执行发现两条既有失败：`test_score_cost_aware_selection_contract` 和 `test_scoring_weight_configuration_contract` 仍读取已删除的 `training/evaluation/**`、`infra/market_data/normalization/**` 路径；本批未修改其断言或迁移 owner，不能声称训练业务门禁通过。
- Review：以 `df7d3f44` 为基线复核完整 diff、Makefile help/phony/命令、pytest 配置唯一性、owner/slow marker 正交关系、收集计数和暂存范围；确认未改生产代码、测试业务断言、活动数据或用户的蓝图修改，无兼容目标或第二 marker 配置。
- 下一阶段边界：基于覆盖证据审计重复契约和退役路径测试，先修复仍引用已删除 source path 的契约，再处理固定等待、重型 fixture 与测试物理目录迁移；不得以移除 marker 或放宽断言掩盖现有失败。

## 阶段：31 - 收敛历史日更校验与复权回退

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`8add6439`；开始时工作树干净，无需保留未提交用户修改。
- Regression-Key：`history-incremental-maintenance-bounded-io-v1`。
- 用户可见问题：已有约 13 GiB、2000 交易日的活动归档只需补齐近期缺口，`./run.sh download` 却在输出行业快照后无进度地重复扫描全部月分片；最近 qfq 变化还会触发单股 2000 日联网重拉。
- 当前 owner：历史同步编排在增量规划前完整验证 active snapshot，读取近期 qfq 前再验证当月，封存时又验证全部月份；qfq、行业或供应商合同变化会决定已有股票是否重拉完整窗口。
- 目标 owner：同步编排只按 active identity 规划缺口与最近 5 日回读；未改月份直接复用已发布引用，只有新建或实际改动的月库由月分片 owner 在封存时执行 WAL checkpoint、quick check、行数和 SHA-256；训练仍在使用快照前执行自身完整验证。
- 全部消费者：`download` 同步编排、BaoStock raw/qfq 供应商边界、月分片封存/发布、checkpoint 续传、训练快照读取与历史同步定向测试。
- 影响边界：日常增量同步的本地 I/O、qfq 修订处理和运维耗时；保留每股 raw/qfq 两侧、最近 5 日回读、2 秒安全调用间隔、旁路封存和原子 active 发布。
- 排除边界：不更改评分、训练特征、raw/qfq 字段语义、结果评价、供应商、活动数据格式或已发布 snapshot；不在本批执行真实全市场下载。
- 退出条件：日更启动和未改月份零整文件验证；最近 qfq 变化不再发起2000日重拉；改动月份仍经封存校验与原子回滚；raw/qfq 消费合同不变；定向测试、Ruff、mypy、compileall 和 diff 门禁通过。
- 实际改动：删除日常同步启动时的 active snapshot 全分片验证、近期 qfq 预读/校验和发布时对未改月份的重复验证；未改月份直接复用 snapshot 引用，改动月份继续由 `seal()` 执行 WAL checkpoint、quick check、行数、SHA-256 和原子替换。删除 qfq 修订二次完整窗口请求和行业/供应商合同变化的 `full_refresh_codes`；已有股票统一只请求最近 5 日与新缺口，新股仍首次获取完整可用窗口。保留 raw/qfq 双侧、2 秒安全间隔、checkpoint、rollback sidecar 和原子 active 发布。
- 验证：历史同步、月分片、归档读取、BaoStock 受控供应商、训练输入、归档状态、应用边界、架构和 CLI 定向测试通过；Ruff、mypy、compileall 和 `git diff --check` 通过。回归明确证明 `already_current` 不调用分片验证、未改中间月份不验证、qfq/行业变化仍只请求最近窗口。
- 未验证项：未启动真实全市场 BaoStock 增量下载，因此不声称实际总耗时或供应商稳定性已验收；未运行阶段 12 全量发布门禁。
- Review：以 `8add6439` 为基线检查完整 diff、新增文档、所有全量刷新和分片验证调用；集中修正首轮只移除 qfq 回退但仍保留行业/供应商全量回退的遗漏。复核后确认新建/改动月份仍封存校验，训练完整快照验证不变，无兼容层、双 owner、活动数据改写或未登记越界。
- 下一阶段边界：本阶段已闭合。BaoStock 仍按股票分别提供 raw/qfq 且需安全调用间隔；若要把全市场日更从数小时降到分钟级，必须以新来源的全市场 raw/qfq、停牌、代码变更和 cutoff 能力实证作为独立批次，不得把 raw 伪装成 qfq 或恢复已删全量回退。

## 阶段：30 - 主页显示 14 层递减漏斗并移除评分卡竖线

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- Regression-Key：`dashboard-14-stage-count-funnel-v1`。
- 范围：仅调整主页 Web 投影；顶部状态行复用观察面板已有 `stage_snapshots`，显示 14 层输出数量递减链并保留正式/观察数量；移除评分领先卡蓝色左竖线。
- 排除边界：不改后端状态、API/SSE、评分、过滤、冻结、发布、持久化或活动数据。
- 实际改动：`status_view.js` 新增 14 层快照格式化；`dashboard_base.css` 删除评分领先卡内嵌蓝色竖线；同步 JS/桌面 Web 契约 fixture。
- 验证：Node 语法、dashboard state contract、Web contract、桌面脚本定向测试、Ruff、Python compile、`git diff --check` 通过。
- 未验证项：未连接活动运行时或真实供应商；未运行阶段 12 全量发布门禁。
- Review：确认仅修改 Web/测试文件，未新增后端字段或前端业务重算；用户已有文档修改未纳入本批。
- 下一阶段边界：阶段 30 已闭合；后续不得在 Web 重算 14 层业务计数，继续读取后端已投影的 `stage_snapshots`。

## 阶段：29 - 拆分评分领先摘要并恢复顶部漏斗可见性

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`9f4ffadb`；开始时工作树干净，无需保留未提交用户修改。
- Regression-Key：`score-leader-header-funnel-layout-v1`。
- 范围：将数据状态、评分领先、模型预算和发布状态拆成四张同高摘要卡；评分卡加宽并突出最终分范围与 Top 3；将“完整评分 · 动作合格 · 最终入池 · 正式 · 观察”固定放回顶部策略状态行。
- 当前 owner：数据状态卡同时承载评分范围、Top 3 和漏斗汇总，漏斗汇总在部分未就绪分支没有稳定赋值。
- 目标 owner：数据状态卡只投影输入就绪性；独立评分领先卡只投影最终分范围和 Top 3；顶部状态行唯一承载短线策略漏斗汇总。
- 全部消费者：主页 Jinja 模板、基础/响应式 CSS、`status_view.js` 状态投影、Web 契约/JS 状态/桌面布局验收、工程设计与产品蓝图。
- 影响边界：仅改变 Web 摘要区信息架构、层级和缺失值展示；保留现有公开 ID 与后端字段语义。
- 排除边界：不改评分、筛选、TopK、观察池、冻结、发布、API/SSE schema 或持久化；不伪造尚未产生的计数。
- 退出条件：1280、1440、1920 桌面视口四卡同高且评分卡重点明确；Top 3 纵向完整展示；顶部漏斗在已完成、未开始、历史、Long 和旧快照分支均有明确值，未知值显示 `—`、真实零保留 `0`；定向 Web/JS/布局门禁与 diff 检查通过。
- 实际改动：数据状态卡恢复可见就绪数与覆盖说明；新增同高加宽的“评分领先 · 统一 0–100 评分”卡，左侧强调线、紧凑分值范围和带名次的纵向 Top 3 共同突出重点；模型预算与发布卡保持原有内容。漏斗五项迁到顶部并在 loading、历史、未开始、完成、Long 和旧快照分支全部显式赋值，共用格式化确保未知值为 `—`、真实零为 `0`。
- 验证：JS 语法与 dashboard state contract 通过；Web app-factory、推荐分区和 HTTP/Web 定向契约 13 个测试通过；受影响 Python 测试文件 Ruff 通过；`git diff --check` 通过。仓库外隔离 fixture 的 headless Firefox 在 1280x720、1440x900、1920x1080 全部通过，确认四卡等高、页面无横向溢出、元素归属正确、顶部漏斗可见、无浏览器错误且外部网络请求为 0。
- 未验证项：未连接活动本地运行时或真实供应商数据；未运行阶段 12 全量发布、打包和数据切换门禁。
- Review：以 `9f4ffadb` 为基线复核完整 diff、新增文件、DOM ID 唯一性、六类状态分支、桌面等高与旧布局契约；集中修正未就绪漏斗的真实观察状态期望，复核后未发现评分/API 语义变更、重复 owner、隐藏 fallback、页面溢出或本批越界改动。
- 下一阶段边界：阶段 29 已闭合。后续可在活动服务实际产生短线评分后补充真实数据的五时段目测；不得把漏斗聚合放回数据卡、重复顶部 owner 或在 Web 重算业务计数。

## 阶段：28 - 将推荐历史输入原子切换到下载活动快照

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`d49b8ee1`；开始时工作树干净，无需保留未提交用户修改。
- Regression-Key：`published-history-single-owner-v1`。
- 用户可见问题：Web“数据源与静态采集”把全市场大量股票显示为缺历史而降级；推荐运行时即使已有或正在执行 `./run.sh download`，仍通过 Tencent/Eastmoney 单独联网预热，且受 360 条运行时缓存容量和易变 warmup universe 约束。
- 当前 owner：`download` 独立维护 BaoStock 月分区、checkpoint 和 active snapshot；推荐侧 `HistoryCache`/`HistoryWarmup` 另行拥有历史联网、运行时缓存、摘要持久化和健康状态，两条链路不互通。
- 目标 owner：`download` 是历史事实唯一写入 owner；手工 `download`、定时维护和启动时 ensure-current 复用同一同步用例、锁、checkpoint 与 active snapshot；推荐只通过有类型只读端口消费已校验活动快照并构建全市场历史特征，不再发起历史行情网络请求或保存第二份历史事实。
- 全部消费者：下载应用端口/用例和 BaoStock reader、组合根与运行资源、推荐 market feature service/health/source router、调度通知、状态 API/SSE、Web 阶段展示、统一运行时诊断、历史/模型输入/冻结相关合同测试。
- 影响边界：历史归档读取、全市场静态特征覆盖、启动资源与关闭、状态字段和页面 pending/rejected 语义、模型输入缺失原因；保持现有下载磁盘格式可读。
- 排除边界：不改变 V2/V3 评分公式、权重、阈值、DeepSeek、TopK、冻结 first-wins、Long 边界、训练工件或活动数据；不在仓库内执行真实全量下载或数据迁移。
- 退出条件：`./run.sh download` 产生的活动快照可被推荐直接消费；推荐历史网络调用为零；全市场覆盖不受 360 容量限制；无快照/下载中/失败/损坏均有显式状态且保留最近有效特征；旧在线历史 owner、双写和隐藏 fallback 删除；差额按互斥原因可核对；定向业务、调度、Web、架构、Ruff、mypy、compileall 和 diff 门禁通过。
- 实际改动：新增 download-owned 已发布历史 manifest/window、只读 application use case 和活动快照 adapter；推荐改用 `PublishedHistoryCache`，按 snapshot hash 以跨月 code/date 流式合并构造 V2 251 日或 V3 61 日上下文，仅保留每股最近 20 根 qfq 特征日线，并从同一快照读取 raw/qfq outcome 对。`./run.sh download`、定时维护和服务启动 one-shot 共用 `HistorySyncConfiguration.for_repository`、`DownloadHistoryUseCase`、BaoStock supplier、维护锁、checkpoint 与原子发布；手工下载后推荐会在下次读取发现新 hash。删除在线 `HistoryCache`/`HistoryWarmup`、专用 worker pool、调度通知、Tushare 参考链历史调用、历史 cache datasets 及第二份历史特征读写接口；旧物理表仅忽略不破坏。状态/API/诊断改为活动快照身份、cutoff、覆盖、读取错误和维护进度；Web 输入阶段改显示“待就绪”，不再把缺历史计作“淘汰”。
- 验证：全量测试收集通过；下载/活动快照 reader、251 日推荐窗口、缺快照 pending、损坏后保留最近有效投影、启动 one-shot、全市场行情组件、参考链无历史调用、数据平面、outcome、Bootstrap、架构、入口、状态/API/Web 和统一诊断定向测试通过；受影响 Ruff、75 个源文件 mypy、compileall、Node 语法、dashboard state contract 与 `git diff --check` 通过。另执行功能包边界合同，仍只有阶段 27 已登记的 `web/api`、`infra/artifacts` 两个既有目录清理断言失败，本批未伪装为通过。
- 未验证项：遵守活动数据边界，未在仓库活动目录执行真实 2000 日全市场下载、未启动真实常驻服务或外部供应商联网验收；因此当前本地若无活动快照，仍需首次启动同步或显式 `./run.sh download` 成功后才会形成历史就绪人口。未运行阶段 12 全量发布、打包和真实数据切换门禁。
- Review：以 `d49b8ee1` 为基线复核完整 diff、新增/删除文件、历史 owner 和全部消费者；集中修复 ApplicationSystem 生命周期方法误嵌套、V2 全市场窗口一次性缓存的 OOM 风险、reference loader 残留历史调用、无用 history pool、诊断持久化参数和过期架构测试路径。复核后生产推荐树无历史联网、360 预热容量、warmup 回调、第二历史写入 owner 或隐藏 fallback；`create_app()` 仍无 I/O 副作用。
- 下一阶段边界：阶段 28 已闭合。后续独立批次可在活动快照实际下载完成后执行真实 `/api/status` 与观察弹窗五时段验收，并另行处理既有 `web/api`、`infra/artifacts` 清理债务；不得恢复推荐侧历史供应商、第二归档或运行期逐股预热。

## 阶段：27 - 建立静态基线、动态批次与完整阶段快照

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`744f0ef4`；保留本批既有供应商、标准化、文档和 `docs/sourcedata.md` 修改，不覆盖用户修改。
- 范围：建立不可变静态/动态批次身份，区分 reference 与 market freshness，补齐后端阶段快照批次连续性和 API/Web 只读投影；第 14 层继续保持“TopK、集中度、冻结与发布”合并阶段。
- 当前 owner：输入质量状态只持有前九层快照；阶段快照缺少 input/output batch identity；Web 按 input-output 自行推算“淘汰”；完整第 10–14 层快照尚未由统一运行时 owner 生成。
- 目标 owner：运行时批次/阶段快照由推荐 application 统一生成，Web/API/SSE 只投影后端真实字段；输入质量仍只负责前九层输入链路，第 14 层作为单一最终选择、冻结与发布阶段。
- 全部消费者：market feature service、source router、质量状态、决策查询、HTTP/SSE status projection、Web `status_view.js`、阶段/冻结/发布测试。
- 影响边界：批次身份、阶段计数/连续性、来源健康、降级原因和流水线监控展示；不改变评分公式、冻结合同或第 14 层业务拆分。
- 排除边界：不把第 14 层拆成 TopK/集中度/冻结/发布多个业务阶段；不伪造尚未由运行时产生的第 10–14 层数据；不触碰活动运行数据。
- 退出条件：静态与动态 epoch 有唯一不可变 owner；完整 14 层快照由后端真实批次生成并通过连续性校验；API/SSE/Web 不自行计算淘汰；部分覆盖、stale、来源年龄和失败类别可观察；定向合同测试通过。
- 实际改动：完成本阶段全部代码切片：阶段快照增加不可变 `input_batch_id`/`output_batch_id`，每个应用层阶段按前一层 output identity 生成唯一 handoff，并校验完整 14 层身份和数量连续性；生产输入质量状态以现有有类型评分审计生成第 10–14 层真实计数，pending/local/hybrid 均不再缺层；status projection 输出完整快照和批次身份；Web 使用后端 `rejected_count`，不再用 input-output 推算淘汰；合并“数据源与静态采集”展示，移除同一 `input_readiness` 的重复阶段映射；市场元数据允许无快照时显式 `missing` 而不伪造动态 epoch，非 missing 状态仍要求 epoch；唯一内容寻址 `reference_epoch` owner 同时服务全市场特征缓存与健康状态，`market_epoch`/`reference_epoch` 通过 HTTP 白名单公开；运行健康展示拆出 `status_health.js` 以恢复活动源码 1200 行上限，并修复架构契约残留的旧 normalization 路径。未拆分第 14 层，未改变评分公式、动作门、冻结合同或 Long 边界。
- 验证：行情 merge/gateway/service/history/reference/lane、输入运行时、Tomorrow 投影、Bootstrap、Web/API 和相关架构定向测试通过；`node --check`、`node tests/js/test_dashboard_state.js src/trader/web/static/dashboard.js`、受影响 Ruff、mypy、compileall 和 `git diff --check` 通过。隔离浏览器门禁最终通过：DOM 刷新 P95 1.067 秒、patch-to-paint P95 17ms/预算 100ms、SSE patch 与 retention 均通过；前一轮曾出现一次 113ms 的阈值边缘抖动，已如实保留为复跑依据。
- 实测证据（2026-09-22）：在仓库外 `/tmp/trader-runtime-live` 隔离运行目录、端口 5057 启动真实服务；`live` 诊断成功读取 Web 3 轮，交易所证券主数据 5221/5221、历史 3/3、Tencent 3 个代码；Tushare 因缺 token 降级，历史日线能力因供应商合同阻塞。真实 `/api/status` 返回 `running`/`closed`，canonical snapshot 明确为 `missing`，Tomorrow/D25 均真实 `not_ready` 且无 pipeline；真实 `/api/events` 返回 200 SSE 并发送 `: connected`。`full` 的 Web 检查保持上述降级；浏览器检查明确失败于主机缺少 `geckodriver`（Firefox 存在），不是页面数据结论。
- 未验证项：未运行全量测试、阶段 12 发布/打包门禁或活动数据迁移。扩展运行时集成检查仍有 4 个既有调度契约失配（旧 14:50/当前 15:00 冻结期望及 after-close 冷启动任务），与本批修改文件无调用路径重叠，未记为通过或在本批越界修复；真实外部供应商仍受本机凭据和来源可用性约束。
- Review：以 `744f0ef4` 为基线复核完整 diff、新增/删除文件、批次 owner、全部快照消费者、HTTP 白名单、旧 normalization 引用、Web 重复计算、源码行数和第 14 层边界；集中修复 stage-output 通用构造仍复用同一批次 ID、架构测试旧路径和 `status_view.js` 超长三项发现。复核后未发现本阶段已知问题，确认无兼容转发、双 epoch owner、Web 反推淘汰或第 14 层拆分。
- 下一阶段边界：阶段 27 已闭合。后续独立批次可实施数据状态卡重新布局，解决 Top 3 股票信息在桌面摘要区溢出；不得回改本阶段 14 层批次链、epoch owner 或公开白名单。

## 阶段：26 - 收敛实时标准化与多源一致性

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`93abc2da`；承接阶段 25 的供应商协议，不改写已推送历史。
- 范围：将推荐专属实时 merge/feature/columnar 标准化迁入 `recommendation/infra/normalization/`；保留供应商边界所需的最小共享 quote canonicalizer；统一实时来源的交易日、观察点、证券身份和 freshness 校验；补充来源年龄、失败类别和降级状态的标准快照元数据；收敛有限 fallback 策略与架构门禁。
- 当前 owner：顶层 `infra/market_data/normalization/` 混合拥有供应商 quote canonicalizer、推荐合并、特征和列式投影；merge 只校验六位代码与未来时间，不完整校验交易日/观察点/身份/freshness。
- 目标 owner：`recommendation/infra/normalization/` 唯一拥有推荐实时清洗、合并、特征和列式投影；顶层只保留供应商输入到标准 `MarketQuote` 的共享 primitive；统一 snapshot metadata 携带来源年龄和降级原因。
- 全部消费者：供应商客户端、推荐 gateway/source coordinator、market feature service、pipeline data source、性能入口、HTTP/Web 健康投影、行情单元/组件/契约测试。
- 影响边界：实时标准化 import 路径、合并接受条件、快照降级元数据、架构契约和定向测试；不改变评分、冻结、持久化 schema、历史归档、活动数据或供应商 HTTP 协议。
- 排除边界：不迁移历史 `download/infra` 标准化；不改变真实供应商优先级配置之外的请求配额；不运行真实服务切换和活动数据迁移。
- 退出条件：推荐专属 normalization 无顶层 owner；所有消费者切换完成；不同时点/身份/交易日/过期 observation 不再进入 canonical merge；全源失败保留最近快照并带来源年龄/失败类别/降级状态；架构、定向业务测试和类型/格式门禁通过。

- 实际改动：完成推荐专属 normalization 物理迁移并清除旧顶层 owner；为 `SourceObservation` 增加交易日、观察点和证券身份字段及派生规则；merge 统一执行交易日、观察点、证券身份和可配置 freshness 校验，拒绝不一致数据拼接；`CanonicalMarketSnapshot`、`MarketSnapshotMetadata` 和健康/HTTP 白名单增加 `source_ages_seconds`、`failure_categories`、`status`；全源失败保留最近快照并标记 `stale`，无快照标记 `missing`；gateway 在显式观察点请求中启用 300 秒 freshness 上限，保留 deadline、hedge 和 circuit breaker 有界切换。
- 验证：Ruff 通过；mypy（7 个受影响源文件）通过；`compileall -q src/trader tests` 通过；`git diff --check` 通过；行情 merge、normalization、gateway、market service 和 Web contract 定向测试通过（66 passed）。
- 未验证项：未运行全量测试；未连接真实行情供应商、未执行真实服务五时段验收或活动数据迁移；历史已知的 `web/api`、`infra/artifacts` 目录清理契约失败未在本批重开。
- Review：复核完整阶段 diff、旧 normalization 引用、canonical merge 所有入口、overlay/subset 元数据传播、全源失败路径、HTTP 字段白名单和供应商 fallback；确认未恢复兼容转发、未引入第二状态源，具体供应商客户端仍仅由组合根/顶层 providers 持有。
- 下一阶段边界：进入后续台账阶段；不回退推荐 normalization owner、实时一致性校验或来源年龄/降级元数据合同。

## 阶段：25 - 收敛行情供应商端口边界

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`94b07497`；本批仅处理推荐/下载行情供应商边界，不纳入其它工作树修改。
- 范围：为推荐行情编排建立显式供应商协议和 deadline-aware fetcher；移除推荐/下载活动代码对 `SinaClient`、`TencentClient`、`EastmoneyClient`、`TushareClient`、`AkshareResearchClient` 等具体客户端类型的依赖；保留现有标准行情对象、路由、缓存和降级行为。
- 当前 owner：`recommendation/infra/market_data`、`download/infra` 直接导入并持有顶层供应商客户端；全市场路由通过具体类判断调用参数。
- 目标 owner：`recommendation/infra/market_data/provider_ports.py` 唯一拥有推荐供应商协议；组合根负责将具体客户端绑定到协议和 deadline-aware fetcher；编排器只依赖协议和标准对象。
- 全部消费者：`bootstrap.py`、`MarketDataGateway`、`MarketSourceCoordinator`、`IntradayLoader`、推荐 reference/research loader、下载历史 source adapter、行情组件测试和功能包边界契约。
- 影响边界：供应商依赖方向、构造函数类型、全市场来源 deadline 注入和静态 import 边界；不改变实时行情字段、来源优先级、合并、缓存、熔断、冻结或 HTTP/Web schema。
- 排除边界：不在本批物理迁移 `infra/market_data/normalization`；不新增交易日/证券身份/freshness 字段；不改变真实供应商协议、活动数据和下载归档格式。
- 退出条件：推荐/下载编排不再 import 具体供应商 client；网关和 coordinator 只依赖协议；生产组合根显式注入 deadline-aware fetcher；边界扫描和行情定向测试通过。
- 实际改动：新增 `recommendation/infra/market_data/provider_ports.py`；`MarketDataGateway`、`MarketSourceCoordinator`、`IntradayLoader`、reference/research loader 和下载历史 source adapter 改用协议；`bootstrap.py` 显式注入 Eastmoney/Sina 的 deadline-aware fetcher；新增客户端 import 边界契约和网关注入测试。
- 验证：Ruff、mypy、compileall、`git diff --check` 通过；行情网关/服务/车道/供应商/reference 定向测试通过；客户端 import 边界契约通过。
- 未验证项：未运行全量测试；功能包契约中两个既有目录清理断言仍失败（`web/api`、`infra/artifacts`）；未连接真实行情供应商、未执行真实服务五时段、未迁移标准化目录、未补齐交易日/证券身份/freshness 校验；未触碰活动数据。
- Review：复核完整 diff、组合根 deadline/cancel 注入、旧客户端 import 扫描和测试边界；确认具体客户端仅保留在组合根/顶层供应商模块，未引入兼容转发或第二状态源。
- 下一阶段边界：物理迁移实时标准化到 `recommendation/infra/normalization/`，并补齐交易日、观察点、证券身份和字段 freshness 的统一校验；不回退本阶段供应商协议边界。

## 阶段：24 - 拆分 research trace JSON projection owner

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`94b07497`；保留用户已有 Web/CSS/JS、`pipeline_status.py`、`test_tomorrow_projection.py` 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：将 research trace 事件 JSON projection 从归档实现迁入共享 `infra/serialization`，保持事件 schema、legacy 读取、压缩/hash 和 SQLite 生命周期不变。
- 当前 owner：`research_trace_archive.py` 同时拥有归档编排与 observation JSON projection。
- 目标 owner：`infra/serialization/research_trace_projection.py` 唯一拥有 observation/event/audit JSON projection；归档只负责持久化编排，typed decode 仍由归档内部编排。
- 全部消费者：research trace record、legacy event fixture、payload hash 校验、入口模块加载契约和研究归档组件测试。
- 影响边界：research trace 内部 projection owner 与入口加载白名单；不改变事件 schema、字段白名单、压缩字节、hash、SQLite 或活动数据。
- 排除边界：不迁移 typed decode、不改变训练 point-in-time 锚点、不触碰用户 Web/CSS/JS 修改。
- 退出条件：projection 只有共享 serialization owner；归档无重复 projection 定义；legacy/current 研究归档和入口契约通过。
- 实际改动：新增 `infra/serialization/research_trace_projection.py`，迁移 observation/audit/item projection；归档显式调用 projection owner；测试改用新 owner。
- 验证：研究归档组件、入口契约和 Ruff 通过。
- 未验证项：未运行全量测试、真实研究数据迁移、真实服务五时段或活动数据；typed decode 编排仍待后续阶段拆分。
- Review：以 `94b07497` 为基线检查 projection 消费者、legacy/current schema、hash 输入、旧定义和暂存边界；确认用户已有修改未纳入本批。
- 下一阶段边界：继续拆分 research trace typed decode 编排；不回退共享 serialization projection owner。

## 阶段：23 - 收敛共享 JSON 校验 owner 并将短线冻结切换到 15:00 收盘价

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`797b534f`；保留用户已有 Web/CSS/JS、`pipeline_status.py` 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：将 research trace JSON 边界校验迁入共享 `infra/serialization`；将 Tomorrow/D25 正式冻结边界、模型时间预算、查询正式态和调度冻结点统一切换到 15:00 收盘价。
- 当前 owner：`research_trace_archive.py` 仍消费训练侧校验；推荐冻结仍以 14:50 为正式封口，15:00 另有收盘恢复路径。
- 目标 owner：`infra/serialization/research_trace_validation.py` 唯一拥有 JSON 边界校验；Tomorrow/D25 以 15:00 为 first-wins 收盘冻结边界，15:00 任务同时执行收盘行情刷新和统一冻结调度。
- 全部消费者：研究 trace typed decode、入口模块加载契约、recommendation schedule/cadence、freeze coordinator、read-only queries、source input model budget、scheduler runtime 及冻结测试。
- 影响边界：冻结时间和收盘阶段可观察行为、模型评分时间预算、研究校验模块路径；Long、训练 14:50 点时研究、事件 schema 和活动数据不变。
- 排除边界：不改变 DeepSeek 14:48 截止、14:49:50 最终候选报价、训练 point-in-time 锚点或 close_fallback 数据格式；不纳入用户前端改动。
- 退出条件：无训练 infra JSON 校验 owner；15:00 前不正式冻结，15:00 收盘任务触发统一冻结，迟到结果 first-wins；定向冻结、输入运行时、bootstrap 和入口契约通过。
- 实际改动：移动 `trace_validation.py` 至 `infra/serialization/research_trace_validation.py`；调度/冻结协调器/只读查询/输入模型预算统一使用 15:00；15:00 schedule point 合并收盘报价与冻结任务；同步冻结测试、入口契约和权威目录/设计文档。
- 验证：冻结、输入运行时和 bootstrap 定向测试通过；Ruff、mypy、compileall、`git diff --check` 通过。
- 未验证项：未运行全量测试、真实收盘供应商、真实服务五时段或活动数据；Web 前端用户已有修改未纳入本批。
- Review：以 `797b534f` 为基线复核 14:50/15:00 消费者、冻结 first-wins、收盘任务、JSON owner、旧路径和暂存边界；确认没有第二 JSON 校验 owner或重复冻结实现。
- 下一阶段边界：继续拆分 research trace JSON projection/typed decode 编排；不回退 15:00 收盘冻结合同。

## 阶段：22 - 拆分 research trace JSON 边界校验

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`c0ab02d5`；保留用户已有 Web/CSS/JS 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：将研究追踪 JSON 输入边界的对象、列表、标量和 pair 校验从归档实现中拆出，保持事件/审计 projection、typed decode 和持久化格式不变。
- 当前 owner：`research_trace_archive.py` 同时拥有研究归档、JSON projection、typed decode 和通用字段校验。
- 目标 owner：`trace_validation.py` 唯一拥有 JSON 边界校验 primitive；`research_trace_archive.py` 保留事件与审计语义、projection 和 typed decode 编排。
- 全部消费者：研究追踪事件读取、审计读取、损坏 payload 校验、入口模块加载契约和研究归档组件测试。
- 影响边界：研究追踪内部校验 owner 与入口包加载白名单；不改变字段白名单、schema 版本、错误分类或活动数据。
- 排除边界：不改变事件/审计 JSON schema、不迁移 projection DTO、不拆分 outcome archive。
- 退出条件：字段校验 primitive 唯一 owner；归档无重复校验实现；研究归档和入口契约门禁通过。
- 实际改动：新增 `training/infra/research/trace_validation.py`，迁移对象/列表/标量/pair 校验函数；归档 typed decode 改为显式调用校验 owner；入口契约登记新模块。
- 验证：研究归档、点时人口和入口契约定向测试通过；Ruff、mypy、compileall、`git diff --check` 通过。
- 未验证项：未运行全量测试、真实研究数据迁移或活动运行库；JSON projection/typed decode 编排仍待后续拆分。
- Review：以 `c0ab02d5` 为基线复核校验消费者、字段错误行为、重复实现、兼容导出和暂存边界；用户 Web/CSS/JS 与 `docs/test.md` 未纳入本批。
- 下一阶段边界：继续拆分 research trace JSON projection 与 typed decode 编排；不回改本阶段 validation owner。

## 阶段：21 - 拆分 research trace payload codec

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`96416ab7`；保留用户已有 Web/CSS/JS 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：将研究追踪 payload 的压缩、解压上限和 SHA-256 校验从归档实现中拆出，保持事件 JSON projection、typed decode 和公开归档端口不变。
- 当前 owner：`research_trace_archive.py` 同时拥有归档编排、payload codec 和 JSON projection。
- 目标 owner：`trace_codec.py` 唯一拥有压缩/解压/hash primitive；`research_trace_archive.py` 继续拥有事件 schema、JSON projection/typed decode 和归档端口。
- 全部消费者：研究追踪 record/get/list、压缩大审计测试、损坏 payload quarantine、入口模块加载契约。
- 影响边界：研究追踪内部 codec owner、入口包加载白名单；不改变持久化字节格式、容量上限、hash 语义、事件 schema 或活动数据。
- 排除边界：不迁移 JSON projection/typed decode、不改变研究审计 schema、不拆分 outcome archive。
- 退出条件：codec primitive 唯一 owner；归档无重复压缩/hash 实现；定向研究归档和入口契约通过。
- 实际改动：新增 `training/infra/research/trace_codec.py`，迁移压缩前缀、默认解压上限、压缩/解压和 SHA-256；归档记录与校验改为显式调用 codec owner；入口契约登记新模块。
- 验证：研究归档、点时人口和入口契约定向测试通过；Ruff、mypy、compileall 通过。
- 未验证项：未运行全量测试、真实研究数据迁移或活动运行库；JSON projection/typed decode 仍待后续拆分。
- Review：以 `96416ab7` 为基线复核 codec 消费者、持久化格式、重复实现、兼容导出和暂存边界；用户 Web/CSS/JS 与 `docs/test.md` 未纳入本批。
- 下一阶段边界：继续拆分 research trace JSON projection/typed decode 与校验读取职责；不回改本阶段 codec owner。

## 阶段：20 - 拆分 research trace SQLite 存储生命周期

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`139634f2`；保留用户已有 Web/CSS/JS 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：复核 `training/infra/research/research_trace_archive.py` 的 SQLite 生命周期、分区定位、连接管理、schema 和 quarantine 存储职责，建立单一存储 primitive owner。
- 当前 owner：`research_trace_archive.py` 同时拥有归档端口、SQLite schema/连接/分区生命周期、容量摘要和 quarantine 写入，文件 1062 行。
- 目标 owner：`trace_storage.py` 拥有 SQLite schema、连接上下文、数据库初始化、分区日期解析、摘要 DTO 和 quarantine 行写入；`research_trace_archive.py` 保留 `SQLiteResearchTraceArchive`、归档协议、codec/projection 和研究点时查询。
- 全部消费者：bootstrap、训练命令、研究追踪组件测试、历史点时人口契约和入口模块加载边界。
- 影响边界：研究追踪 SQLite 内部 owner 与入口包加载白名单；不改变事件 schema、压缩/hash 校验、归档容量、点时筛选或活动数据。
- 排除边界：不迁移 codec/projection、不改变研究事件格式、不拆分 outcome archive、不触碰运行时数据库。
- 退出条件：SQLite 存储 primitive 有唯一 owner；`SQLiteResearchTraceArchive` 仍是唯一归档实现；无兼容导出、双 schema 或双 quarantine；定向研究归档和入口契约通过。
- 实际改动：新增 `training/infra/research/trace_storage.py`，迁移 schema、连接、初始化、分区日期、摘要 DTO 和 quarantine 行写入；归档类通过显式存储 owner 调用；入口模块加载契约登记新 owner。
- 验证：研究归档组件、点时人口、入口契约定向测试 `76 passed`；Ruff、mypy、compileall、`git diff --check` 通过。
- 未验证项：未运行全量测试、真实研究数据迁移、活动运行库或真实服务；codec/projection 仍待后续阶段拆分。
- Review：以 `139634f2` 为基线复核完整 diff、SQLite schema/连接/分区/校验消费者、旧定义、重复 owner、兼容导出和暂存边界；确认用户 Web/CSS/JS 与 `docs/test.md` 未纳入本批，未发现已知问题。
- 下一阶段边界：继续拆分 research trace codec/projection 与校验/quarantine 读取职责；不回改本阶段 SQLite storage owner。

## 阶段：19 - 拆分 source router 的输入组装、质量降级和请求身份职责

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`d6934e2e`；保留用户已有 Web/CSS 修改和未跟踪文件 `docs/test.md`，均不纳入本批。
- 范围：复核 `recommendation/application/pipeline/data_source/source_router.py` 的模块化职责，将输入组装、输入身份/版本和质量降级/失败摘要从行情适配器中拆出；保持 `MarketDataAdapter` 为唯一应用端口实现。
- 当前 owner：`source_router.py` 同时拥有刷新编排、输入组装、版本身份、质量降级、请求截止时间和阶段输出，文件 1165 行。
- 目标 owner：`input_assembly.py` 拥有报价 overlay、观察时间、任务截止时间和模型输入上下文；`input_identity.py` 拥有输入/报价版本及稳定摘要；`source_quality.py` 拥有降级判定、失败代码和数据源阶段输出；`source_router.py` 只拥有适配器编排与输入 DTO。
- 全部消费者：推荐调度刷新、评分输入构建、报价 overlay、数据源阶段观测、架构/功能包边界和输入运行时测试。
- 影响边界：source-router 内部模块路径和私有职责边界；不改变数据源协议、评分、冻结、API/Web、持久化或活动数据。
- 排除边界：不迁移 source archive、不拆分 research archive、不新增兼容导出、不改变 3.1 外层两层目录结构。
- 退出条件：三类纯职责有唯一 owner；`MarketDataAdapter` 继续是唯一 `DataRefreshPort`/`DecisionBuilderPort` 实现；旧函数定义和重复阶段输出清零；定向架构、输入运行时和候选规划门禁通过。
- 实际改动：新增 `data_source/input_assembly.py`、`input_identity.py`、`source_quality.py`；从 `source_router.py` 移除 18 个纯函数并改为显式职责导入；更新输入运行时测试从新 owner 导入模型上下文；未改变适配器流程和公开端口。
- 验证：输入运行时、推荐模块边界、架构、功能包边界、候选规划、历史评分路径和 bootstrap 定向测试通过；source-router 及新增模块 Ruff 通过；`compileall -q src/trader tests`、`git diff --check` 通过。
- 未验证项：未运行全量测试、真实行情供应商、真实服务五时段或活动数据；未拆分训练 research archive。
- Review：以 `d6934e2e` 为基线复核完整阶段 diff、所有 source-router 消费者、旧函数定义、重复 owner、兼容导出和 3.1 路径；确认用户 Web/CSS 与 `docs/test.md` 未进入暂存范围，未发现已知问题。
- 下一阶段边界：继续拆分 `training/infra/research/research_trace_archive.py` 的 SQLite 生命周期、codec/projection 与校验/quarantine；不回改本阶段 source-router owner。

## 阶段：18 - 拆分推荐运行时状态 owner

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`27c3697a`；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：从 `scheduler_runtime.py` 提取运行时状态 DTO，明确 scheduler 执行器与状态投影的模块边界，并复核 source router/archive 的后续拆分边界。
- 当前 owner：`scheduler_runtime.py` 同时拥有调度执行、运行时状态 DTO 和状态默认值，文件 1188 行。
- 目标 owner：`runtime/status.py` 拥有不可变运行时状态 DTO；scheduler 只拥有资源编排和调度行为。
- 全部消费者：bootstrap/status projection、调度集成测试、运行时契约和诊断脚本。
- 影响边界：scheduler runtime 类型定义与 import 边界；不改变调度执行行为。
- 排除边界：不改变调度时序、冻结、DeepSeek 预算、行情或持久化行为；source router/archive 本批只完成边界复核，不做半成品迁移。
- 退出条件：`scheduler_runtime.py` 不再定义状态 DTO；`runtime/status.py` 成为唯一状态 owner；定向 runtime 门禁通过。
- 实际改动：将 `TradingCalendarRuntimeStatus` 与 `SchedulerRuntimeStatus` 迁入 `recommendation/application/runtime/status.py`，同步 scheduler import，保留运行时行为不变。
- 验证：scheduler integration、bootstrap contract、architecture 定向测试 `79 passed`；Ruff、compileall、`git diff --check` 通过。
- 未验证项：source router 和 research archive 尚未拆分；未运行全量测试或真实运行时五时段验收。
- Review：确认状态 DTO 无重复定义、scheduler 仍是唯一执行 owner，未引入兼容转发或改变公开 schema。
- 下一阶段边界：阶段 19 - 拆分 source router 的输入组装、质量降级和请求身份职责。

## 阶段：17 - 收敛 HTTP/Web 交付模块 owner

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`372b7a36`；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：按 3.1 目标树收敛 `http_api` 的 routes/handlers/response 物理 owner，并建立 `web/pages`、`web/widgets` 的展示边界，保持现有 HTTP/Web 行为。
- 当前 owner：`http_api/routes/page_routes.py` 混合路由注册、endpoint handler、状态投影和响应辅助；`web/` 缺少 pages/widgets owner。
- 目标 owner：routes 只注册路由；handlers 持有请求处理；response 持有白名单投影；web pages/widgets 持有页面展示组合，不注册 Flask 路由。
- 全部消费者：`web/app.py`、性能入口、HTTP/Web 契约、状态诊断和决策流测试。
- 影响边界：HTTP 文件路径、页面上下文 owner、架构/包边界契约；URL、schema 和运行时行为保持不变。
- 排除边界：不改变 URL、HTTP/SSE schema、推荐业务用例、create_app 副作用或活动数据。
- 退出条件：`page_routes.py` 仅保留统一注册；handlers、schemas、response、pages、widgets 目录存在且无反向业务依赖；定向 Web/API 门禁通过。
- 实际改动：将原 `page_routes.py` 实现迁入 `http_api/handlers/product_handler.py`；将决策序列化迁入 `http_api/response/decision_projection.py`；新增 status/decision/event handler、status/decision schema、status projection、`web/pages/dashboard.py` 和三个只读 widgets；更新性能入口和测试契约。
- 验证：Web/API、架构、功能边界和 Web 健康定向测试 `86 passed`；Ruff、compileall、`git diff --check` 通过；`page_routes.py` 降至 16 行且仅负责注册。
- 未验证项：未运行全量测试；未执行真实浏览器三分辨率验收；未改变活动数据。
- Review：确认 `web/` 未新增路由、Flask handler、业务查询或序列化；HTTP 仍只通过推荐只读服务边界工作，无第二路由注册入口。
- 下一阶段边界：阶段 18 - 拆分 recommendation runtime 大模块并复核调度、source router、archive 的单文件职责上限。

## 阶段：16 - 收敛推荐行情基础设施 owner

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`d6934e2e`；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：拆分顶层 `infra/market_data/service`，将推荐运行时 service/cache/loader/health/coordinator 迁入 `recommendation/infra/market_data`，保留供应商共享的 observations/router primitive。
- 当前 owner：顶层 `infra/market_data/service` 混合持有推荐行情编排、业务缓存、研究加载和供应商共享解析。
- 目标 owner：`recommendation/infra/market_data` 唯一拥有推荐行情 service；顶层 `infra/market_data` 只保留跨业务供应商和共享 primitive。
- 全部消费者：bootstrap、推荐持久化初始化、性能入口、行情 provider/normalization/history/reference、市场组件测试、架构与功能包边界契约。
- 影响边界：行情 service/cache/loader/health/coordinator 的包路径和推荐 infra owner；不改变公开行情协议。
- 排除边界：不改变行情协议、评分、冻结、API/Web 行为或活动数据；不迁移训练和下载业务。
- 退出条件：顶层 `infra/market_data/service` 删除；推荐 service 全部位于 `recommendation/infra/market_data`；共享 observations/router primitive 位于顶层 market_data；旧导入和契约通过。
- 实际改动：迁移推荐 gateway、cache、loader、health、coordinator、reference codec 到 `recommendation/infra/market_data`；将共享 `observations.py`、`router.py` 放入顶层 `infra/market_data`；同步 bootstrap、provider、history、normalization、测试和架构文档，删除旧 service 目录。
- 验证：架构/功能包边界/市场服务/行情路由定向测试 `65 passed`；Ruff、compileall、`git diff --check` 通过；旧 service import 扫描为空。
- 未验证项：未运行全量测试；未连接真实行情供应商；未拆分 HTTP/Web 大模块。
- Review：复核 service 全部文件消费者，确认仅 observations/router 保留为共享 primitive，其余均已切换到推荐 owner；未删除活动实现或兼容转发。
- 下一阶段边界：阶段 17 - 拆分 `http_api/routes/page_routes.py` 的 handler/schema/response 职责，并建立 `web/pages`、`web/widgets` 展示 owner。

## 阶段：15 - 按 3.1 目标树收敛训练评价层

- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`dd866d30`；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：将现行 `training/domain/evaluation` 与 `training/application` 原子迁入 3.1 要求的 `training/domain/evaluation` 与 `training/application`，同步全部消费者、测试和包边界。
- 当前 owner：`training/domain/evaluation` 与 `training/application`。
- 目标 owner：`training/domain/evaluation` 拥有评价领域对象；`training/application` 拥有评价用例，并与训练主链 application 共享唯一应用层。
- 全部消费者：训练评价源码、研究基础设施、下载历史消费者、评价/研究测试、功能包边界契约和 `docs/02_工程设计.md`。
- 影响边界：训练评价源码、脚本、测试 import/path、包边界和工程说明；不改变评价行为或活动数据。
- 排除边界：不迁移顶层 `infra/market_data/service`，不拆分 `http_api/page_routes.py`，不改变评价行为或活动数据。
- 退出条件：源码、测试和工程说明统一使用 3.1 规定的两层 owner，旧 `training/evaluation/` 外层目录和活动引用清零。
- 实际改动：将评价领域文件迁入 `training/domain/evaluation/`，评价应用文件迁入 `training/application/`，同步全部源码、脚本、测试和文档引用，删除旧 `training/evaluation/` 外层目录。
- 验证：架构/功能包边界/结果评价契约 48 passed；研究、回放、留出、结果结算和研究归档定向测试 292 passed；Ruff、compileall、`git diff --check` 通过；旧路径扫描为空。
- 未验证项：尚未迁移顶层 `infra/market_data/service` 中的推荐专属实现；尚未拆分 HTTP/Web 大模块；Git 索引当前不可写，尚未创建提交或推送。
- Review：复核 3.1、3.2、3.5、阶段 4 台账和全部评价路径引用，确认无旧外层目录、兼容转发或重复 owner。提交前暂存区核对待 Git 索引权限恢复后完成。
- 下一阶段边界：阶段 16 - 收敛推荐行情基础设施 owner；将推荐专属 market-data service/cache/loader 从顶层 `infra/market_data` 迁入 `recommendation/infra/market_data`，保留共享供应商与底层历史 primitive。

## 当前阶段

- 阶段：14 - 清理无用旧目录并复核目标树子模块
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`f18947e23b24a518e3d18fee8462e7bfca03f487`，阶段 13 已推送；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：逐层复核《项目重构详细.md》第 3.1 节目标树，删除无活动源码、无资源和无 owner 的旧目录，确认仍有行为消费者的推荐模型加载与训练工件目录继续保留。
- 当前 owner：旧根 `application/`、`domain/`、`resources/`、`web/api/` 及顶层 `infra/{deepseek,persistence,pipeline,research}` 仅残留缓存目录；`recommendation/infra/scoring` 和 `training/infra/artifacts` 仍分别拥有生产 profile factory 与训练工件实现。
- 目标 owner：活动源码只保留第 3.1 节允许的业务、交付和共享 primitive 目录；删除无 owner 的残留目录，不建立兼容目录或空占位包。
- 全部消费者：架构/功能包边界契约、推荐 bootstrap/status projection、模型加载测试、训练工件测试和包发现。
- 必删路径：`src/trader/application/`、`src/trader/domain/`、`src/trader/resources/`、`src/trader/web/api/`、`src/trader/infra/deepseek/`、`src/trader/infra/persistence/`、`src/trader/infra/pipeline/`、`src/trader/infra/research/`；删除前已确认无受跟踪文件和活动导入。
- 影响边界：源码目录树、旧路径契约、包发现和架构扫描；不改变业务实现和运行数据。
- 排除边界：保留有真实消费者的 `recommendation/infra/scoring`、`recommendation/infra/status_projection`、`training/infra/artifacts` 及全部目标业务目录；不迁移测试物理目录。
- 退出条件：残留目录完全删除，目标树契约与旧导入扫描通过，受影响测试和静态门禁通过后提交推送。
- 实际改动：删除 `src/trader/application/`、`src/trader/domain/`、`src/trader/resources/`、`src/trader/web/api/` 以及顶层 `infra/{deepseek,persistence,pipeline,research}/` 的缓存残留目录；新增契约确保这些旧目录整体不存在。复核并保留仍有活动消费者的推荐 DeepSeek、推荐持久化、推荐 profile/status projection 和训练工件 owner。
- 验证：架构、功能包边界、打包布局和 bootstrap 契约 `61 passed`；旧目录物理扫描为空；`git diff --check` 通过。未发现活动源码对被删除路径的导入。
- 未验证项：未运行全量测试和完整快测（本批仅影响目录存在性和契约）；未迁移或拆分测试物理目录，不触碰活动运行数据。
- Review：以 `f18947e23b24a518e3d18fee8462e7bfca03f487` 为基线复核第 3.1 目录树、所有 tracked 文件、源码导入和缓存残留；确认被删目录无 tracked 文件、无活动 owner，未发现误删行为实现。用户未跟踪的 `docs/test.md` 未纳入本批。
- 下一阶段边界：继续复核目标树中仍有实现的子目录模块化职责和测试物理归属；不删除有消费者的推荐/训练 owner，不恢复旧兼容目录。

- 阶段：13 - 目标目录收敛与测试分层优化
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`0aaef0c8c3b6339dda123e5bea05a4fe2c196f7a`，阶段 12 已推送；保留用户未跟踪文件 `docs/test.md`，不纳入本批。
- 范围：按《项目重构详细.md》第 3.1 节复核活动目录，移除仍在使用但职责不符合目标树的共享目录；按 `docs/test.md` 第一阶段建立快速默认测试与历史、运行时、供应商专项测试分层。
- 当前 owner：共享工件序列化位于 `infra/serialization`，单头 profile 组合器位于 `training/infra/profile`；测试按通用与专项 marker 分层。
- 目标 owner：保持共享序列化与单头组合器的目标树归属；测试默认集合排除历史迁移、运行时并发和供应商时序测试，并提供显式专项命令。
- 全部消费者：下载归档 codec、训练工件与研究报告、推荐持久化 codec、测试契约、Makefile、pytest collection 和阶段 12 快速门禁。
- 必删路径：`src/trader/infra/artifacts/`、`src/trader/infra/scoring/` 的活动 owner 与残留空目录；不删除仍有行为消费者的业务源码，不触碰活动数据。
- 影响边界：共享序列化导入、训练 profile 工件、pytest marker/默认集合、测试命令契约和目标树契约。
- 排除边界：不改变业务评分、冻结、API/SSE、运行时行为；不迁移本批未覆盖的历史/研究测试物理目录，不删除测试用例。
- 退出条件：目标目录契约、旧导入扫描、`test-fast` 与四类专项命令可验证；受影响 Ruff、类型和定向测试通过；台账记录实际结果后提交并推送。
- 实际改动：将跨业务规范 JSON、字段校验和不可变发布 primitive 从旧 `infra/artifacts` 迁入 `infra/serialization`，将单头 profile 组合器迁入 `training/infra/profile`，同步下载、训练、推荐、脚本和测试消费者；清理对应旧目录缓存；在 pytest 中为历史、迁移、运行时和供应商测试建立专用 marker，并将 `make test` 收敛为 `test-fast`，新增 `test-history`、`test-runtime`、`test-suppliers` 专项目标；补充目标目录和测试命令契约。
- 验证：目标树/工件发布/归档重打包/测试命令契约定向测试 `20 passed`；`make test-fast` 通过；三类专项 marker collection 成功（history/migration、runtime、supplier）；`make type-check`（451 个源文件）通过；受影响 Ruff 与 `git diff --check` 通过；旧活动导入扫描为空。
- 未验证项：本批未运行完整 `test-history`、`test-runtime`、`test-suppliers` 执行集合，仅验证 marker collection；未迁移 docs/test.md 后续阶段列出的测试物理目录、固定 sleep 替换、大文件拆分或 Today/V1 历史测试清理；未触碰活动运行数据。
- Review：以 `0aaef0c8c3b6339dda123e5bea05a4fe2c196f7a` 为基线复核目标树、所有导入消费者、旧路径、测试 marker、Makefile 分层和用户未跟踪文件边界；修复脚本遗漏导入、pytest.ini marker 注册和一项供应商时序用例归类后，未发现已知问题。
- 下一阶段边界：继续按 `docs/test.md` 第二阶段优化真实等待和并发测试，再按业务 owner 迁移测试物理目录；不删除行为测试、不放宽契约、不改变业务运行逻辑。

- 阶段：12 - 完整验收、发布候选与人工切换
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`dc882e3c860bad2af50fc10ddf4e43ffb4a39c0c`，开始时本地与上游一致；保留用户未跟踪文件 `docs/test.md`，不纳入本批。系统与项目虚拟环境为 Python 3.14.4。
- 范围：对阶段 0–11 的完整结果执行发布级全量门禁和蓝图专项验收，从仓库外安装 wheel，验证桌面三分辨率、统一诊断与隔离数据副本迁移/回退，并收敛最终文档和发布候选证据。
- 当前 owner：阶段 0–11 已完成最终目录与唯一 owner 切换；本阶段只验证 `download`、`training`、`recommendation`、`http_api`、`web`、共享 `infra`、唯一组合根、公开入口、包资源和隔离迁移工具的发布一致性。
- 目标 owner：保持阶段 11 已确认的最终 owner 不变；任何验收发现必须回到现有 owner 修复，不新增兼容层、第二组合根、重复 API、双读写或发布时 fallback。
- 全部消费者：公开 CLI/server、bootstrap、推荐运行调度与冻结、V2/V3 加载、DeepSeek 预算、SQLite 恢复、API/SSE/Web、浏览器资源、诊断框架、wheel 安装和仓库外数据迁移/回退工具。
- 必删路径：本阶段不预设新增删除；若门禁发现阶段 11 漏留旧 owner、旧资源、生成物或兼容路径，必须在原 owner 边界内清除并证明全部消费者已切换。
- 影响边界：最终架构与依赖、`create_app()` 副作用、固定融合、预算并发、原子模型加载、分组选择、14 层快照、SSE、冻结/恢复、打包、桌面展示、真实新进程只读交付和隔离数据迁移。
- 排除边界：不触碰活动 `.runtime`、`data/history` 或 `data/train`；不消耗真实 DeepSeek 配额；不自动创建 PR、合并、tag、切换真实数据或发布。
- 退出条件：全量门禁和适用专项验收通过，真实供应商/时间窗等不可获得证据被逐项如实登记，完整 Review 零已知发现；单一阶段提交推送至 `origin/Afuture` 并确认上游包含后，只形成待人工授权的发布候选。
- 实际改动：移除 Makefile 对已删除评分打包脚本的引用；同步阶段 11 后遗留的 data-plane、decision event、scheduler、版本命名、工件发布和点时数据投影契约；收敛复杂度质量基线；修复桌面验收对动态 Long 行情、质量漏斗和输入质量尾部文本的等待与断言；保留现有 owner、组合根、API/SSE、冻结、预算、模型加载、迁移和回退边界不变。Ruff 对受影响源码与测试执行了规范化格式调整。
- 验证：`make format-check`、`make lint`、`make type-check`（mypy 451 个源文件）、`make test`（全量非 slow，最终退出码 0）、`make package`、`scripts/verify_wheel_install.py --dist-dir dist`（8 项资源验证）通过；阶段 12 冻结/SSE/慢客户端/预算并发/模型原子加载/迁移/恢复/决策记录/调度专项通过；`scripts/diagnose_runtime.py --profile performance --output -` 通过；Chrome 无头桌面验收在 1280×720、1440×900、1920×1080 均通过，无横向溢出或浏览器错误，报告在仓库外 `/tmp/trader-stage12-desktop-final4/report.json`；`git diff --check` 通过。
- 未验证项：未连接真实行情供应商或 DeepSeek，未执行真实服务五时段/重启和真实活动数据切换，未完成隔离副本长时迁移演练；Firefox 因系统仅有 snap 占位入口且无 geckodriver 未运行，Edge 未运行；运行时诊断未在真实服务进程上执行。阶段 12 未触碰活动 `.runtime`、`data/history`、`data/train`，未创建 PR、合并、tag 或发布。
- Review：以 `dc882e3c860bad2af50fc10ddf4e43ffb4a39c0c` 为基线复核完整 diff、阶段 11 旧路径/旧 owner、兼容转发、重复状态源、隐藏 fallback、TODO、生成物、文件大小、测试断言和暂存边界；修复上述门禁发现后，未发现已知问题。用户未跟踪的 `docs/test.md` 明确排除在本批之外。
- 下一阶段边界：阶段 12 已完成；本分支仅形成待人工授权的发布候选。不得自动合并、创建 PR、打 tag、切换真实数据或发布，后续动作须由用户另行明确授权。

- 阶段：11 - 删除旧活动树与执行全局命名清理
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`92957cb8dcf78c8b3b9c6883f8994bb6d21435cf`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境为 Python 3.14.4。
- 范围：将根 `application/` 的 Long owner 收回 recommendation，将根 `infra/` 中确认属于单一业务的 DeepSeek 证据渲染与训练评价持久化归入对应业务，删除旧活动树、空旧路径与过期测试布局，并把架构和专业命名合同收敛到最终目录图。
- 当前 owner：根 `application/` 仍持有 Long 分组类型和运行时；根 `infra/market_data` 同时持有共享供应商 primitive 与推荐业务缓存/标准化/服务；根 `infra/persistence` 持有训练评价 outcome/research trace；根 `domain/`、旧业务子目录和 `web/api/` 仅剩空路径或缓存；部分测试物理目录仍沿用旧 `application/domain` 布局。
- 目标 owner：Long 归入 `recommendation/domain|application`；DeepSeek 候选证据渲染归入 `recommendation/infra/deepseek`；训练评价持久化归入 `training/infra/research`；阶段 8 已裁决为跨业务使用的供应商、历史、标准化与执行 primitive 继续由顶层 `infra` 持有；旧根级业务 owner 清零。
- 全部消费者：bootstrap、推荐 source router、settings、download/training 只读消费者、HTTP/Web 状态投影、运行诊断、组件/单元/契约测试、包发现和工程说明。
- 必删路径：根 `src/trader/application/`、`src/trader/domain/`、`src/trader/web/api/`、空旧业务目录、被迁移的业务型根 `infra` 路径、旧测试夹具/物理布局及复数空 `recommendation/infra/scoring/profiles/`；不建立兼容转发。
- 影响边界：最终目录与导入图、Long 当前快照、DeepSeek 证据渲染、训练评价持久化、包发现、架构与命名门禁。
- 排除边界：不改变评分、风险、融合、冻结、API/Web schema、配置或持久化格式；不触碰活动 `.runtime`、`data/history` 或 `data/train`；不提前执行阶段 12 全量发布验收。
- 退出条件：活动树只剩目标 owner，旧路径/旧导入/重复 owner/兼容层为零；最终架构、专业命名、包发现、行数、空聚合、重复定义与 TODO 门禁通过。
- 实际改动：将 Long 分组类型与运行时迁入 `recommendation/domain/publication` 和 `recommendation/application`；将候选证据渲染迁入 `recommendation/infra/deepseek`；将 outcome evidence 与 research trace SQLite 适配器迁入 `training/infra/research`；删除根 `application`、根 `domain`、根 `infra/persistence` 与复数空 scoring profiles 的活动包文件；将四个 V3 测试迁入 training/profile 测试布局；消除诊断 DTO 和离线 research port 的重复公共名称；同步工程设计、入口授权、最终包边界和旧路径禁止合同。
- 验证：阶段 11 核心架构/功能包/专业命名/稳定命名/Long/outcome/point-in-time/DeepSeek/存档定向测试 `121 passed`；入口、打包、下载训练边界、诊断、测试命令、V3 训练与市场服务定向测试 `120 passed`；`.venv/bin/python3 -m mypy src/trader`（451 files）、阶段范围 Ruff、compileall 和 `git diff --check` 通过；旧活动导入扫描为空，活动源码最大 1185 行，未发现 TODO/FIXME/XXX。
- 未验证项：未连接真实行情供应商或 DeepSeek，未执行真实服务五时段运行/重启、稳定版浏览器三分辨率验收、隔离副本长时迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `92957cb8dcf78c8b3b9c6883f8994bb6d21435cf` 为基线复核完整 diff、移动/删除路径、全部消费者、旧导入、重复公共类型、兼容层、隐藏 fallback、TODO、生成物、文件大小和暂存边界；修复入口授权清单、诊断 DTO 重名、离线端口重名和 V3 测试旧布局后，未发现已知问题，未提前执行阶段 12。
- 下一阶段边界：阶段 12 - 对阶段 0–11 的完整 diff 执行发布级验收并形成候选；不得自动合并、打 tag、切换真实数据或发布，任何真实切换仍需用户另行明确授权。

- 阶段：10 - 收敛组合根、公开入口、包资源与数据布局
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`5406eac08eebdfb620291e4cbd9ffb70cc6aacdf`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境为 Python 3.14.4。
- 范围：收回四个根级 bootstrap 辅助 owner，迁移历史/训练/H1 业务入口，补齐 wheel 资源声明，并提供仓库外隔离副本的数据布局校验、原子发布与回退工具。
- 当前 owner：`bootstrap.py` 仍依赖 `bootstrap_*.py` 状态/策略/时钟/数据初始化辅助；根 `entrypoints/` 仍拥有下载历史投影、训练进度和 H1 审计；`pyproject.toml` 只声明 Web/下载模板资源；无统一隔离数据迁移工具。
- 目标 owner：共享时钟归入 `infra/clock`，策略归入 `infra/settings`，数据平面初始化与运行状态归入 `recommendation/infra`；历史与训练/H1 入口分别归入 `download/entrypoints`、`training/entrypoints`；wheel 明确声明 `http_api` 类型资源；迁移工具位于 `scripts/`。
- 全部消费者：bootstrap、性能入口、CLI、训练/下载命令、H1/历史投影测试、wheel 验证和数据迁移专项测试。
- 必删路径：`src/trader/bootstrap_clock.py`、`bootstrap_data_plane.py`、`bootstrap_policy.py`、`bootstrap_status.py` 及根入口中已迁出的三个业务入口；不建立兼容转发。
- 影响边界：组合根导入图、公开 CLI/server 延迟装配、wheel 资源发现、历史/训练入口路径、隔离数据复制与回退。
- 排除边界：不删除阶段 11 才清理的旧业务树，不触碰活动 `.runtime`、`data/history` 或 `data/train`，不改变推荐评分、冻结、API/Web 行为。
- 退出条件：`bootstrap.py` 不再导入根级辅助模块，业务入口归属正确，wheel 资源声明和隔离迁移工具有定向证据；活动数据未被写入。
- 实际改动：将四个 bootstrap 辅助模块分别迁入共享时钟/策略与推荐数据初始化/状态投影 owner；将历史自动化、Tomorrow 训练进度和 H1 审计入口迁入 download/training；CLI 与诊断脚本改为调用新 owner；声明 `trader.http_api/py.typed`；新增仅允许仓库外路径的 verify/build/rollback 隔离数据迁移工具及专项测试；同步阶段 10 相关边界、打包、入口和诊断合同。
- 验证：阶段 10 定向测试 `195 passed, 1 skipped`；架构/功能包/蓝图/诊断/测试命令合同 `50 passed`；`.venv/bin/python3 -m mypy src/trader`（455 files）通过；`compileall`、阶段 10 范围 Ruff 和 `git diff --check` 通过；`make package` 首次因隔离环境网络限制失败，获审批重试成功；`scripts/verify_wheel_install.py --dist-dir dist` 在仓库外安装验证通过；迁移工具专项测试通过且未写入活动数据。
- 未验证项：未执行真实稳定版 Chrome/Edge/Firefox 三分辨率验收、真实服务五时段运行、真实行情或 DeepSeek、隔离副本长时迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `5406eac08eebdfb620291e4cbd9ffb70cc6aacdf` 为基线复核完整 diff、移动路径、全部消费者、旧导入、重复组合根、application 反向依赖、兼容层、隐藏 fallback、TODO、生成物、包资源和文件大小；修复下载状态类型混用及诊断脚本旧 owner 导入后，未发现已知问题，未提前实施阶段 11。
- 下一阶段边界：阶段 11 - 删除旧活动树并完成全局命名清理；只删除阶段 10 之后仍存的退役活动 owner 并完成命名/引用清理，不回改阶段 10 的组合根、入口、包资源或迁移工具行为。

- 阶段：9 - 拆出 `http_api/` 并保持 Web 主页行为
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`456229115585122040593676dc4ccc5100cfa0b8`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境为 Python 3.14.4。
- 范围：将 `web/api/` 的路由、请求校验、schema、SSE 和白名单投影迁入顶层 `http_api/`；页面路由由 `http_api/routes/page_routes.py` 唯一注册，`web/` 只保留页面、模板、组件和静态资源。
- 当前 owner：`http_api/` 持有 HTTP handler、请求解析、响应序列化、SSE、只读服务协议和页面路由注册；`web/` 持有 Flask app factory、页面模板与静态资源。
- 目标 owner：`http_api/` 持有只读 HTTP/API 边界及页面路由注册；`web/` 只持有无业务语义的页面展示、模板和静态资源；`recommendation/application/` 继续是唯一只读数据用例 owner。
- 全部消费者：`web/app.py`、bootstrap、performance/deepseek 入口、决策流单元测试、Web/API 合同与功能包边界测试、历史评分路径断言、工程蓝图断言及桌面页面静态资源。
- 必删路径：`src/trader/web/api/` 全部活动 Python owner、旧 `trader.web.api` 导入和重复路由路径；不建立兼容转发。
- 影响边界：API current/history/dates/status/events、ETag、SSE 游标恢复/完整替换、主页 Tomorrow/D25/Long、正式/观察池展示、状态与静态资源响应。
- 排除边界：不改变推荐业务查询、评分/冻结/持久化、运行时、组合根、包资源或活动数据；不提前实施阶段 10。
- 退出条件：`http_api/` 成为 HTTP 唯一 owner，`web/api/` 删除且旧导入清零；HTTP 不访问 infra/供应商/评分/DeepSeek/写状态；现有 Web 合同、架构边界、SSE/ETag 和 app factory 门禁通过。
- 实际改动：将决策序列化、SSE、只读服务协议和全部 Flask handler 原子迁入 `http_api/`，页面/API 路由统一由 `http_api/routes/page_routes.py` 注册；切换 app factory、bootstrap、性能/组件/单元测试消费者；删除 `web/api/` 活动 Python owner；同步 `docs/02_工程设计.md`、功能包边界、架构边界、历史评分路径和蓝图断言。
- 验证：阶段 9 定向 pytest（Web contract、app factory、功能包边界、架构、蓝图实施计划、历史评分路径、decision stream、DeepSeek）`103 passed`；`compileall`、阶段 9 范围 Ruff、`.venv/bin/python3 -m mypy src/trader`（455 files）和 `git diff --check` 通过；`trader.web.api`/`web/api` 活动导入与源码扫描为空，主页/SSE/ETag/Long 页面合同保持通过。
- 未验证项：未执行真实稳定版 Chrome/Edge/Firefox 三分辨率验收、真实服务五时段运行、真实行情或 DeepSeek、隔离数据迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `456229115585122040593676dc4ccc5100cfa0b8` 为基线复核完整 diff、移动/删除路径、全部消费者、旧导入、重复路由、兼容层、HTTP 越界依赖、TODO、生成物和文件大小；未发现已知问题，未提前实施阶段 10 的组合根/入口/包资源迁移。
- 下一阶段边界：阶段 10 - 收敛组合根、入口、包资源和数据迁移工具；只处理 `bootstrap.py`、公开入口、wheel 资源和隔离副本迁移，不回改 `http_api`/Web 行为。

- 阶段：8 - 迁移推荐基础设施、运行调度与模型加载
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`4b49424266a8a0e2f7455af97b447835d7d9ca88`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境均为 Python 3.14.4。
- 范围：将推荐专属行情适配/标准化/缓存、DeepSeek、推荐 SQLite、V2/V3 模型加载与推理迁入 `recommendation/infra`，并将 cadence、latest-wins、资源车道、deadline、停止顺序和状态聚合迁入 `recommendation/application/runtime`。
- 当前 owner：根 `infra/market_data` 同时持有供应商 primitive 与推荐业务适配，根 `infra/deepseek` 持有预算/schema/证据/复核，根 `infra/persistence` 持有推荐运行库，根 `infra/scoring` 持有在线档位加载，根 `application/runtime` 持有推荐运行编排。
- 目标 owner：推荐业务适配分别归入 `recommendation/infra/{market_data,normalization,deepseek,persistence,scoring/profile}`；推荐运行编排归入 `recommendation/application/runtime`；顶层 `infra` 只保留跨业务 primitive、底层供应商客户端、参考数据与通用执行支撑。
- 全部消费者：bootstrap/status/entrypoints、推荐 14 层 application、Long、HTTP/Web 只读投影、下载供应商桥、训练评价只读消费者、诊断/性能脚本，以及行情、DeepSeek、持久化、模型、调度、资源/关停相关测试。
- 必删路径：被替代的根 `application/runtime`、`infra/deepseek`、`infra/scoring` 及 `infra/market_data`、`infra/persistence` 中已裁决为推荐专属的活动 Python owner；不建立兼容导出、双加载器、双预算、双 schema、双读写或隐藏客户端。
- 影响边界：行情来源选择与缓存身份、DeepSeek 168 次物理请求和独立预算桶、V2/V3 两头工件原子启动校验、在线推理降级、调度 cadence/latest-wins/deadline、资源启动/停止顺序、推荐 SQLite 与恢复。
- 排除边界：不拆分 `http_api`/Web，不收敛根组合辅助模块/公开入口/包资源，不演练或切换活动数据，不改变评分、风险、融合、冻结、API schema 或页面行为。
- 退出条件：推荐 application 不导入 infra；所选档位全部 head 原子校验且失败关闭，在线单次失败仅该次显式降级；DeepSeek 预算/缓存/single-flight 合同不变；runtime owner 唯一且可有界停止；旧 owner、旧导入、兼容层和重复资源清零。
- 实际改动：将 recommendation runtime、DeepSeek 和在线 V2/V3 profile 加载器迁入 `recommendation/application/runtime` 与 `recommendation/infra/{deepseek,scoring}`；将决策记录、资格账本和 data-plane 迁入 `recommendation/infra/persistence`；将 `DecisionObservation` 收回发布事件 owner，移除 runtime 到 training 的反向依赖；保留顶层 `infra` 的共享 `SingleHeadCombiner`、供应商/历史 support 与研究/结果归档，供下载/训练使用。
- 验证：阶段 8 定向 pytest（架构/包/boot 边界、runtime cadence/latest-wins/resources/supervisor/workers/observer、DeepSeek、V2/V3 profile、data-plane/迁移、决策记录、资格账本、冻结、决策身份、市场历史/研究、scheduler integration）`385 passed`；`compileall`、阶段 8 范围 Ruff、`.venv/bin/python3 -m mypy src/trader`（454 files）和 `git diff --check` 通过；旧 runtime/DeepSeek/推荐持久化/profile loader import 扫描为空，迁移源码最大 1185 行。
- 未验证项：未连接真实行情供应商或 DeepSeek，未执行实际服务五时段运行/重启、真实浏览器验收、隔离数据迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `4b49424266a8a0e2f7455af97b447835d7d9ca88` 为基线完成完整 diff、移动路径、consumer、旧导入、application 反向依赖、重复 owner、兼容层、TODO、生成物和文件大小复核；未发现已知问题，未提前拆分 `http_api`/Web 或组合根。
- 下一阶段边界：阶段 9 - 拆出 `http_api/` 并保持 Web 主页行为；只迁移路由、校验、schema、SSE 和白名单投影，Web 仅保留页面/组件/资源，`create_app()` 保持无副作用。

- 阶段：7 - 迁移第 10–14 层、冻结与发布
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`3ab523241bff4745971c21f43fd8669f6a57aa8b`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境均为 Python 3.14.4。
- 范围：依次建立 `recommendation/application/pipeline/{local_score,risk_review,score_merge,downside_action,final_selection,freeze_publish}` 的唯一 owner，迁移评分、风险复核、固定融合、下行保护/动作门、组内排序/集中度/TopK、冻结与发布，并串联第 10–14 层统一阶段观测。
- 当前 owner：根 `application/recommendation` 持有评分、模型诊断、风险、融合、排序和冻结用例，根 `application/decisions` 持有草稿、当前索引、正式记录接纳、事件、查询、SSE 输入、观察者和报价 overlay 发布。
- 目标 owner：第 10–14 层及冻结发布归入 `recommendation/application/pipeline` 对应子包；纯公式、风险与排序继续仅由 `recommendation/domain` 持有；外部读写只经 `recommendation/application/ports` 注入。
- 全部消费者：第 1–9 层 source router、调度 application、bootstrap/status/performance、推荐行情/DeepSeek/持久化/评分基础设施、Web API/SSE/静态投影、研究审计与持久化、诊断脚本和评分/决策/冻结/调度/Web 相关测试。
- 必删路径：被替代的根 `application/recommendation` 与 `application/decisions` 活动 Python owner；不建立兼容导出、重复索引、重复发布源或冻结 fallback。
- 影响边界：统一证据质量分、模型诊断与成本门、本地风险一次扣除、DeepSeek 结构化复核、固定 68/32 融合、动作终态、分组 TopK/集中度、决策身份、当前/历史投影、14:50 first-wins、15:00 后合法 `close_fallback`、迟到 CAS、重启恢复与 Long 排除。
- 排除边界：不迁移推荐 infra、调度实现、模型加载或资源编排，不拆分 `http_api`/Web，不改变评分公式、阈值、配置、持久化 schema 或活动数据。
- 退出条件：第 10–14 层有唯一顺序、唯一不可变阶段输出和统一聚合观测；档位 × 策略 × 板块独立组不混排不借位；冻结记录与当前发布共享同一决策身份；旧 owner、旧导入、兼容层和重复状态源清零，定向门禁通过。
- 实际改动：将评分、模型诊断、风险复核、固定融合、下行保护/动作门、组内排序/集中度/TopK、冻结、发布、决策查询、事件流和观察者从根 `application/recommendation`、`application/decisions` 原子迁入六个 `recommendation/application/pipeline` 子包；同步 bootstrap、调度、Long、行情/DeepSeek/持久化、Web/SSE、研究审计、性能入口、脚本和测试消费者；补齐工程设计目标目录与阶段边界契约。
- 验证：阶段 7 定向 pytest（评分边界、模型路由、Tomorrow 投影/冻结、决策查询/事件流/统一决策核心、功能包边界、推荐模块边界、D25 合同）`80 passed`；`compileall`、阶段 7 范围 Ruff、`.venv/bin/python3 -m mypy src/trader`（454 files）、`git diff --check` 全部通过；旧 owner `.py` 与旧导入扫描为空，活动阶段源码最大 769 行。
- 未验证项：未连接真实行情供应商或 DeepSeek，未执行真实服务/浏览器验收、运行时重启五时段实测、隔离数据迁移演练和阶段 12 发布级全量门禁；未读取或修改活动运行数据。
- Review：以 `3ab523241bff4745971c21f43fd8669f6a57aa8b` 为基线复核完整 diff、六个新 owner、删除路径、旧导入、兼容层、隐藏 fallback、TODO、生成物和文件大小；未发现重复状态源或阶段 8–9 越界改动。
- 下一阶段边界：阶段 8 - 迁移推荐基础设施、运行调度与模型加载；只处理 recommendation infra、cadence/resource/deadline/shutdown 和 V2/V3 模型加载，不提前拆分 `http_api/` 或 Web。

- 阶段：6 - 迁移推荐端口与第 1–9 层
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 基线：`f6c6855b44b0547cec284bf5c94c6b5610c5f3e3`，开始时本地与上游一致且工作树干净；系统与项目虚拟环境均为 Python 3.14.4。
- 范围：建立 `recommendation/application/ports` 与 `recommendation/application/pipeline` 的第 1–9 层唯一 owner，按数据源、静态采集、静态标准化、一级稳定过滤、动态采集、动态标准化、二级动态过滤、候选生成和质量评估顺序切换现有生产链与观测。
- 当前 owner：推荐端口与第 1–9 层已分别归入 `recommendation/application/ports` 和 `recommendation/application/pipeline`；第 10–14 层、冻结/发布、调度、模型加载和交付边界仍由阶段 7–9 的现有 owner 持有。
- 目标 owner：推荐端口归入 `recommendation/application/ports`；第 1–9 层归入 `recommendation/application/pipeline/{data_source,static_market,static_standardize,static_filter,dynamic_market,dynamic_standardize,dynamic_filter,candidate_pool,quality_check}`；训练评价端口归入 `training/application`。
- 全部消费者：决策/调度应用、Long 运行时、行情/DeepSeek/持久化/评分基础设施、bootstrap 与性能入口、诊断脚本、训练评价只读消费者及相关契约/单元/组件测试均已切换到新 owner；HTTP/SSE/Web 继续通过既有只读应用边界消费。
- 已删路径：`application/market_data`、候选/质量旧 owner、根 `application/ports` 中已完成裁决的活动 Python 文件和根 application cache owner；无兼容导出。
- 影响边界：行情与历史输入、候选和证据质量、流水线状态聚合，以及 API/SSE/Web 的既有只读消费者；覆盖全市场总体、板块分桶、每板 120 上限、数据待补充/刷新待补充/无效、来源失败和最近有效快照降级。
- 排除边界：不迁移第 10–14 层实现、冻结与发布实现、推荐基础设施、运行调度、模型加载、`http_api/` 或 Web 资源；不触碰活动运行数据。
- 退出条件：第 1–9 层具有唯一顺序、不可变阶段输出和统一 `PipelineStageSnapshot` 聚合观测；业务拒绝与数据待补充分开计数；旧 owner、旧导入、兼容层和重复状态源清零，定向门禁通过。
- 实际改动：迁移推荐端口、数据源/静态市场/静态标准化/稳定过滤/动态市场/动态标准化/动态过滤/候选池/质量检查九层及其不可变阶段输出；新增 `PipelineStageSnapshot`、来源健康、业务拒绝和质量证据类型；将训练评价 research/outcome 端口与 JSON 边界收回 `training/application`；切换 bootstrap、决策/运行时、行情/DeepSeek/持久化、诊断脚本、性能入口和测试的全部消费者；删除旧 owner 并同步工程设计与架构/包边界契约。
- 验证：`.venv/bin/python3 -m compileall -q src/trader tests`、阶段 6 影响范围 Ruff、`.venv/bin/python3 -m mypy src/trader` 和阶段 6 定向 pytest 全部通过；定向测试覆盖架构、功能包/推荐模块边界、候选规划、历史评分、评分计划、来源能力、输入运行时、候选规划、推荐边界和 Tomorrow 选择；`git diff --check` 通过；完整变更中活动源码无旧 owner 导入，新增/迁移文件无超过 1200 行者。
- 未验证项：未连接真实行情供应商或 DeepSeek，未读取或修改活动运行数据，未执行真实服务/浏览器验收、隔离数据迁移演练和阶段 12 发布级全量门禁；这些不属于阶段 6 的最小充分门禁。
- Review：以 `f6c6855b44b0547cec284bf5c94c6b5610c5f3e3` 为基线完成完整 diff、删除路径、重复 owner、旧导入、兼容层、隐藏 fallback、TODO 和生成物复核；第 10–14 层及阶段 7–9 排除边界未提前实施，未发现已知问题。
- 下一阶段边界：阶段 7 - 迁移第 10–14 层、冻结与发布；只处理评分、风险、固定融合、动作门、组内排序、TopK、冻结和发布，不提前迁移推荐基础设施、调度、模型加载或 `http_api/`。

- 阶段：5 - 迁移推荐领域并切换策略与档位身份
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 范围：将推荐纯值对象和纯函数原子迁入 `recommendation/domain/{market,candidate,scoring,risk,evidence,selection,publication}`，同步把生产策略收敛为 Tomorrow、D25 与不评分的 Long，评分档位收敛为 V2/V3。
- 当前 owner：`domain/market`、`domain/recommendation`、`domain/review`，Today 调度/评分/冻结/交付消费者，`infra/scoring/profiles/v1`，V2/V3 Today 训练头与 `data/train/*/today`。
- 目标 owner：`recommendation/domain/market|candidate|scoring|risk|evidence|selection|publication`；活动策略身份仅 Tomorrow/D25/Long，活动档位身份仅 V2/V3。
- 全部消费者：根 application 端口与运行时、推荐编排与决策查询、行情/DeepSeek/持久化/评分基础设施、bootstrap 与入口、HTTP/SSE/Web 投影、训练与离线研究只读消费者、配置/包资源/脚本与测试。
- 必删路径：旧 `domain/market`、`domain/recommendation`、`domain/review` 活动 Python owner；Today 运行实现与测试、`data/train/v2|v3/today`；`infra/scoring/profiles/v1` 及其配置、CLI 和包资源消费者。
- 影响边界：候选/评分/风险/融合/选择身份，决策冻结与持久化投影，调度、API/SSE/Web、训练工件白名单和入口配置。
- 排除边界：不迁移推荐 application 端口或第 1–14 层编排，不迁移推荐 infra 适配器，不拆分 `http_api/`，不触碰活动运行数据。
- 退出条件：活动 Python、配置、工件白名单、HTTP schema 和 Web 不再识别 Today/V1；Long 不含分数、名次、TopK 或冻结字段；旧领域 owner 和兼容导出清零。
- 实际改动：将市场、候选、评分、风险、证据、选择和发布领域 owner 原子迁入 `recommendation/domain`，同步所有生产、训练研究、持久化、脚本与测试消费者并删除旧 `domain/market`、`domain/recommendation`、`domain/review`；移除 Today 调度、评分、冻结、API/Web、研究与训练头，移除 V1 profile、入口接受和包资源；Long 改为显式无评分 HTTP 白名单且不提供历史。
- 验证：`pytest --collect-only -q`、阶段 5 推荐领域/application/评分/DeepSeek/调度回归、冻结与持久化、bootstrap/入口/Web、JavaScript 状态合同全部通过；排除用户独立测试命令优化批次的合同套件通过；Ruff、`mypy src/trader`、compileall、`git diff --check` 通过；活动源码最大 1182 行。
- 未验证项：未连接真实行情供应商或 DeepSeek，未读取或修改活动运行数据，未执行隔离数据迁移演练、真实浏览器进程验收和阶段 12 发布级全量门禁。
- Review：以 `caee0a8c` 为基线完成两轮完整 diff 审查；旧领域/V1 owner 和活动导入清零，Today 仅在退役断言、历史记录和非策略“今日”语义中保留；无兼容转发、重复 owner、隐藏空对象 fallback、TODO、生成物或阶段 6 越界改动；Long 输出白名单不含分数、名次、TopK、选择诊断和冻结字段。
- 下一阶段边界：阶段 6 - 迁移推荐端口和第 1–9 层；不得提前迁移第 10–14 层、冻结发布、推荐基础设施、调度、模型加载或 `http_api/`。

- 阶段：4 - 迁移离线研究、回放、留出与结果评价
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 范围：将剩余 research/outcome 的领域值对象、离线应用、归档适配器、回放、留出、评价和报告迁入 `training/` 对应边界；保持点时、防穿越、成本一次、终端留出只开一次和不可变证据身份。
- 影响边界：`src/trader/training/`、研究命令延迟入口、研究脚本/测试、架构契约和工程说明。
- 排除边界：不迁移推荐生产领域、模型在线加载、HTTP/Web、评分/冻结和活动数据；不改变研究结论或自动改配置。
- 退出条件：旧 research/outcome 顶层层内路径清零，研究命令只由 `training/entrypoints/commands.py` 延迟分派，下载、训练研究与实时推荐之间无可变状态共享。
- 实际改动：将剩余 research/outcome 领域、应用与基础设施 owner 迁入 `training/domain/evaluation`、`training/application` 和 `training/infra/research`；历史快照、控制和修订合同归入 `download/domain`；研究命令迁入 `training/entrypoints/commands.py` 并保持 CLI 分支内延迟加载；同步脚本、生产只读消费者、测试、架构契约和工程说明，删除旧 research/outcome Python owner。
- 验证：`compileall`、阶段 4 范围 Ruff、`mypy src/trader`、`git diff --check` 通过；应用/领域研究与 outcome 测试、研究基础设施非慢测、研究工件组件测试、入口/架构/功能包/outcome/历史研究契约全部通过；普通 `trader.entrypoints.cli` 导入未加载 evaluation/research 模块。
- 未验证项：明确标记 slow 的 300 证券月分片历史行业审计在长时执行后中止；未运行真实供应商、真实研究数据、终端留出实盘式演练、隔离数据迁移和发布级全量门禁。
- Review：旧 `application/research`、`application/outcomes`、`domain/research`、`domain/outcome`、`infra/research` 活动 `.py` 与旧 import 清零；无兼容导出、双实现、自动配置/评分/profile 切换或运行数据写入；下载与训练研究只通过不可变历史身份和工件边界交互。
- 下一阶段边界：阶段 5 - 迁移推荐领域并原子切换 Tomorrow/D25/Long 与 V2/V3；不得提前迁移推荐应用端口、14 层编排、HTTP/Web 或模型加载基础设施。

- 阶段：3 - 原子迁移训练核心与 V2/V3 工件所有权
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- 范围：迁移训练特征、数据集、样本构建、模型拟合、资源约束、训练 due/input 与 V2/V3 工件 codec、校验和原子发布到 `training/`；保持 train-v2/train-v3 输入与输出合同。
- 影响边界：`src/trader/training/`、训练入口/脚本、V2/V3 工件契约及训练定向测试。
- 排除边界：不迁移���荐侧模型加载/推理、离线研究回放、策略评分和运行数据；不触碰活动训练数据。
- 退出条件：两个训练命令从入口到工件发布只有一条链，训练与推荐之间仅通过不可变 V2/V3 工件边界；训练旧 owner 与兼容导出清零，定向资源/确定性门禁通过。
- 实际改动：新增 `training/application`、`training/domain`、`training/infra/history`、`training/infra/profile/v2|v3`、`training/infra/artifacts` 与训练包入口；迁移训练进度/输入、history due、样本构建、模型拟合、内存证据、V2/V3 合同及工件 codec/locator/repository/predictor/profile；同步 train-v2/train-v3、下载归档消费者、推荐组合边界、架构契约与工程设计。删除旧 `application/training`、旧训练 `application/research`、`infra/scoring/training`、`infra/scoring/head_bundles` 与 V2/V3 训练实现空壳。
- 验证：`compileall`、阶段 3 范围 Ruff、`mypy src/trader` 通过；架构/功能边界契约、入口/特征契约、history training due/input、V2/V3 profile、模型拟合、样本仓库、训练应用和进度测试通过；V3 profile 完整测试 12 passed；训练迁移后契约路径已同步到 `training/`。
- 未验证项：未运行真实历史训练、实时 BaoStock、隔离数据迁移演练、发布级全量门禁；资源上限与 251/61 日长时训练仅由现有定向契约和静态边界验证，未执行生产数据训练。
- Review：训练旧 owner 路径与旧导入清零；training 内部依赖遵循 `entrypoints/infra -> application -> domain`，推荐在线加载器仍保留为阶段 8 边界，未复制训练实现；无兼容导出、双写或运行数据变更。
- 下一阶段边界：阶段 4 - 迁移离线研究、回放、留出与结果评价；只迁移剩余 research/outcome 研究职责到 training 对应 evaluation 边界，不提前切换推荐模型加载、评分或 API/Web。

- 阶段：2 - 原子迁移下载业务
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- Python：系统 `python3` 为 3.10.12；项目 `.venv/bin/python3` 为 3.11.15
- 范围：将历史下载、BaoStock 同步、控制库、年月分片、snapshot 校验和维护锁迁入 `download/`，保持零参数 `download` 命令和原子 active snapshot 语义；同步更新下载消费者、契约测试和工程布局文档。
- 影响边界：`src/trader/download/`、下载入口/脚本、历史下载测试、架构/功能边界契约和本实施台账。
- 排除边界：不迁移训练、离线研究或推荐业务 owner，不修改评分、冻结、API、Web 或运行数据。
- 实际改动：完成 `download/domain`、`download/application`、`download/infra` 与 `download/entrypoints` 原子迁移；迁移 BaoStock 会话/同步、控制库、年月分片、snapshot 校验、维护锁、进度与状态投影，更新 CLI、脚本、测试和包资源；旧 `application/history` 与已迁移 `infra/research` owner 清零。
- 验证：下载核心契约与历史同步定向测试全部通过；`compileall`、下载范围 Ruff、mypy 通过；全仓旧下载路径扫描无结果；`git diff --check` 通过。
- 未验证项：未运行实时 BaoStock、隔离数据迁移演练、慢测试分组和发布级全量门禁；这些不在阶段 2 的最小充分门禁内。
- Review：下载专项门禁后复核完成；活动源码/测试无旧下载 owner 导入，旧 `application/history` 与已迁移 `infra/research` 文件路径无活动 `.py`，无兼容导出或已跟踪生成物；`history_training_due/input` 明确保留到阶段 3 训练边界。
- 退出条件：下载命令只加载 `download/` 与共享 primitive，失败不移动旧 active snapshot，旧历史实现路径和兼容导出为零；专项门禁通过后提交并推送同名上游。
- 下一阶段边界：阶段 3 - 原子迁移训练核心与 V2/V3 工件所有权；不得提前迁移推荐模型加载或生产推理。

- 阶段 4 测试切片记录：研究/回放/留出契约已从旧 `application/research`、`domain/research`、`infra/research` 路径切换到训练评价边界；默认 `test-fast` 收敛为 395 个稳定用例，阶段 4 专项契约 26 passed，`compileall` 与 Ruff 通过。旧路径文本仅作为历史迁移证据保留。
