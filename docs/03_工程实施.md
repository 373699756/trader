# 工程实施进度

## 当前阶段

- 阶段：1 - 收敛共享 `infra/` primitive 和依赖守卫
- 状态：`completed`
- 分支：`Afuture`，跟踪 `origin/Afuture`
- Python：系统 `python3` 为 3.10.12；项目 `.venv/bin/python3` 为 3.11.15
- 范围：固定共享技术 primitive 的顶层 owner，补齐业务层依赖方向、HTTP/Web 只读边界和泛称命名守卫；不改变运行行为。
- 影响边界：`src/trader/infra/` primitive、架构/功能边界契约测试和本实施台账。
- 排除边界：不迁移下载、训练、研究或推荐业务 owner，不修改评分、冻结、API、Web、数据布局或运行数据。
- 实际改动：确认时钟、设置、原子 JSON、通用有界缓存、进程锁和运行资源六类共享 primitive 已由顶层 `infra/` 单一 owner 持有；新增架构契约，扫描全仓定义并阻止应用层重新持有基础设施实现。
- 验证：新增架构守卫、功能边界和专业命名定向测试共 49 passed；`make quality` 通过（Ruff 格式/lint、重构质量、mypy、默认非慢测试）；`git diff --check` 通过。
- 未验证项：未运行慢测试、实时供应商、浏览器和发布级全量验收；本阶段无运行行为或包资源变化，因此这些门禁留给阶段 12 或相应业务阶段。
- Review：完整 diff 仅包含阶段 1 契约守卫与台账；没有重复 owner、兼容导出、隐藏 fallback、生成物或越界业务迁移。
- 退出条件：共享 primitive 单一 owner 守卫通过，依赖方向与命名守卫通过，定向门禁与完整非慢测试通过，工作树只包含本阶段文件，提交已推送到同名上游。
- 下一阶段边界：阶段 2 - 原子迁移下载业务；只处理下载 domain/application/infra 与入口，不提前迁移训练、研究或推荐链。
