# Point-in-time 数据资格报告（2026-09-08）

## 结论

资格审计已完成，结果为 `historical_data_insufficient`。日线、历史行业、历史分钟三门均独立失败关闭；
`point_in_time_parity=false`、`terminal_holdout_opened=false`、`production_authority=false`。本报告不表示
V3 可训练、可启用或未来收益已经提高。

## 审计证据

| 数据门 | 本次证据 | 结论 |
| --- | --- | --- |
| 2000 日日线 | 只读识别 22 个根级旧 checkpoint；268/5453 只完成、5185 只未完成，无正式 manifest | `historical_data_insufficient` |
| 历史行业 | 未完成至少 300 只分层样本；`effective_to`、`queried_at` 和完整历史有效性尚不能证明 | `historical_data_insufficient` |
| 历史分钟 | 600519、2022-01-04 单股探针未取得 11:20/14:50 历史分钟 bar，也未证明量额、时区、raw/qfq 与公司行动语义 | `historical_data_insufficient` |

当前磁盘可用空间约 14GiB，低于全量 2000 日任务的 25GiB 启动门。一次全量入口尝试在供应商请求前即以
`disk_below_25gb` 退出，没有下载股票或改写已有归档；此后按用户要求不再执行全市场下载。

## 单股工程 smoke

统一历史诊断仅请求 600519、61 日、1 个 worker，腾讯直连得到 1/1 可用观测：普通特征历史和 Outcome
raw/qfq 配对均可用，零空结果、零错误，单次供应商延迟约 819ms；报告输出到仓库外 `/tmp`，没有持久化。
这只证明日线链路可工作，不代表 5453 只覆盖、2000 日窗口、历史行业或分钟点时资格通过。

## 本批行为变化

- checkpoint 状态读取同时识别当前 `shards/` 与根级旧分片，不再把已保存的 268 只显示为 0；完成记录覆盖
  同代码陈旧失败。
- 下载锁内可以校验并迁移旧分片，迁移后保留 recovery 副本；状态查询和资格脚本保持只读。
- 日线 manifest 只绑定日线 batch，不再依赖或伪造历史行业训练事实；历史行业修复不得改写封存日线。
- BaoStock 行业只接受供应商实际 `updateDate`，拒绝缺失、未来或同日冲突事实；当前来源仍未取得资格。
- 新的有类型资格报告把三门结果白名单投影为脱敏 JSON，任一门不足都阻止点时数据集和 V3 后续阶段。

## 后续阻塞

需要先提供至少 25GiB 可用空间并由用户再次明确允许，才能续传全市场日线；随后还需独立完成至少 300 只
历史行业来源评估，并选择能通过分层覆盖门的真实历史分钟来源。在这些条件满足前，不运行 V3 正式训练，
不读取终端留出，不改变生产评分档位。
