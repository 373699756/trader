[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: long-running scoring or runtime tests
    integration: tests that exercise Flask endpoints, snapshots, persistence, or runtime workflows
