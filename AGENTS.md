# AGENTS.md

本文件适用于整个仓库。所有自动化代理和人工协作者修改项目时都必须遵守。

## 1. 当前项目与权威来源

- 产品是仅运行在个人 PC 上的本地 A 股研究看板，不提供真实下单、多租户或移动端业务。
- 桌面验收使用当前稳定版 Chrome、Edge 或 Firefox，分辨率为 1280x720、1440x900、1920x1080。
- `docs/01_评分逻辑.md` 是候选、过滤、评分、风险、融合和排名的唯一权威。
- `docs/项目重构详细.md` 是产品、架构、时间线、API、Web、运维和验收的唯一权威。
- `pyproject.toml` 是依赖、构建、包资源和公开入口的唯一权威。
- `docs/02_工程设计.md` 是待随阶段迁移的现行工程说明，不得覆盖 `docs/项目重构详细.md`；行为实际切换时按蓝图同步更新。
- `docs/03_工程实施.md` 是本轮重构唯一活动进度台账；不得在 Changelog、其它计划或临时文件中维护第二份进度。

## 2. `Afuture` 目标边界

- 活动产品代码只能位于 `src/trader`。目标顶层业务只有 `download/`、`training/`、`recommendation/`；`http_api/` 和 `web/` 是推荐业务的只读交付边界；顶层 `infra/` 只保留跨业务技术 primitive。
- 三个业务内部依赖方向固定为 `entrypoints/infra -> application -> domain`。`domain` 不读取配置、时钟、网络、文件或数据库；`application` 不导入本业务 `infra`、Flask 或其它业务实现。
- `http_api/` 与 `web/` 只能调用 `recommendation/application/` 的只读用例，不得抓行情、训练、评分、调用 DeepSeek、访问业务数据库或写运行状态。
- `bootstrap.py` 是唯一组合根；`trader-cli` 与 `trader-server` 只调用它。禁止第二组合根、全局服务定位器和适配器隐藏创建外部客户端、连接池或线程池。
- `create_app()` 必须无线程、无网络、无数据库和无文件写入副作用。
- 业务间只传递有真实类型的不可变对象。内部状态不得用 `dict`、`Any`、反射或空对象 fallback 代替；JSON 只存在于配置/供应商解析、持久化 codec、schema 事件和 HTTP/可观测性投影边界。
- 输入边界先解析为类型对象，输出边界由 adapter 按公开字段白名单投影。内部对象不得用 `as_dict()`、`to_json()` 或双表示自行决定公开 schema。
- 新增或迁移的项目符号不得使用 `Store`、`Stored`、`*Store`、`Repository`、`Archive`、`Registry` 等泛称；使用能表达实际职责的名称，或按真实语义使用 `Index`、`Cache`、`Snapshot`。
- 项目版本身份只允许评分档位 V2/V3。不得新增其它 `vN` 项目版本命名；供应商模型名、Python/包版本、日期和随机种子不属于项目版本身份。
- 一个职责只能有一个活动所有者。禁止转发模块、兼容导出、双实现、双读、双写、重复状态源和隐藏 fallback。
- 活动源码单文件最多 1200 行；按职责和耦合拆分，禁止为满足行数制造含义模糊的聚合模块。
- 旧目录、Today、V1 和旧资源只是在阶段 5/11 前待迁移或待删除的现状，不是目标能力；不得为它们新增长期接口或兼容分支。

## 3. 不可破坏的业务合同

- 目标生产策略只有 Tomorrow、D25 和不评分的 Long；Long 只展示当前快照，不排名、不进入 TopK、不冻结、不写推荐历史。
- 融合固定为 `clamp(local_score * 0.68 + deepseek_score * 0.32 - deepseek_risk_penalty, 0, 100)`，使用 `ROUND_HALF_UP` 保留两位小数；`local_score` 已扣本地风险，禁止重复扣除。
- DeepSeek 自由文本不得直接扣分；每日物理 HTTP 请求全局上限为 168，重试和失败同样计数，预热与 emergency 使用独立原子预算桶。
- Tomorrow/D25 在 14:50 first-wins 冻结。已有同日正式记录不得被迟到结果覆盖；同日记录缺失时，15:00 后只能按权威合同创建一次合法 `close_fallback`。
- 所有业务时间使用可注入且带时区的 `Asia/Shanghai` 时钟；deadline 和时长使用单调时钟。
- 数据源、模型或 DeepSeek 失败不得阻塞合法的本地降级和只读 Web；保留最近有效快照并显式标记来源年龄、失败类别和降级状态。

## 4. `Afuture` 强制实施顺序

- 当前工作分支固定为 `Afuture`；不得重建、改名、删除或改写已有 `branch`。
- `docs/03_工程实施.md` 为空时从阶段 0 开始；非空时只执行其中最小未完成阶段。用户每次“继续”只实施一个完整阶段，提交并推送后立即停止。

| 阶段 | 唯一主目标 |
| --- | --- |
| 0 | 闭合基线阻塞并确认分支、Python 和工作树 |
| 1 | 收敛共享 `infra/` primitive 和依赖守卫 |
| 2 | 原子迁移下载业务及入口 |
| 3 | 原子迁移训练核心及 V2/V3 工件所有权 |
| 4 | 迁移研究、回放、留出和结果评价 |
| 5 | 迁移推荐领域并切换 Tomorrow/D25/Long、V2/V3 |
| 6 | 迁移推荐端口和第 1–9 层 |
| 7 | 迁移第 10–14 层、冻结和发布 |
| 8 | 迁移推荐基础设施、调度和模型加载 |
| 9 | 拆出 `http_api/` 并保持主页行为 |
| 10 | 收敛组合根、入口、包资源和数据迁移工具 |
| 11 | 删除旧活动树并完成全局命名清理 |
| 12 | 对阶段 0–11 完整 diff 执行发布级验收 |

- 每阶段必须完整执行 `docs/项目重构详细.md` 第 10.4 节对应子项，并从第 10.3 节登记当前 owner、目标 owner、全部消费者和必删路径；不得跳号、拆走容易部分或提前实施后续阶段。
- 迁移使用“新 owner + 全部消费者 + 旧路径删除”的单次原子切换；无法确定所属业务时先停止并完成所有权裁决。
- 阶段 0–11 都是不可发布中间态。阶段 10–12 只在仓库外隔离副本演练数据迁移，不触碰活动数据。
- 阶段 12 通过仅表示形成候选；PR、合并、tag、真实数据切换和发布都需要用户另行明确授权。

## 5. 单阶段交付流程

1. **开始**：读取本文件、两份权威文档、蓝图对应阶段、相关代码/配置和 `$trader-delivery` skill；确认当前分支及其上游、工作树已有修改和本批文件范围。所有直接 Python 命令只用 `python3`，虚拟环境只用 `.venv/bin/python3`，禁止使用 `python` 或 `.venv/bin/python`。
2. **登记**：在 `docs/03_工程实施.md` 写入阶段号、名称、范围、影响边界、退出条件和 `in_progress`。一次只能有一个未完成阶段。
3. **实施**：先完成本阶段全部代码与资源切换，再集中处理必要测试和文档；保留用户已有修改，不混入未纳入范围的内容。
4. **Review**：以上一个已推送提交为基线检查完整 diff 和新增/删除文件，重点检查错误行为、重复 owner、旧引用、兼容层、隐藏 fallback、TODO、生成物和越界改动；集中修复后重新 Review，直到零已知发现。
5. **验证**：按第 6 节选择实际风险所需的最小充分门禁；记录真实结果和无法验证项，不得把未运行、mock、HTTP 200 或旧进程结果写成通过。
6. **交付**：提交前在台账写入实际改动、验证、未验证项、Review 结论、下一阶段边界和 `completed`。只暂存本批文件，创建一个 Conventional Commit，推送同名上游并确认本地提交已完整到达上游，然后停止。

直接下达的新修改任务是独立批次；先闭合未完成批次，不能混成一个提交。单文件低风险任务可直接实施，不为形式创建多步计划。

## 6. 测试、门禁与归档

- 测试和 Changelog 是风险证据，不是每批必填项。非关键任务禁止为流程形式新增、扩写或修改测试文件，也不运行无关测试。
- 只改 Markdown、注释或非运行元数据时，默认只 Review 完整 diff 并运行 `git diff --check`。
- 关键业务行为、公开合同或重要回归风险变化，在完整实现切片结束后集中补最小充分测试并运行定向门禁。改 Python 逻辑时对受影响范围运行 Ruff；公共类型或复杂边界变化时再运行 mypy。
- 阶段 0–11 默认不重复全量门禁；用户明确要求、公开契约立即切换或 release/cutover 修复可提前升级。阶段 12 必须统一运行 `make format-check`、`make lint`、`make type-check`、`make test`、`make package` 及蓝图要求的全部专项验收。
- 只有用户可观察行为、评分/冻结/风险、公开 API/SSE/schema、配置或持久化格式、数据迁移、依赖/入口/打包、重要缺陷、性能资源边界、release/cutover 或权威文档规范变化，才修改根 `CHANGELOG.md` 并建立 `docs/changelog/` 记录。
- 流程说明、代理规则、进度同步、普通重命名、目录整理、测试维护和无行为变化的内部迁移不写 Changelog。
- 行为实际切换时才更新对应权威文档；`docs/03_工程实施.md` 只保存状态和证据摘要，不复制业务合同。

## 7. 实现与 Git 安全

- 支持 Python 3.10–3.14；沿用 Ruff、mypy、pytest 和 `src` 布局，不引入第二套工具或配置真相源。
- 核心计算保持纯函数；网络、SQLite、线程、文件和时钟通过边界显式注入。外部 I/O 必须有超时、上限和明确失败策略，后台资源必须可停止、可等待、可观察。
- 捕获最窄异常并保留因果链；日志、文档和提交不得包含密钥、完整外部载荷、运行数据或个人敏感路径。
- 可复用诊断和实测优先扩展 `scripts/` 既有框架，默认只读并输出到标准输出或显式仓库外路径；不得反复在 `/tmp` 或 shell heredoc 中重写同类工具。
- 所有手工文件修改使用 `apply_patch`。禁止 `git reset --hard`、`git checkout --` 和擅自删除用户修改。
- 提交前检查 `git status`、完整 diff、`git diff --check` 和暂存区；不得提交 `.runtime/`、`.venv/`、`dist/`、`build/`、缓存、截图或临时数据。
- 每个独立批次只创建一个新提交并立即推送；禁止 amend、squash 或改写已推送历史。推送失败时保留本地提交并停止，成功核对上游后才能继续。
- 回退必须切换完整旧 release 及匹配数据副本；新数据不得写回旧运行库，不得现场破坏性改库。
