# AGENTS.md

本文件适用于整个仓库。所有自动化代理和人工协作者在修改本项目时都必须遵守。

## 1. 项目目标与运行范围

- 产品是仅运行在个人 PC 上的本地 A 股研究看板，不是多租户 SaaS，不提供真实下单能力。
- 目标浏览器为当前稳定版 Chrome、Edge 或 Firefox 桌面版；验收分辨率为 1280x720、1440x900 和 1920x1080。
- 手机和平板浏览器不属于产品范围，不得为了移动端增加新的业务分支或运行依赖。已有窄屏 CSS 可以保留，但不作为发布门禁。
- `docs/02_工程设计.md` 是产品、架构、时间线、API、运维和验收契约的唯一权威；`docs/01_评分逻辑.md` 是候选、过滤、公式、风险、融合和排名策略的唯一权威；`pyproject.toml` 是依赖、构建和入口的唯一权威。

## 2. 固定架构边界

- 活动产品代码只能位于 `src/trader`。
- 依赖方向固定为 `entrypoints/web/infra -> application -> domain`。
- `domain` 不得读取配置、时钟、网络、文件或数据库。
- `application` 不得导入 `infra`、Flask 或旧包。
- `web` 只能调用应用层只读用例；HTTP 请求不得抓行情、评分或调用 DeepSeek。
- `bootstrap.py` 是唯一组合根。禁止全局服务定位器和隐藏构造外部客户端。
- `create_app()` 必须保持无线程、无网络、无数据库和无文件写入副作用。
- 新代码不得导入 `stock_analyzer`。v2 切换完成后，活动树中不得保留旧业务实现、旧测试或旧 Web 资源。
- 活动源码单文件最多 1200 行；接近上限仍须按职责、耦合和可测试性独立 Review，禁止为满足行数机械拆分或新增含义模糊的聚合模块。

### 2.0 版本命名边界

- 只有评分生产档位及其评分模型身份允许使用 `V1`、`V2`、`V3`（大小写不影响语义）。这些名称仅表示用户确认的评分策略选择。
- 未经用户在当前交付批次明确确认，任何其他逻辑都不得新增或扩展 `V4`、`V5`、`vN` 等项目版本控制，包括 API、SSE、配置、运行目录、数据集、特征、报告、缓存、模块、测试和 Web 资源。
- 已存在的非评分版本字符串若属于不可变历史审计、持久化解码或跨进程数据身份，只能原样只读兼容，不得继续晋级、分叉、自动迁移或作为新功能命名；新代码不得复制这种命名模式。
- 供应商实际模型名（例如 `deepseek-v4-*`）、Python/包发布版本、日期、随机种子和 SHA-256/content hash 是外部身份或完整性信息，不属于本项目版本控制，不得改名冒充评分版本。
- 修改版本边界前必须先获得用户明确确认，并在同一批次同步更新契约、消费者、测试、两份权威文档和 `CHANGELOG.md`；未获确认的改动应停止在稳定无版本命名。

### 2.1 JSON 与类型对象边界

1. 进程内业务状态、控制状态和诊断状态统一使用不可变且字段有真实类型的值对象；`status()` 的根返回值
   和集合值不得使用 `dict`、`Mapping`、`JsonObject`、`Any` 或 `object` 代替状态类型。
2. 来源、数据集、策略等运行期动态键允许使用 `Mapping[Key, TypedStatus]`，但 `Mapping` 只是有类型对象
   内部的键控集合，不是外部 JSON schema；进程内调用者必须读取对象字段，不得依赖 JSON 字段下标、
   `.get()`、反射或空字典 fallback。
3. JSON 只允许存在于明确的序列化边界：配置和供应商载荷解析、持久化 codec、schema 约束的不可变
   事件载荷，以及 Web/API/可观测性响应投影。输入边界必须先解析为类型对象再进入应用流程；输出边界
   必须由对应 adapter 按公开字段白名单显式投影，不能把内部对象直接透传为 JSON。
4. 领域、应用和内部基础设施状态对象不得定义 `as_dict()`、`to_json()` 或 `to_status()` 自行决定线格式；
   需要转换时使用边界 adapter 的模块级投影函数，并显式处理枚举、日期时间、动态键及脱敏。只有本身
   就是外部可观测性契约的端口或 adapter 可以返回 JSON 形状。
5. 内部类型增加字段不得自动改变公开 JSON schema；公开字段变化必须同步修改 adapter、schema/API
   契约测试、`docs/02_工程设计.md` 和 `CHANGELOG.md`。禁止为兼容旧字典调用保留双表示、
   临时转换层或隐藏 fallback。
6. `tests/contract/test_architecture.py` 的序列化边界 AST 契约是强制门禁。修改状态接口或新增序列化
   边界时，先完成本节实现，再在整节收尾时集中更新必要的关键契约测试；不得为每个小字段调整重复写测试，
   也不得通过扩大豁免路径绕过本节。新增豁免必须证明该模块是最终外部边界，并在需要归档的重要交付中说明理由。

### 2.2 存储职责命名边界

1. 新增或重命名的项目自有模块、类型、函数、参数和局部变量不得使用 `store`、`stored` 或 `*Store`
   这类无法表达所有权与行为的泛称；必须按实际职责选择 `Repository`、`Archive`、`Registry`、`Index`、
   `Cache`、`Snapshot` 或更具体的业务名称。
2. 外部协议、第三方 API、SQL/HTTP 标准字面量和不可变历史审计中的原始 `store` 文本可以原样保留，
   但不得据此为新项目符号复制该命名；既有活动符号应在其所属重构批次迁移，不得新增双命名兼容层。
3. 相关迁移先完成本节代码和引用调整，再在整节收尾时把旧路径、旧公共符号和目标边界集中纳入
   `tests/contract/test_professional_naming_contract.py` 的关键 AST 契约；不得为单个局部变量改名逐次增加测试。

## 3. 不可破坏的业务约束

- 融合公式固定为 `clamp(local_score * 0.68 + deepseek_score * 0.32 - deepseek_risk_penalty, 0, 100)`，最终使用 `ROUND_HALF_UP` 保留两位小数。
- `local_score` 已扣本地风险，融合时禁止再次扣除 `local_risk_penalty`。
- DeepSeek 自由文本不得直接决定扣分；只允许本地规则映射经 schema 和证据校验的风险事实。
- DeepSeek 每日物理 HTTP 请求全局上限为 168，重试和失败同样计数；共享预热与 emergency 必须使用独立原子预算桶。
- today 在 11:20 冻结，tomorrow/d25 在 14:50 冻结。已有同日正式记录在冻结后不得被迟到结果修改；today 错过 11:20 后不得通过启动检查点、旧发布链或收盘行情追补，同日保持 `not_ready`，已有正式记录只更新报价 overlay；tomorrow/d25 同日正式记录缺失时，15:00 后允许优先固化本次运行 当前决策，或在冷启动时按同日收盘行情执行一次本地补算并创建 `close_fallback`，创建后同样不可覆盖。
- long 只展示当前快照，不冻结、不写推荐历史。
- 所有业务时间使用 `Asia/Shanghai`，时钟必须可注入且带时区。
- 数据源或 DeepSeek 失败不能阻塞本地推荐和只读 Web；必须保留最近有效快照并显式标记降级。

## 4. 强制执行流程

涉及仓库修改的产品、工程和重要文档任务才构成交付批次。纯解释性问答、只读 Review、状态说明和不落盘的讨论不建立计划、不写 Changelog，也不执行提交或推送。单文件、目标明确且低风险的小改可直接实施，不必为了形式创建多步计划；跨模块、高风险或需要持续跟踪的任务才使用可验证计划，一次只允许一个计划项处于 `in_progress`。不得把多个互不相关的产品目标混入同一批次；为实现统一目标架构而纳入的工程重构按第 4.2 节执行。

测试与 Changelog 是风险证据，不是每批必填的流程文件。本文件第 4.1.1、4.6 和 5 节对是否
新增或修改测试、是否写 Changelog 具有最终裁决权；计划、Skill 或其他流程文档中的通用“更新测试/
交付记录”描述必须按这些风险规则解读，不得用来扩大低风险批次的文件范围。

### 4.1 “继续”命令与整节任务边界

1. 用户每发送一次“继续”或语义等价的继续执行指令，都视为一个新的独立交付批次；该批次必须处理计划或需求文档中下一个完整未完成章节，以同级标题为章节边界，并完成章节内全部明确子项。
2. 同一章节可以包含多个可独立验收子项，必须作为一个整节任务统一实现、回归、Review、提交和推送；不得完成首个子项后提前停止，也不得擅自实施相邻章节或扩展无关产品需求。为形成最优目标架构而纳入的跨模块或全仓工程重构不视为相邻产品任务，但必须在本批计划、Review 和验收中完整覆盖。
3. 开始前记录当前 `HEAD`、上游分支、工作树已有变更和本任务文件范围。此前批次如尚未完成 Review、提交或推送，必须先闭合此前批次，不能把两个批次合并。
4. 当前整节任务完成全部子项、Review、提交并成功推送后必须停止，不得自动执行计划中的下一章节；等待用户下一次“继续”。
5. 用户直接下达且需要修改仓库的新任务形成独立交付批次；纯问答和只读任务不属于交付批次。用户明确指定完整章节时按整节交付，否则多个互不相关目标仍拆成多个任务逐次交付。

#### 4.1.1 大任务与子任务的验证边界

1. “大任务”是用户一次明确提出的整体目标，或计划文档中需要连续完成的完整章节集合；“子任务”是为完成该大任务而拆出的一个可独立 Review、提交和推送的实现切片。拆分子任务不改变依赖边界、完成条件或提交纪律。
2. 子任务是否需要测试由实际风险决定。只有涉及关键业务行为、公共合同、重要回归风险或用户可见结果的关键任务，才在完整子任务或同级章节代码完成后集中补充必要测试，并在切片末尾运行一次直接覆盖风险的定向检查；非关键任务禁止为了流程形式新增、扩写或改动测试文件，也不运行无关测试。不得在每个小改、字段调整或 Review 修正后重复编写和执行测试，也不得因为子任务属于高风险大任务就在每个子任务末尾重复执行全量测试、打包或仓库外安装。
3. 大任务的最后一个子任务完成后、标记整体计划完成前，统一运行一次完整门禁：`make format-check`、`make lint`、`make type-check`、`make test`、`make package`，并追加该大任务适用的性能、浏览器、冻结、API/SSE、安装或其它发布专项门禁。全量结果必须覆盖大任务的完整合并 diff，而不是只覆盖最后一个子任务。
4. 若用户明确要求立即全量验证，或某个子任务本身就是最终 release/cutover、公开契约切换或需要立即证明的线上修复，可以在子任务阶段提前执行完整门禁；应在计划和当前批次交付记录中说明升级原因，后续大任务收尾仍需确认完整合并 diff 已覆盖。
5. 只有符合第 4.6 节归档条件的子任务才写交付记录并修改根 `CHANGELOG.md`；不符合时两者都不得为流程形式修改。需要归档时，其 `Verification` 写明本次实际运行的关键门禁，以及“全量门禁待大任务收尾统一执行”。大任务收尾记录必须列出唯一一次全量门禁结果和未适用项，不得把未运行的检查写成通过。

#### 4.1.2 重构实施进度台账

1. `docs/03_工程实施.md` 是跨阶段重构的唯一活动进度台账。新的整体重构开始前先清空上一批已结束内容，后续只在该文件同步当前大任务的阶段进度。
2. 每个阶段开始时记录阶段名、基线 hash、范围和 `in_progress`；完成 Review 后再记录实际验证、提交 hash、推送核对和 `completed`。一次只能有一个 `in_progress` 阶段。
3. 进度台账只保留任务状态、证据摘要、阻塞和下一阶段边界，不复制两份权威文档的业务合同，也不代替 Changelog 归档。
4. Changelog 不得用作日常实施进度日志。阶段是否写 Changelog 仍且只按第 4.6 节判定；普通重命名、目录整理、进度同步、测试维护和无行为变化的内部迁移不写 Changelog。

### 4.2 工程方案选择与重构原则

1. 实施前必须确认问题根因、当前结构、目标架构和受影响边界，并比较局部补丁与系统性方案；计划必须记录选择依据和完成条件，不能把推测当根因。
2. 方案默认按正确性、架构一致性、长期维护成本、可测试性、失败降级、性能和兼容成本的综合结果择优。不得以最小 diff、最少文件、最少改动行数或最短运行链为目标，也不得因为重构范围大而退回已知较差方案。
3. 改动范围不受原模块、文件数量或当前调用链限制。允许纳入有明确整体收益的跨模块或全仓工程重构，包括统一接口与数据流、消除重复实现、删除废弃链路和偿还会妨碍目标架构的既有技术债；相关契约、迁移或兼容处理、测试和文档必须在同一批次闭合。
4. 禁止用“先跑起来”代替完整修复，默认不得新增临时适配层、双实现、隐藏 fallback、重复状态源或继续保留应删除的旧链。只有权威分阶段计划明确要求或存在本批无法解除的外部阻塞时才允许临时方案，并必须记录退出条件、后续章节和不可误用边界。
5. “最优”必须由可审查证据支撑，不等于无目标地重写全仓。扩大的工程范围必须服务于清晰的目标架构并形成可验证的最终状态；固定架构边界、不可破坏的业务约束和用户已有变更始终优先。

### 4.3 实施

1. 先阅读 `AGENTS.md`、`docs/02_工程设计.md`、`docs/01_评分逻辑.md`、相关代码、测试、配置和当前 `git status`。
2. 按第 4.2 节选择并完成目标架构需要的全部改动，不以最小改动限制文件或模块范围；保留用户已有变更，不得把未纳入计划和验收的内容混入提交。
3. 改动顺序固定为先完成当前完整章节或实现切片的代码，再集中补充测试。测试只覆盖关键业务合同、首次损坏边界、重要回归风险和最终用户可见结果；普通重命名、注释、局部整理或已被上层测试充分覆盖的小改不强制新增测试。缺陷修复仅在问题可能复发、影响关键行为或缺少现有覆盖时补回归测试。
4. 所有手工文件修改使用 `apply_patch`。禁止使用破坏性 Git 命令。
5. 不提交密钥、运行数据库、快照、缓存、构建产物或浏览器临时文件。
6. 可复用的诊断、性能、数据源或浏览器实测在首次完成后必须参数化并固化到 `scripts/`，默认只读、
   输出到标准输出或显式指定的仓库外路径，并补直接契约测试；后续同类任务必须优先复用或扩展已有脚本，
   不得在 `/tmp`、shell heredoc 或对话工具调用中重复重写。只有经 Review 确认无复用价值的一次性探索
   才可留在临时目录，且不得提交其输出或运行数据。
7. 计划、实现、缺陷修复、性能优化和工程 Review 必须加载仓库级 `$trader-delivery` skill，按其影响矩阵
   选择下游边界、测试和运行实证；纯解释性问答且没有仓库修改请求时不强制加载。skill 不得覆盖本文件、
   两份权威文档或用户授权边界，缺失或无法读取时必须明确报告并继续执行本文件的完整流程。
8. 所有直接 Python 命令必须显式使用 `python3`；使用项目虚拟环境时必须使用 `.venv/bin/python3`。
   禁止调用解释器指向不明确的 `python` 或 `.venv/bin/python`。

### 4.4 每个独立任务完成后的 Review 循环

独立任务实现完成后，不能直接标记完成。必须以上一次已推送提交为基线，针对本任务完整 diff 执行以下循环：

1. 查看该计划项的完整 diff 和新增文件，确认没有越界改动、死代码、重复实现或遗留 TODO。
2. 按改动范围审查适用维度；正确性始终检查，冻结一致性、并发/资源生命周期、错误降级、安全、类型、API 兼容、桌面 UI 和可安装性只在相关边界受影响时检查并留证。
3. 完整章节实现和 Review 修正完成后，关键任务再运行一次风险匹配且足以证明关键行为的测试或检查；非关键任务只需完成 Review 和 `git diff --check`。大任务收尾再运行覆盖完整合并 diff 的全量门禁。
4. 记录所有发现并先集中修改代码；只有发现涉及关键合同或回归风险时才同步补测试。
5. 重新查看 diff；代码继续变化时无需每个小改都立即测试，待本轮 Review 修正整体完成后再执行一次必要验证，直到没有已知审查发现且适用门禁通过。
6. 只有满足上述退出条件后，才把 Review 计划项标记为 `completed`；独立交付任务在提交成功推送并核对上游前必须保持 `in_progress`，整体计划 Review 不能替代本任务的独立 Review 证据。

“完美”在本项目中表示：没有已知未解决问题、需求逐项可追踪、所有适用自动门禁通过、剩余外部风险被明确记录；禁止用主观判断代替证据。

### 4.5 每个独立任务完成后的提交与推送

1. 对当前任务 diff 再做一次独立最终 Review，并修复到零已知发现。
2. 仅当任务符合第 4.6 节的重要归档条件时，更新根目录 `CHANGELOG.md` 的 `Unreleased` 并链接
   `docs/changelog/` 交付记录；不符合条件时不得为了流程形式新增空泛记录。
3. 按第 4.1.1 节和第 5 节判断是否存在适用门禁；只有关键任务才运行相应测试，非关键任务不为形式补测试。子任务不得默认重复全量，大任务收尾必须统一完成完整门禁，不得以拆分子任务为由跳过实际受影响的共享边界。
4. 用 `git status` 和 `git diff --check` 确认没有生成物、空白错误或遗漏文件。
5. 仅暂存本任务文件，使用 Conventional Commit 格式创建且只创建一个新提交；提交信息必须准确描述行为变化，禁止 amend、squash 或复用此前任务提交。
6. 提交后立即推送当前工作分支到其跟踪远端；首次推送使用同名分支建立上游。必须分别读取本地 `HEAD` 与 `@{upstream}` 并确认提交哈希一致，未成功推送的任务不得宣告完成，也不得开始下一任务。
7. 推送只包含当前任务提交。除非用户明确要求，不创建 PR、合并分支、打 tag 或发布版本。

### 4.6 重要交付与修改说明归档

1. 只有以下变化必须更新根 `CHANGELOG.md` 并建立 `docs/changelog/` 交付记录：用户可观察的产品行为、评分/冻结/风险语义、公开 API/SSE/schema、配置或持久化格式、数据迁移、依赖/入口/打包、重要缺陷修复、性能或资源边界、release/cutover，以及两份权威文档的规范性合同变化。
2. 以下内容默认不写 Changelog：纯问答、只读 Review、状态说明、拼写/注释/排版、无行为变化的内部整理、测试自身维护、流程或代理说明的小幅调整，以及用户明确要求不归档的低风险修改。若这些改动实际改变公开行为或关键合同，则按第 1 项归档。
3. 需要归档时，每份记录至少覆盖用户可观察到的症状或明确诉求、原因或现状判断、实际修改及行为变化、关键验证证据、剩余风险与后续项；原因尚未确认时必须写明“待验证”，不得把推测写成事实。连续反馈属于同一根因时合并记录，禁止为每轮问答重复建档。
4. 已完成记录移入 `docs/changelog/` 后必须保留在其索引中；历史根文件内容只能原样迁移到带日期边界的 `docs/changelog/archive/` 快照，不得为压缩体积删除、重写或重排其中的交付事实。
5. 产品、架构、时间线、API、运维或验收契约变化必须更新 `docs/02_工程设计.md`；候选、过滤、公式、风险、融合或排名变化必须更新 `docs/01_评分逻辑.md`；跨边界变化同时更新两份。归纳文档是交付记录，不得与权威契约形成第二套定义。
6. 文档不得写入密钥、令牌、完整外部请求/响应、个人路径中的敏感信息或运行数据；必要证据只保留脱敏摘要、错误类别、计数、版本和验证结论。

## 5. 风险分级质量门禁

完整章节或实现切片完成后，先根据实际 diff、依赖方向和用户可观察行为判断任务是否关键。只有关键任务才选择最小但充分的验证集并运行测试；非关键任务以完整 diff Review 和 `git diff --check` 为准，不新增测试文件，也不运行无关测试。只有符合第 4.6 节归档条件时，才把验证范围写入交付记录的 `Verification`。大任务收尾按第 4.1.1 节统一执行一次完整门禁，并以全部子任务合并后的完整 diff 为对象。

### 5.1 所有批次的基础门禁

- Review 本批完整 diff 和新增文件，运行 `git diff --check`，确认暂存区只包含本任务。
- 只有改动影响运行行为、关键合同或存在实质回归风险时，才运行直接覆盖该风险的测试；不为普通小改强制新增或执行测试。
- 改动 Python 逻辑时在完整章节收尾对受影响文件或包运行一次 Ruff；公共类型或复杂边界变化时运行对应 mypy。只改注释或格式时不重复运行无关检查。
- 定向验证暴露跨边界影响、测试污染或无法证明影响范围时，立即提升到更高一级门禁。

### 5.2 低风险：文档、注释和非运行元数据

仅修改 Markdown、注释、拼写、Changelog 或不影响构建/入口的仓库元数据时，默认只做 Review 和
`git diff --check`。只有文档直接改变机器契约、生成配置、测试收集或两份权威文档中的关键规范时，
才运行对应的文档契约、链接或格式检查。不得运行全量 `make test`、`make package`、仓库外 wheel
安装或三档浏览器验收。

### 5.3 中风险：局部实现

局部纯函数、单一适配器或隔离模块变化，只有触及关键行为、公共合同或实质回归风险时，才在整个
实现切片完成后选择少量直接证明该风险的 unit/component/contract 测试，并对确有逻辑变化的受影响包
执行格式、Ruff 或必要的 mypy。非关键修改以及已有上层覆盖充分、不改变行为的局部整理只做 Review 和
`git diff --check`；默认不因任意一处局部代码改动运行全仓 pytest、打包、wheel 或浏览器验收。

### 5.4 高风险与发布门禁

以下高风险变化所属大任务必须运行完整命令组，默认在大任务最后一个子任务完成后统一执行，不要求
每个高风险子任务重复执行；第 4.1.1 节规定的提前升级场景除外。高风险变化包括：共享领域/应用协议、`bootstrap.py`、线程与生命周期、SQLite schema
或恢复、评分/融合/风险/冻结、DeepSeek 预算、统一行情、API/SSE、配置 schema、依赖/构建/入口、
包资源、活动 Web 行为、旧生产链删除，以及最终 release/cutover 验收。用户明确要求全量验证时
同样执行。

```bash
make format-check
make lint
make type-check
make test
make package
```

高风险批次只追加与改动相关的专项门禁；最终 release 必须全部验证：

- 架构 AST 契约和 `create_app()` 无副作用契约。
- 固定融合向量结果为 `83.40`。
- 预算并发上限、SSE 游标恢复、慢客户端、冻结恢复和哈希一致性。
- 从仓库外安装 wheel 后可导入包、执行 `trader-cli`，并读取模板、CSS、JavaScript 和图标资源。
- 桌面 1280x720、1440x900、1920x1080 无白屏、重叠、页面级横向溢出或明显布局跳动。

## 6. 编码与文档规范

- 支持 Python 3.10-3.14，公共边界和复杂返回值必须有真实类型标注。
- 沿用 Ruff、mypy、pytest 和 `src` 布局；不要引入第二套格式化、依赖或配置真相源。
- 核心计算保持纯函数；网络、SQLite、线程、文件和时钟通过边界显式注入。
- 外部 I/O 必须有超时、上限和明确失败策略；后台线程必须可停止、可等待、可观察。
- 捕获最窄异常，保留异常因果链，不记录密钥、完整外部载荷或敏感数据。
- 注释只解释非显然约束和原因；禁止留下无完成条件的 TODO。
- 业务契约变化必须同步更新对应的两份权威文档、测试和 `CHANGELOG.md` 所链接的交付记录。

## 7. Git 与工作树安全

- 工作树可能包含用户未提交修改，默认全部保留并与之协作。
- 禁止 `git reset --hard`、`git checkout --` 或擅自删除用户变更。
- 提交前检查暂存区，避免把 `.runtime/`、`.venv/`、`dist/`、`build/`、缓存或截图带入提交。
- 每个独立任务保持一个可审计提交和一次对应推送；不得把多个“继续”批次压入同一提交，也不得用后续任务改写已推送历史。
- 推送失败时保留本地提交，记录失败原因并停止；恢复推送并核对上游后才能继续下一任务。
- 回退必须切换完整旧 release；v2 数据不得写回旧运行库。
